# Maugham/Views — area notes

The SwiftUI view layer for the project window. Composes the binder (left), editor (center), and detail pane (right) plus modals and sheets.

## Important files

- `ProjectWindow.swift` — the root view. ViewModifier-extracted modal stack (`CheckpointModifier`, `SessionAndNavigationModifier`, `CollectionPieceModifier`, `RewindModifier`, `PersonaModifier`) to dodge SwiftUI's body type-check ceiling. **When you hit "the compiler is unable to type-check this expression in reasonable time," extract a ViewModifier** — established pattern. `TopChromeModifier` (private to `ProjectWindow.swift`) owns the window's top chrome: the persona bar as a **window-toolbar item** (`.toolbar { ToolbarItem(placement: .navigation) }`, whole toolbar hidden in `⌘\` focus mode) plus the update/backup banners in a top safe-area inset. **Never put permanent chrome in a top `safeAreaInset` wrapped around the `NavigationSplitView`** — the inset is not propagated into the columns' own layout, so it occludes the top of both of them (it ate the binder's segmented picker and the right pane's segment row, 2026-07-25 smoke defect A). The banners survive there only because they are zero-height in the common case. The `NavigationSplitView` carries a `columnVisibility:` binding, written by `CanvasCollapseModifier` and by `PersonaModifier` and by nothing else — `⌘\` on the planning canvas collapses both side columns (spec §8A.3), and `.detailOnly` is the case that looks right and hides the canvas, because the canvas is the CONTENT column. The whole of the reasoning, including why the stash release lives in `PersonaModifier`'s synchronous handler rather than in a later pass, is in `Maugham/Canvas/AREA.md`.
- `PersonaMemory.swift` (in `Maugham/Models/`) — each persona's remembered binder + right-pane segment, persisted in `UIState` (schema v5). `PersonaModifier.applyPersonaChange` records the persona being LEFT and restores the one being entered; transient segments (`.find`/`.trash`) ride through and are never recorded. Do not add per-click recording — departure-time is sufficient and is why the reopen case is correct.
- `PersonaBar.swift` — the four-persona switcher (`PersonaBar` + private `PersonaBarButton`), mounted by `TopChromeModifier` as a window-toolbar item, so it carries no background/padding chrome of its own. Holds no mutation logic — clicking a segment posts `.maughamSetPersona` exactly as ⌘1–4 do, so `PersonaModifier` is the one code path a *writer-initiated* persona switch takes.
- **Persona is also written by two automatic routes, and neither goes through `.maughamSetPersona`** — reaching that handler would mean posting and then writing the binder on top of the segment it restores, a correctness argument resting on SwiftUI delivery order (tripwire 2). The two are `CanvasClaudeArrivalModifier.show` (Show on the Claude-arrival banner; deliberately does NOT record the departing position, because a look is not a choice) and `ManuscriptNavigation.go` (below; deliberately DOES, because ⌘1 has to bring the writer back to the tree they were arranging). Both call `PersonaModifier.applyPersonaChange` or state why they do not.
- `ManuscriptNavigation.swift` — **where a navigation to a manuscript document decides the persona.** Denver, 2026-08-02: *"if I'm moving to the manuscript I'm moving to Author — I shouldn't be writing the manuscript in plan."* `.maughamNavigateToDocument` (a `[[wiki-link]]`, the Inspector's Links row, the stats window), `.maughamNavigateToParagraph` (an annotation row, a history row, a task row) and `.maughamNavigateToScene` (a slugline, in the Scenes navigator or on Plan's Structure tab) all route through it. **Count the array in `ManuscriptForceCensusTests.test_everyNavigationReceiverStillRoutesThroughTheNavigation`, never a number in prose** — the scene notification was the one this list did not name for a whole slice, and it did nothing at all when clicked from Plan (slice 2 review, F2). It has **two** receivers on purpose: this one moves the window to where the script can be read, and `EditorCoordinator`'s scrolls to the slugline; the already-recorded first-click edge at `SceneNavigatorPane.subject(_:whenNavigatingTo:)` is therefore reachable from Plan too, and is deliberately not papered over with a delayed re-post. **The guard is `Persona.showsManuscriptDocuments(for:)` — the persona's own binder registry — never `== .plan`**: Review and Publish centre the editor, and a reviewer ejected into Author cannot adjudicate. A `.project`-scoped navigate writes a background window's persona, and that is intended (the stats window is key; this window is the one that must move). **What is NOT a navigation**: leaving a transient segment. Closing find and emptying the trash also move the binder, name no document, and go to `Persona.binderHome(for:)` in `BinderPaneToggle` / `CollectionBinderPaneToggle` / the `.maughamCloseFind` handler — three routes out of two states that must agree, censused in `ManuscriptForceCensusTests`.
- `ScreenplayScriptSource.swift` — **the second producer of `ProjectWindow.lastParsedScript`, and the reason Plan's Structure tab can list a screenplay's scenes at all.** The first producer is a mounted `EditorCoordinator`, which exists only inside `EditorHost`, which `existingEditorSwitch` mounts only when the centre column is a document — so in Plan the navigator got `nil` and drew "No scenes yet" over a script with ninety scenes (slice 2 review, F1). **A second PRODUCER, never a second value** (tripwire 6): `needsDerivation` is false the moment anything has produced a parse, so the editor always wins and the two cannot disagree. It derives from the **op log** — the live `Document.displayText` when the doc is open, `ProjectStore.derivedCache.displayText` when it is closed — and never from the `.fountain` (tripwire 20 / ADR 0018). Called from one `.task(id:)` on `binderColumn`, keyed on the predicate, so it is one parse per arrival and not one per body pass (tripwire 4). The single (segment × project type) pair that derives is asserted as a census in `ScreenplayScriptSourceTests`, not stated here.
- `BinderPaneToggle.swift` / `CollectionBinderPaneToggle.swift` — the left column's two shells, one per `ProjectWindow.BinderShell`. **Both must answer every `BinderSegment` question the same way**, which is why each shared answer lives on the enum or on `Persona` rather than in an arm: `.tree` (Plan's Structure tab, persona shell slice 2) fans out to `BinderView` / `SceneNavigatorPane` / `CollectionPiecesPane` through `BinderSegment.treePane(for:)`, and the second toggle answering that its own way — `type == .screenplay` inline — is precisely how the 2026-07-02 bug shipped. **Three segment questions are now asked in three deliberately different ways, and the difference is the point.** `centresTheCanvas` is a switch (`.canvas` and `.tree`, spelled once for four sites). `showsManuscriptStatusFooter` is a switch (`.manuscript`/`.scenes`, and `.find` is the row that shows it is *not* "the centre is a document" — find centres the document and the footer is silent under it). The Exports footer is a **derivation**, `segment == .documentHome(for: type)`, because "no Exports list" is the right default for a segment nobody has thought about and "no status footer" is not. All three replaced hand-spelled equalities; `ManuscriptForceCensusTests` walks these files for both the write and the read shapes, with plants and a control.
- `EditorHost.swift` — fragile cluster (see CLAUDE.md tripwires 2/3/6/7). Single-binding contract enforced by `EditorIntegrationHarnessTests`.
- `StatementPane.swift` / `StatementEditorHost.swift` — the Intent (⌘⌥N) and Visual Language (⌘⌥V) panes (M1A). The pane resolves WHICH statement through the pure `StatementPane.effectiveScope`; the host mounts `EditorSurface` (never `EditorHost`) with the sanctioned binding and nothing else. **Several things here are not obvious — count the numbered items, never this sentence.** (1) The text goes through `StatementTextTarget`, a plain reference box, because a statement's `Document` does not exist until the first keystroke mints it — absence is valid — and `makeCoordinator` captures its binding ONCE, so a binding closing over a `@State Document?` would write to the nil it saw at mount. The box also means the mint does not remount the editor mid-word. It is not a second text mirror (tripwire 6): the draft and the `Document` are mutually exclusive by construction. **The draft is KEYED BY SCOPE** (`typedBeforeItsFileExisted`, issue #21) and not a single value, because words with no file yet outlive the pane that was showing them: a single draft had to be emptied when the pane moved on, and `release()` emptied it mid-mint — one character typed into a chapter with no intent, one binder click, and the statement was created without it. Keyed, nothing needs emptying; `release()` no longer touches the words at all and `draft` simply reads the entry for the scope the pane is on now. The alternative that looks cheaper is worse than losing the character: with one draft the second undeclared scope's editor SHOWS the first's words and its mint writes them into the wrong statement (`StatementDraftHandoffTests`' planted-offender run, measured). (2) It is the first SECOND editor alive in a window that already has one — see `Maugham/Editor/AREA.md`'s "Two editors in one window", which is where the two things that broke are recorded. (3) **A `load` is refused on BOTH sides of `Document.load`, and the two refusals ask different questions.** After it, `loadMayBind` asks whether the pane still wants this scope — the superseded-load defect, where a load lands in the box the NEXT scope is using. Before it, and only once the statement's open gate is ours, `gateArrival` asks whether this statement is ALREADY in the box: the host has two openers (its uncancellable mint and its `reconcile`), and a writer who types into an undeclared scope, leaves and comes BACK gives both of them a true `loadMayBind` — same scope, no cancellation — so both bind and the first `Document` is dropped unclosed, two live `Document`s on one file. Waiting on the gate is a delay; the re-ask inside it is the fix, which is `ProjectStore.appendToStatement`'s own discipline. **Both refusals are about BINDING and neither is about the WORDS** (issue #21): every arrival deposits what was typed for its scope into that scope's own file, including the two that turn away — `loadMayBind`'s closes the `Document` it created *after* writing them into it, and `gateArrival`'s, which refuses before the load and so has no `Document` at all, opens one under the gate it already holds and closes it again, but only when there are words waiting.

  **Visual language takes pictures and intent does not** *(Task 12, spec §7)*, on one switch — `takesPictures` — with `StatementImageIngestTests.test_intentTakesNoPicturesAtAll` as the control that it is a switch rather than an oversight. **Paste and drop, and the reason they differ is `MaughamTextView.paste(_:)`'s synchronous contract.** `⌘V` inside the editor goes through the existing `imagePasteHandler` seam (`ResearchNoteEditor`'s) and inserts at the caret — but only when the statement already exists, because the well is derived from `Statement.path` and `vacantStatementPath` makes that path find-or-create's answer rather than a constant. With no statement the handler returns nil and finishes asynchronously on the other route, which is lossless and lands in the same place: an empty statement has nothing to be after. **The drop target is a well BESIDE the editor, never over it** — `EditorSurface` mounts a real `NSTextView`, which is the deeper AppKit view and therefore the one a drag is offered to first, so a SwiftUI `.onDrop` laid over it is a target whose delivery nobody can predict; `PaletteCardEditor`'s dashed strip is the shape. **A finished ingest names its destination and reads no view state to find one** *(review I1, rounds 1–2)*. `take` puts the ref in through `ProjectStore.appendToStatement`, keyed on the `Statement` the ingest itself returned. Two earlier shapes were wrong in the same way and are worth knowing: writing through `target` reached whatever the pane held *now*, and routing on `resolvedScope` did not help, because that marker describes the resolved SCOPE rather than whether the host is MOUNTED — and `⌘⌥N` is not a scope change here at all, since `DetailPaneToggle.segmentContent` gives the two kinds separate `case` arms and this host is **torn down**. The costs were an accepted drop with no text (a ref in a `draft` that a bind for another scope clears, or a `setFullText` on a husked `Document`) and a `visual-language_assets/` ref appended to the writer's *intent* prose. `leave()` now clears `resolvedScope` on the way out so the marker stops lying, but nothing about where WORDS go depends on it any more. **The gap this leaves is deliberate and the fix for it was measured and removed:** a picture landing in a scope with no statement leaves the editor showing its empty draft until the pane is next opened there. Binding the pane to close that gap made the writer's next keystroke `setFullText("a")` over a ref they could not see; unbound, that keystroke MERGES through the mint's `deposit`. A picture you cannot see yet beats a picture you lose. `pictureMessage` is cleared in `reconcile` for the same reason `resolvedScope` is: this host is deliberately not remounted on a scope change, so every `@State` here outlives one.
- `HistoryPane.swift` — read-only forensic timeline. Filter pills + per-row ↺ + header "Rewind…" button. Row rendering branches on synthesisSource enum, not strings.
- `AnnotationsPane.swift` — sibling segment to HistoryPane; action surface for Claude annotations (Accept/Reject/Archive).
- `RewindWindow.swift` — time-travel modal. Per-doc v1; scrubber + Doc/Diff preview + Snapshot/Restore footer. Snapshots the op log at open-time.
- `RewindTickLayout.swift` — pure decimation helper for the scrubber. Unit-tested independent of SwiftUI.
- `PartialRestorePicker.swift` — per-doc-vs-project picker used by checkpoint-row reverts. NOT used by rewind restore (which is per-doc by construction). **The scope it opens on is `initialScope(for:allDocIds:)`, and the checkpoint's recorded `activeDoc` is not trusted to name a document** — it may be `nil`, or (on rows already on disk, tripwire 11) a group id or `BinderSubject.noDocumentSubject`, or a document since deleted. Same membership test the write side makes, `CheckpointCapture.documentSubject(of:in:)`. `seededScope` reads the `@State`'s own box rather than a copy beside it, because a copy left the delivery-path test green against a planted raw seed.
- `CheckpointLabelPromptSheet.swift` — reusable label-entry sheet; used by both ⌘⇧S and "Snapshot here" in the rewind modal.

## Patterns

- **Right-pane mode-swap**, now scoped by persona ([ADR 0005](../../docs/adr/0005-right-pane-mode-swap.md), amended by [ADR 0025](../../docs/adr/0025-persona-shell.md)) — each persona offers a subset of `DetailSegment` via `Persona.panes`. Every pane has one `⌘⌥` letter, dispatched from the View menu in `MaughamApp.swift` rather than from `DetailPaneToggle`, so every one reveals a hidden inspector column (**count `DetailSegment.allCases` rather than a sentence here**; `DocSyncTests` holds the letters against `docs/guide/reference.md`). **A new right-pane surface is a `DetailSegment` case, one entry in `Persona.panes`, one arm in `DetailPaneToggle.segmentContent`, and one `Button` in that View menu** — and nothing in the picker or `ProjectWindow`. The content arm is not optional and not a bug: `segmentContent` is exhaustive with no `default`, so the compiler asks the question. `Persona.swift` claimed for two milestones that `DetailPaneToggle` was untouched; M1A corrected it there and here.
- **Notification-based modal triggers** — `.maughamOpenRewind`, `.maughamShowProjectSettings`, etc. Posted by buttons in subviews, observed by ProjectWindow's `MaughamEvent` receive helpers (see below). Keeps the modal-state ownership in the root view.
- **Scoped window events (ADR 0021).** `ProjectWindow`'s receivers use the `MaughamEvent` helpers exclusively — `.onKeyWindowCommand`, `.onProjectEvent`, `.onGlobalEvent` (grep-verifiable: no hand-written `isKeyWindow` guard survives in this file). Every post declares its scope at the post site; `ProjectWindow` never re-derives one. When adding a new event, pick the scope where you post (`.keyWindow` for menu commands the key window alone should act on, `.project(id:)` for data events windows on that project should see, `.allWindows` for genuine app-wide fan-out), then subscribe with the matching helper — don't hand-roll a filter in the receiver closure. Panes that need to know their own window's liveness (e.g. to gate a receiver) resolve it via `WindowAccessor`, not by caching an `NSWindow?` and treating `nil` as "closed" (`WindowAccessor` caches the window and never re-nils it on close — see `MaughamEvent.isLive`).
- **Dark-mode propagation to side panes** is a known carry-forward (lost twice). Re-check after touching theme code.
- **Empty-state top-anchoring** — Any pane that conditionally shows a `ContentUnavailableView` MUST give it `.frame(maxWidth: .infinity, maxHeight: .infinity)`, AND the enclosing `VStack` (when the pane has a top toolbar + content body shape) MUST get `.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .top)`. Without both, the empty state collapses to its intrinsic size and the parent vertically centers the entire pane — the toolbar floats to the window's middle. Bug recurs every time a new pane is added; the established panes (HistoryPane, AnnotationsPane, OutlinePane, LinkedResearchPane, TasksPane, CollectionPiecesPane) all have this treatment.
- **Inline rename TextField focus claim** — `BinderRow`, `PieceRow`, and `ResearchRow` use the same shape: `.focused($isRenameFieldFocused)` + a `claimFocus()` helper that `Task.sleep(30ms)` before setting focus to `true`. The 30ms deferral lets `List(selection:)`'s focus-claim pass settle first so the TextField wins the gesture race. Both `.onAppear` (for the "row appears in rename mode" path) AND `.onChange(of: renamingItemId)` (for the "context-menu Rename on a visible row" path) trigger the helper — idempotent, either alone is incomplete. **When adding any new rename-capable row, copy the BinderRow shape verbatim.**
- **Close-before-FS-surgery (tripwire 14) — now enforced by construction.** Moving or deleting a file the user might be editing MUST close the writing surface (so its autosave can't recreate the file at the old path) AND flush the research-note debounce, BEFORE the FS call. This discipline is no longer remembered per-site: it lives INSIDE the typed `DocumentStore` mover — `relocate(plan:)` / `relocateUserContent(affectedPaths:perform:)` / `trash(relativePath:using:…)` — which is the only legal way to relocate/trash a user-editable path. See **`Maugham/Stores/AREA.md` → "Typed user-content mover"** for the entry-point table, the boundary (which moves are intentionally left raw), and the `TripwireGrepTests` guard. **When adding a new mover of user content, route it through one of those three entry points — don't re-hand-roll the close+flush.**

## Tripwires

See CLAUDE.md for the full list; the ones touching Views are:

- #2 (no SwiftUI ↔ AppKit flag-based loop guards), #3 (no heavy work in synchronous SwiftUI binding setters), #4 (no O(N²) per-row reparsing), #5 (no NSPopover-for-autocomplete), #6 (no parallel observable state on EditorHost), #7 (no fourth caller to `EditorSurface.applyExternalText`), #9 (no `.onTapGesture` for `List(.sidebar)` rows), #21 (no raw `maugham.*` NotificationCenter post/subscription outside `MaughamEvent`).

## Tests worth knowing

- `MaughamTests/Integration/EditorIntegrationHarnessTests.swift` — 10/10 contract tests for the editor binding shape.
- `MaughamTests/Views/RewindDensityTests.swift` — tick decimation rule.
- `MaughamTests/Integration/RewindEntryPointsTests.swift` — both rewind entry points route through the same notification → modal.
