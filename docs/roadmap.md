# Roadmap

The live roadmap, grouped by writer intent. See [ADR 0002](adr/0002-roadmap-by-writer-intent.md) for the rationale behind the group structure. Phases 1–3 (foundation, novel depth, screenplay) shipped as phase-numbered milestones and are summarized below as historical record.

Within each group, items are listed roughly small-first. Each group's "next up" is the suggested first concrete milestone, but groups are independent — pick what to work on based on day-to-day friction, not a linear order.

Status legend: ✓ shipped, • open, ⤴ superseded.

## Codebase health & hardening

- ✓ **MCP tests that measure the machine — resolved** (2026-08-08) — three tests asserted real subprocess and socket timing against wall-clock deadlines and were reproducibly flaky or hanging under a loaded serial suite (never in isolation); the cross-class parallel test runner (`parallelizable: true`, project.yml) removed the trigger by construction — no process any longer grinds the whole suite through one main actor, which is what the "something holding the main actor" hypothesis turned out to be. Confirmed by two full no-skip parallel gates (green, margins no longer close) plus a five-gate burn-in and two green CI runs on the slower `macos-26` runner. Two of the three tests were additionally hardened (event-driven binds instead of assumed wall-clock timing, a widened exit allowance that costs nothing when green) so a genuinely overloaded machine — e.g. two sessions' gates overlapping — still can't produce a false red. The `test.sh full` skip is retired. Evidence and the disproved Claude-Desktop-holds-the-socket diagnosis: `docs/superpowers/notes/2026-07-29-mcp-clock-dependent-tests.md`'s Resolution section.

- ✓ **Hardening milestone** (branch `feat/hardening-2026-07`, 2026-07-12) — full execution of the 2026-07-11 maintainability review's shortlist: 31 tasks + 1 follow-up across sync/durability correctness (task-op quit-durability, the external-merge door folding un-bursted work + range-safe pure-append undo preservation), palette hardening (rename-revert, coordinated writes, parse/render byte-fidelity — cross-surface via MaughamCore), doc-truth + generation tests (doc claims now assert against source), the enforcement ladder (mint/DeviceSlug/OpKind/InboxConvention/TaskMarkup/DocIdShape promotions), MCP robustness (text byte-budget, cache-routed reads, first-call-after-restart fixed), and release-pipeline pinning. Subagent-driven with two-stage per-task review + a whole-branch review that caught one emergent cross-task bug (close-drain vs compound-undo). CLAUDE.md gained Default-Workflow lines 9/10 (whole-branch review; •→✓ sibling-doc sweep) and tripwire rows 22–24. Findings + status: `docs/superpowers/notes/2026-07-11-maintainability-review.md` §9. Ships as **v0.21.0 + phone-v0.7.0** (paired).
  - • **Follow-up F-A: palette `## Images`-in-body hardening** — a body line spelling a known section heading is claimed by section detection (drops following body text; converges from 2nd render). Promote to a section only when blank-delimited after the first real heading. Own TDD + parity-corpus check.
  - • **Follow-up F-B: external-merge sweep reentrancy** — a prior local delete leaves `_pendingSweep` set; a keystroke racing the pure-append merge's sweep `await` can mutate paragraphs/sequence mid-merge (data-correctness window; undo-safety holds). Reason about the sweep-await reentrancy.

## Phase 1–3 — Shipped foundation, 2026-05-07 to 2026-05-10

These milestones established the editor, the novel + screenplay project shapes, and the file-coordinated DocumentStore. See `docs/superpowers/plans/` for the dated milestone breakdowns and `docs/superpowers/specs/` for per-milestone designs.

- ✓ Phase 1a — Foundation (welcome window, recents, short-story project, basic DocumentStore)
- ✓ Phase 1b — Editor + theming + typography + smart typography + ProseMode
- ✓ Phase 1c — Focus chrome (centered column, typewriter, focus dimming, ⌘\, ⌘⇧F, goals, ⌘S flash)
- ✓ Phase 1d — Three-pane window, Novel binder, inspector, all four project types, per-project typography
- ✓ Phase 1e — DocumentStore with NSFileCoordinator/Presenter, 750ms autosave, conflict resolution, UI state, per-doc cursor restore
- ✓ Phase 2a — Binder polish (drag-reorder, cross-group move, Duplicate, Tidy Filenames)
- ✓ Phase 2b — Surfaces (Research browser with 5 inline renderers, Conflict diff sheet)
- ✓ Phase 2c — Metadata + Metrics (inspector growth: tags / word target / links / [[wiki]] / session tracking / Project Statistics)
- ✓ Phase 2d — Hot-patch: wiki-link rename propagation
- ✓ Phase 3a — Fountain foundation (parser, per-element styling, page count + target)
- ✓ Phase 3b — Editing UX (Tab/Shift+Tab element cycling, element gutter, forced-syntax markers)
- ✓ Phase 3c — Screenplay parity (title page, scene navigator, inline emphasis, ⌘/ syntax help)
- Phase 3d — Multi-file screenplay: attempted and abandoned. See [ADR 0001](adr/0001-multi-file-screenplay-abandoned.md).

---

## Group 1 — Editing flow polish

Daily-writing improvements. Reduces friction in the surface you spend hours in.

**Shipped:**
- ✓ Research polish (2026-05-13) — New Text Note + click-to-edit research docs + rename-renames-on-disk + inline images with ⌘⇧P preview + [Trash & undo](adr/0006-trash-and-undo.md)
- ✓ Cross-document Find/Replace (2026-05-14) — in-doc find re-enabled (⌘F) + cross-document find (⌘⌥F) via grouped results, options (case-sensitive, whole-word), per-row + Replace All, click-to-jump
- ✓ Writing Companion (2026-05-14) — bundled three quality-of-life features around the [right-pane mode-swap pattern](adr/0005-right-pane-mode-swap.md):
  - Research ↔ manuscript linking with click-to-view markdown preview
  - Structure views: Outline (table) + Corkboard (cards) with layout toggle
  - Keyboard cheatsheet tab in ⌘/
- ✓ History Rewind (2026-05-21) — per-doc time-travel modal: scrubber over every op with color-coded legend, Doc/Diff preview, Snapshot or Restore, per-row ↺ button on HistoryPane; SynthesisSource enum refactor + RewindCursor / RewindAction / RewindScope / RewindRestoreResult typed contracts; SweepReason enum→struct carries `cause: SynthesisSource`; multi-window-scoped notification (`object: projectURL`); tag `milestone-history-rewind`; 794 tests passing; carry-forwards: project-scope rewind (multi-doc clock UX), live-update of scrubber during MCP writes, un-archive annotation lifecycle action, scrubber pan/zoom beyond 1k ops, and accuracy on legacy projects whose ops predate the always-capture-sequence-on-burst fix (first-appearance fallback is approximate — can't recover post-burst reorders or deletions).
- ✓ UI Polish (2026-05-22) — right-pane width resilience + status footer + scene-row layout. New shared `AdaptiveFilterRow` control (ViewThatFits-based, with `.layoutPriority(1)` claim so the filter row keeps labels and the trailing button falls to icons first) used by `HistoryPane` and `AnnotationsPane`; `LinkedResearchPane` / `OutlinePane` / `HistoryPane` / `AnnotationsPane` outer VStacks now top-anchor their action bars and expand empty-states to fill below; new `EditorStatusFooter` (session words · ¶pid·element · pages/words progress) mounts via `.safeAreaInset(edge: .bottom)` on `contentColumn` and supersedes the floating `GoalIndicatorView` capsule (hidden in no-chrome `⌘\` and full-screen focus `⌘⇧F`); `SceneNavigatorPane` rows are now single-line with compact trailing `p1 · ½` caption via new `formatPagesCompact`; HistoryPane `Rewind…` button is icon-only at all widths. Tag `milestone-ui-polish`; 809 tests passing (added 7 EditorStatusFooter formatter tests + 8 SceneNavigatorPane compact-format tests; deleted 4 throwaway PreferenceKey-fit tests when AdaptiveFilterRow switched to `ViewThatFits`).
- ✓ UI Polish follow-ups (2026-05-22) — wire ¶pid + element abbreviation to the status footer center column (new `Document.paragraphId(at:)` + new `EditorCoordinator.onElementChanged` callback fed from `lastParsedScript` + `ElementGutterView.abbreviation`); session start exposed via `DocumentStore.currentSessionStart`; orphan `lengthLabel(for:)` deleted from SceneNavigatorPane; `DateFormatter` cached on EditorStatusFooter; tooltips on the right-pane segment picker icons (`DetailPaneToggle.segmentPicker`) — closes the Annotations-vs-History affordance carry-forward from `milestone-editing`. **Regression fix:** restored the lost `recordWordCount` + `recordSessionActivity` call sites in EditorHost's binding setter (silently broken since the 2026-05-19 document-first-class refactor — `projectWordCount` was returning 0 forever, no session ever started, SessionLog stayed empty). `DocumentStore.liveSessionWordsNet` exposes the live session delta so the footer ticks per-keystroke; `goalIndicatorState.wordsToday` sums the persisted log + the live delta. Tag `milestone-ui-polish-followups`; 814 tests passing (added 5 `Document.paragraphId(at:)` tests).
- ✓ Inline emphasis contract (2026-06-03, v0.6.1-adjacent) — `***bold italic***` + nested `*a **b** a*` unified across all four surfaces (Mac prose, Mac screenplay, phone reader, publish) via a shared `InlineEmphasisScanner` + `EmphasisTraits` OptionSet; Mac prose was the outlier (mutually-exclusive Bool). Asterisk-only for now; underscore on Mac prose deferred. Merge `a405b0b`.
- ✓ **CommonMark + Fountain expansion** (2026-07-06, branch `feat/commonmark-fountain-expansion`) — actioned the full compliance audit (`docs/superpowers/notes/2026-07-06-commonmark-fountain-compliance-audit.md`): strikethrough (`~~x~~`, prose) + backslash escapes across every inline parser; paragraph-scoped (not line-scoped) prose emphasis; **publish Fountain now parses through the real `FountainTokenizer`** via `FountainNodeMapper` instead of a duplicated hand-rolled classifier (the audit's highest-severity finding — boneyard/notes/synopses were leaking into compiled PDFs/EPUBs; dual dialogue now publishes as true side-by-side columns as a result); Fountain scene numbers (`#4A#`, faded in the Mac editor, not shown on the phone Read tab, right-aligned in publish), dot-less scene-heading stems (`INT ROOM - DAY`), and the two-space held blank line in dialogue; publish lists + a fence verbatim guard + backslash hard breaks; editor scene-break styling parity (`---`/`***`/`###`) with publish; Help renderer ordered lists + pipe tables (audit A3) and research-preview paragraph grouping (A4). Full IN/OUT decision ledger: `docs/superpowers/specs/2026-07-06-commonmark-fountain-expansion-design.md`; binding record: [ADR 0022](adr/0022-commonmark-fountain-ledger.md). `markdown-syntax.md` / `fountain-syntax.md` rewritten to match shipped behavior.
- ✓ **Approach C — shared block parser** (2026-07-06, branch `feat/shared-block-parser`) — actioned the deferred review: a single `MarkdownBlockParser` (MaughamCore) now backs all five former independent block-splitters (phone `MarkdownBlocks`, `GuideMarkdownView`, `ResearchNotePreviewPane`, `SyntaxHelpSheet`, and publish's `ProjectASTBuilder`), closing the audit's structural root cause behind A3/A4. Phone Read tab, research previews, Guide, and the syntax-help sheet gained lists, fenced-code (monospace), GFM tables, and styled blockquotes — previously editor-only styling. Publish stays byte-stable against pre-cutover output except one pinned, ledger-sanctioned deviation (a leading table/image block immediately followed by prose splits into two paragraph nodes instead of gluing). New permanent regression corpus: `MarkdownBlockParityCorpusTests`, keyed to the audit's section E table. Registry row: `docs/superpowers/notes/cross-surface-contracts.md`.
- ✓ **Persona shell — Plan/Author/Review/Publish** (2026-07-25, branch `feat/persona-shell-2026-07-25`, [ADR 0025](adr/0025-persona-shell.md)) — a persona bar in the window toolbar (⌘1–4) reconfigures all three columns around one stage of the work: `Persona.panes` (right pane) and `Persona.binderSegments(for:)` (left column) are pure registries — the extension point for later right-pane surfaces is a new `DetailSegment` case plus one registry entry, no picker/window change. Optional lenses, not gates: no persona is ever unavailable, and switching never disables the manuscript. Persona lives as `@State` on `ProjectWindow` (independent per window) and persists to the per-project `.maugham/ui-state.json`. Forced the nine right-pane shortcuts off an exhausted `⌘⌥1–8` numeric space onto mnemonic `⌘⌥`-letters (⌘⌥I/R/O/A/H/T/B/P/L), which also fixed a real asymmetry — all nine now dispatch through the View menu, so each reveals a hidden inspector column (previously ⌘⌥4–8 silently no-opped when the column was closed). Two shortcuts moved to make room: Toggle Review Mode `⌘⌥R` → `⌘⌥⇧R`, Toggle Inspector `⌘⌥I` → `⌘⌥0`. [ADR 0005](adr/0005-right-pane-mode-swap.md) (right-pane mode-swap) is amended, not superseded — the pattern survives, now scoped by persona. Spec `docs/superpowers/specs/2026-07-25-mode-based-ux-redesign-design.md` §6.
- ✓ **Planning canvas (M1C)** (2026-07-26 → 2026-07-31; **smoked whole with M1A and passed, 2026-08-01**) — the Plan persona's centre column: a freeform spatial surface for thinking before anything has firmed up. Spec `docs/superpowers/specs/2026-07-25-planning-canvas-design.md` (§7A binding); decisions of record [ADR 0026](adr/0026-planning-canvas-rendering.md); spike `docs/superpowers/notes/2026-07-25-canvas-rendering-spike.md`. Delivered as the slices below — **count the list, not this sentence**; c was split into c1/c2/c3 as it was re-derived against the built code, and c2 took two in-slice corrections after its own smokes rather than becoming new slices. **Merged and pushed, untagged**: M1A's manifest bump forces a paired Mac + phone release, and that release is deferred by choice until the persona-shell work lands (`docs/superpowers/specs/2026-08-01-persona-shell-workflow-design.md`) — shipping the spine into a shell that is reshaped straight afterwards would cost two paired releases where one will do. *Left open at the close, each recorded in the slice that found it:* ⌫ is the only route to deleting a scrap; a VoiceOver user can hear a line but has no route to select one except the mouse — and therefore none to delete it without one; ⌫ does delete a line once it is selected; Claude can read the canvas but cannot see an *owned* picture's pixels (it wants an image response keyed on a canvas node id); a region holding only pictures is still refused as "empty" by a sentence that is false of it; and the project window's three transient banners can overlap, which wants one banner host rather than a fourth overlay.
  - ✓ **1C-a — the surface and scraps** (2026-07-26, branch `feat/planning-canvas-1c-a-2026-07-25`) — a single SwiftUI `Canvas` draws every node under a manual-CTM camera (pan, pinch-zoom 0.1–6×, viewport culling in the draw loop) over a `[[stitchable]]` Metal ground sampled in *content* space and washed 3–5% by the project's own palette swatches; exactly one real `NSTextView` mounts on the focused scrap, off the **same TextKit stack the card is drawn from** (§7A.2's "text jumps on focus" risk retired structurally, pinned by glyph-origin equality *and* a bitmap diff). Double-click to create, type in place, drag with momentum, resize the corner to rewrap. Cards carry a seeded sub-degree angle and the focused one straightens to level over ~120 ms — the editor mounts on the click but only becomes *visible* once the card is level, which is what keeps the swap invisible. Scrap text is plain `canvas.md` at the project root; geometry is derived state in `.maugham/canvas.json` (delete it and the words survive). Canvas-scoped snapshot ⌘Z (sentence-grained inside a scrap, whole-gesture outside one — deliberately *not* op-log-backed, ADR 0026 §5), a hand-owned accessibility tree mirroring the scene graph in rows-then-columns order (§7A.6), and a 2,000-node performance bound. New `BinderSegment.canvas`, Plan-exclusive. Six new tripwires (25–30) and `Maugham/Canvas/AREA.md`. Mac-only by design (§9) — MaughamCore and MaughamPhone untouched.
  - ✓ **1C-b — regions** (2026-07-28, branch `feat/planning-canvas-1c-b-2026-07-27`) — a region is a labelled rectangle drawn by dragging on empty canvas, and **membership is stored, and no *transition* ever derives it from geometry** ([ADR 0026 §8](adr/0026-planning-canvas-rendering.md), tripwire 31): moving a region over a card absorbs it not at all and resizing never ejects one — the bug class Obsidian, Scapple and tldraw (#6017, *despite* explicit storage) each ship a different version of, and every one of them a transition. **Creation is not a transition and does absorb** (Denver's ruling on the smoke, 2026-07-28 — spec §4.2 and ADR 0026 §8 carry dated amendments): a sweep takes in every card whose *centre* it was drawn around, a scrap made inside a region joins it, and a drop targets the same centre. One pure function, `CanvasInteraction.absorbedNodes`, decides — and skips cards that are unmeasured or hidden inside a collapsed region, the two cases where "the writer swept around it" is untrue. One home plus many appearances (references, never copies); a resident that ends up outside its region gets a tether rather than a silent ejection. Regions move with their residents under one ⌘Z, collapse to a counted label bar (hidden in the *scene*, so drawing, culling, hit testing and the accessibility tree agree by construction), and bind to a piece by id — §4.4's bridge, produced here (the reference rail that draws it is M2's; 1A gave the projection a reader instead, `list_canvas`'s `piece_references`). **Deleting arrived with them** (the gap 1C-a left with no caller at all): ⌫ deletes the selected scrap or region through the real responder chain, a region's cards survive it, and a ⌫ inside an open gesture is refused rather than absorbed — the lossy variant put a card and its words on disk with nothing on the undo stack. Scene state moved onto an `@Observable` `CanvasModel` owned by `ProjectWindow`, so the **region inspector** (Inspector mode, ⌘⌥I, on the Canvas segment) reads and mutates the same scene the canvas draws — through `mutateFromInspector`, never `mutate` (tripwire 32: the inside verb *refuses* mid-gesture, the outside verb closes-runs-reopens; missed three times in one slice, and it fails silently). Sidecar at schema 2, non-destructive both ways — an older build reading it loses the arrangement and never the words. **Citing arrived with them too**, and by the same route: the docs sweep found `CanvasMembership.addAppearance` persisted, drawn, listed and removable with no production caller — the delete gap's own shape recurring inside the slice that closed it, found by grepping for production callers of what the slice had built rather than by any test. The "Appears here" section now carries a **Cite a Card** menu offering every card not already in the region, gated on the structural counter so a scene-wide walk stays off the drag loop, and a caller census asserts the list by name. *Left open, on the record:* ⌫ is the only route to deleting a scrap.
  - ✓ **1C-c1 — lines** (2026-07-28, merged `73f0588`, **smoked and passed the same day**) — an **untyped** edge between two cards with an optional free-text name, and it must not gain a `kind`: Kinopio shipped author-typed connections for years and removed them in April 2026 because they confused every first-time user observed, so an untyped edge with an optional label is the empirically supported floor (spec §5). Two routes draw one and they resolve to a single `Bool` before the gesture layer sees anything — ⇧-drag from any card is the fast route, a drag out of the connect mark on the *selected* card is the findable one — so the discoverable gesture cannot drift from the shortcut. **One precedence rule, in drawing and in hit testing alike: cards over lines over regions**, the click order being the draw order read backwards, asserted against each other rather than each against a literal. That reversal owed the DRAG too, and Task 5's review is what found it: a press on a line crossing a region's chrome bar selected the line and *dragged the region*, nudging every resident under a "Move Region" step the writer's next ⌘Z would take. A press resolving to a line is now idle, which is the honest answer for a thing that cannot be dragged. Endpoints are node centres; a line to an unmeasured card or to a resident of a collapsed region is neither drawn nor clickable, because both read one projection (`CanvasScene.drawnLines`) rather than two copies of the rule. Named in the right-hand column beside the region, through `mutateFromInspector` (tripwire 32), and **clearing a name is its own undo step, "Clear Line Label"** — `label` is optional, so clearing reaches a genuinely different state. ⌫ takes a line and leaves both cards; deleting a card takes its lines with it, and one ⌘Z brings back the card, its words and its lines together. **Reached by VoiceOver at both its ends** rather than as an element of its own — a line's frame is a bounding box with nothing in it — so each connected card names its connections and the canvas summary counts them (§7A.6). Sidecar at schema 3, additive-optional. Two projections moved onto `CanvasScene` in the sweep (`drawnLines`, `selectionTarget`), each having cost a layering inversion the moment a second caller needed it; `CanvasScene.lines(touching:)` was censused at zero callers a third time and **deleted** — the fourth built-and-unreachable half in this area, and the fourth found by a caller count rather than by a test. No new tripwire: the new `CanvasSelection` case is compiler-enumerated and the undo verb is an existing census. Tripwire 15's grep was widened from `Maugham/Views` to `Maugham/` in the same sweep, which is where both canvas inspector panes had been sitting unenforced. *Left open, on the record:* a VoiceOver user can now **hear** a line — each connected card names its connections — but has no route to **select** one, so selecting a line is mouse-only — naming needs a selection, and ⌫ deletes it once selected. A line is deliberately not an element of its own (its frame is a bounding box with nothing in it), and the two candidate fixes are an element at the label pill, which is small, aimable and absent on an unlabelled line, or a custom action on each connected card. Which one is a question for a session with a real screen reader, not for a unit test.
  - ✓ **1C-c2 — promotion** (2026-07-28, branch `feat/planning-canvas-1c-c2-2026-07-28`; **corrected after its smokes by 1C-c2a and 1C-c2b, 2026-07-29, on the same branch** — see the two amendments at the end of this entry) — **the step that makes the canvas pay off**: `Promote…` on the current selection turns a scrap into a research note, a palette card or a craft intent, a region into a research note or a palette card, and a line into a `[[wiki-link]]` (§6's table, made executable in one pure function that never mutates the scene — which is what makes the preview honest). **What it produces is a SNAPSHOT and the mark left behind is provenance, not a link** ([ADR 0026 §9](adr/0026-planning-canvas-rendering.md)): the region row forces it in one move — promoting a region joins six cards' text while all six stay on the canvas — so a scrap that moved its words *out* would give one verb two rules and would pull 1C-d's item rendering forward. Re-promoting names the artifact and offers **Rewrite** or **Make a new one** with neither defaulted, because "always update" eats edits made in `research/` and "always new" leaves `The falls at night 2`, `… 3` and two orphans. One gesture, four doors: a File-menu item at **⌘⇧↩** gated on a published focused value (so a command that could do nothing is greyed out rather than silently no-op), plus a `Promote…` button in each of the inspector's three arms — a card gained an arm of its own here, which is where it says what it became, including when the note has been deleted out from under it. All four post the *same* command, so the button and the keystroke cannot drift; and a `.keyWindow` post from inside a **sheet** is dropped, which is why they live in the project window and why moving one into a modal would break it silently (the v0.24.0 shape). Writes go through one `@MainActor async throws` performer with no `inout` on the path (an `inout CanvasScene` cannot cross an `await`), validate-first, flushing the 750 ms autosave before every body write and **refusing an unreadable destination** rather than appending to an empty string and writing the result back — must #1 on an append path. The mark reaches tripwire 32's census as its third entry, because `PromotionPerformer` writes it from outside the canvas and can run while a focused scrap holds "Edit Scrap" open (1C-c2a added a fourth, `ScrapInspector`, through the identical repro). A promoted card wears a **stripe down its left edge**, a promoted region wears one on its chrome bar (permanent chrome, like the resize triangle and unlike the connect dot), and VoiceOver hears "promoted" with the kind still first. **The line row shipped with its reader**: `list_all_links` and `find_references` now scan research-note bodies, because promotion never produces a manuscript document and every link it writes lands in a research note — without that the row would have been this area's fifth built-and-unreachable half. No new MCP tool (still 52) and no new tripwire. Sidecar at schema 4, additive-optional, with the mark on the node and the region rather than as a collection of its own (5 and 6 in the amendments below). *Left open, on the record:* the promoted `[[…]]` is **not rename-propagated** (`propagateWikiLinkRename` walks manuscript documents only, so renaming the note leaves the link on the old title) and is **not rendered as a link** in the research note editor or its preview pane — it is read by the link layer and nowhere else. **(Closed 2026-08-09, issue #24: `propagateWikiLinkRename` now rewrites `[[…]]` in research-note bodies and statements, and composed statement titles, on rename. The render-as-link half is unchanged.)** And **a promoted card and its note can drift apart, which is the design and not a defect**: there is no reconciler, no freshness stamp and no "out of date" chrome, because the mark is provenance and nothing was promised.

    **Amendment — 1C-c2a, the piece association** (2026-07-29, same branch, spec §6's 2026-07-29 amendment + the new §6.2, [ADR 0026 §9](adr/0026-planning-canvas-rendering.md)). The smoke found the region row wrong on both halves, and this is the same slice corrected rather than a new one. **A piece binding is not a promotion**: committing one produced no artifact — it set the field `RegionInspector`'s own **Piece** picker already sets — while wearing the words "Produce" and "Goes to", and the field's only intended reader, 1A's reference rail, is unbuilt (measured: `RegionBinding.references(forPiece:)`, zero production callers). The writer's report was "I don't see it doing anything". The target is gone, the picker and `RegionBinding` are untouched, and **a region promotes to a research note** in its place — the palette card stays, and its case gets stronger in 1C-d when a region can hold an image. **The same association is now on a card as well** (`CanvasNode.boundPieceID`, sidecar 4 → 5, additive-optional both ways), set in a new **Piece** picker in the scrap arm, and **a promotion's destination resolves by precedence: the card's own association, else its *home* region's, else the project's research — overwriting nothing.** A region-level cascade was considered and rejected: it destroys a deliberate per-card choice invisibly and revives §4.2's bug class in a new place, since a card *cited* in two regions bound to different pieces would follow whichever was touched last. Home only, because a citation is not luggage (§4.3's rule for dragging, applied to destination). **The containment-vs-link routing is adopted, not invented**: `ResearchScope` has decided it since the 2026-07-07 scoped-research milestone — containment for a Collection loose piece, shared plus an automatic link for a novel chapter, shared alone for a short story or screenplay — and one resolver (`PromotionPiece.resolve`) is the only place the pure half meets it, called by the sheet when it opens and by the performer at Commit, so the destination the writer reads and the one they get cannot disagree. The sheet names the piece **and the route** rather than just the folder, because the two shared-research rows are otherwise indistinguishable. Both pickers now offer `researchScopeTargets()` rather than every document, so a writer can no longer choose a reference piece the router throws on — and that function was **O(documents²)** (a whole-structure re-walk per id to find the item it had just been handed), unnoticed while its only caller was a picker opened by hand and worth fixing the moment 1C-c2a put it on the window's body path. **A stale association refuses before Commit rather than redirecting silently**, and the refusal distinguishes an *inherited* association from the card's own, because a card cannot clear its region's piece and telling it to would name a control that cannot fix it. The **craft intent takes the piece only for a Collection loose piece**, and the reason is a lookup rather than a scope preference: `craftIntentItem(forPieceId:)` finds an existing intent doc by the piece's research *prefix*, nil for anything else, so an intent filed under a novel chapter's routing would never be found again and the next promotion would mint a second — the writer's intent statement silently in two files. `ScrapInspector` is tripwire 32's fourth census entry. *Left open, on the record:* `Promotion.piece(for:in:)` runs **twice** per evaluation of `ScrapInspector.association` — once directly, once inside `pieceIsInherited` — on a body that is on screen while a card is dragged; it is bounded by the region count, is not the leading term in its own body, and **nothing has measured it**. The gate exists one file over and its key would be correct.

    **Amendment — 1C-c2b, what a promotion records on the cards it consumed** (2026-07-29, same branch, spec §6.3, [ADR 0026 §9](adr/0026-planning-canvas-rendering.md)). The second smoke correction on this slice, and the same slice again rather than a new one. The writer promoted a region of several cards to a research note; it worked, every card's text was in the note — and in the inspector only the cards they had promoted *individually* said anything, while the rest reported **"Not promoted yet."** Their report: *"not all the scraps know they were promoted, some think they weren't — all did turn up in the research note though."* Two truths on one screen. **So a contributing card now carries a record that its words are IN that artifact — `CanvasNode.contributedToItemID`, sidecar 5 → 6, additive-optional — and that record is deliberately NOT the promotion mark.** `promotedItemID` means *"I am this artifact"* and is what `Promotion.existingArtifact` reads to offer **Rewrite**; stamping a contributor with it would let promoting one member afterwards rewrite the whole six-card note with that one card's text — the 1C-c2 Critical (a mark that did not record its artifact's *kind*) returning as a mark that does not record its *cardinality*. The guard was falsified by adding exactly that fallback and watching the no-Update test report an update. **Recorded at promotion time from the members whose text actually went in** (`Promotion.regionBodies`, already shared by the preview, the refusal and the body), never derived from live membership: a card added to the region afterwards has no words in that note, and an empty scrap contributes nothing. **One gesture, one undo step** — the region's mark and every record are written in a single `mutateFromInspector` bracket, proven by opening a second one and watching the one-⌘Z test go red. **An update rebuilds the set**, clearing every node naming the artifact before stamping the current contributors, so a card that has left the region stops claiming the note and one that joined starts — and the clear is scoped to the artifact, so a promotion cannot wipe a record naming somebody else's note. **The record is single-valued and the most recent contribution wins** *(as shipped; superseded 2026-08-09 by RULING-51 — the record is now multi-valued (`contributedToItemIDs`, sidecar schema 9), the stamp appends, a rewrite removes only its own artifact's fact, and §6.3's rewrite-guard protects every artifact the card fed)*. **A card may carry both and the inspector shows both**, in visibly different sentences (*Became “…”* against *Its words are in “…”*), neither naming a count, since a region's contributors are sometimes one card; the decision lives in `PromotedArtifactSection.Provenance` because which SwiftUI arm renders cannot be asserted, and the caption under the contribution line is where the writer learns that Promote… from there makes something **new**. A deleted note is resolved through the index the pane already had and says so rather than showing a raw id — records persist through the codec and are never rebuilt on load, so that state is genuinely reachable. **A contribution gets no stripe and no VoiceOver term**: the drawn mark means *this produced that*, and a second identical stripe would say on the canvas the very thing §6.3 exists to separate. No new tripwire and no movement in tripwire 32's census.
  - ✓ **1C-c3 — the MCP canvas surface** (2026-07-30, branch `feat/planning-canvas-1c-c3-2026-07-30`; spec §8A.2's 2026-07-30 amendment, [ADR 0026 §10](adr/0026-planning-canvas-rendering.md)) — **Claude can read the canvas and add what it read off a photographed page to it.** `list_canvas` returns the whole canvas — every card, every region with both member sets, every line, every mark — and reads the **open** canvas when the Plan persona has one and the sidecar otherwise, which is tripwire 20 on this surface; `add_canvas_scraps` adds cards. **The reproduction corollary is made structural rather than promised: the write tool's signature can express no position, no node id and no region id**, so where Claude's scraps land is the canvas's decision and not Claude's — every call mints one *labelled* region (nothing Claude adds is ever loose), the page goes at the top of the column and what was read off it below, so "what was read off this page" is answerable by looking, and **lines are drawn by `connect`, which indexes the call's own `scraps` array** rather than naming ids, which is how Claude draws the arrows it read off a page while touching nothing the writer made. A page already on the canvas is **cited** where it lives and never moved, because moving it would be the geometry→membership transition tripwire 31 exists to forbid. **Whose hand made a thing is said three ways, and the primary one is the TILT** (Denver's ruling, 2026-07-30, prompted by a measured ceiling): the canvas already said "a hand placed this" by leaning, so **Claude's cards and Claude's regions are drawn at exactly 0°** and everything the writer made leans by at least `minimumTiltDegrees` — true zero is *reserved*, because a usually-right provenance signal invites the writer to trust that a card is theirs when it is not. Regions had no irregularity before and gained it (drawing rotated, grab left square, so the `r·θ` band does not grow). Colour ships as a second signal, **corrected**: the light-mode tint is bounded in red and only in red, so holding blue at 1.000 and bringing red and green down is a *tint* rather than a *dim* and doubles the perceptual difference (ΔE2000 1.36 → 2.79) with red's headroom byte-identical — the first analysis read red's ceiling as every channel's and escalated for a ground recalibration, which was wrong. And **the term is spoken on all three primitives** — scrap, region and item node — because a lean is inaudible and the accessibility tree is flat, so a user tabbing card to card can reach a Claude card without passing its region. The two signals answer different questions and **diverge on exactly one node**: the photograph is the writer's and Claude placed it, so the source page carries `author = .claude`, is drawn straight, and is not tinted. A batch is **one arrival, one region, one undo step and one wording** whether or not the Plan persona is open (`CanvasClaudeWrite`, tripwire 32's **fifth** census entry, whose repro needs no gesture at all — an MCP call arriving while the writer is inside a scrap), with the scrap text travelling *inside* the undo bracket because both other orderings are measured failures. The writer is told by an arrival banner whose **Show** switches persona, opens the inspector, selects the region **and moves the camera** — not polish: Claude's region is placed beyond the union of everything already on the canvas, so it is off-screen by construction. Two tools (52 → 54), sidecar at schema 7 (`author` on nodes, lines and regions in one additive-optional bump), no new tripwire — 32's census grew instead. *Left open at the time, and **1C-d paid it**:* the corollary was structural here and became visible at 1C-d — the page drew as a dashed placeholder carrying its reference id until item nodes got a thumbnail, so source and reproduction were tied in the model and in the accessibility tree while what the writer *saw* of the source was a code. Neither tool can claim the write reached **disk** (`CanvasStore.writeNow` swallows I/O errors area-wide), so both the response and the banner say *added*. And the three transient banners in the project window can overlap, which needs a promotion within 8 s of a batch landing and wants one banner host rather than a fourth overlay.
  - ✓ **1C-d — getting things onto the canvas** (2026-07-31, branch `feat/canvas-1c-d`; spec §8A.1/§8A.3/§8A.4 + §3.1's and §6's 2026-07-30 amendments and §6.3's 2026-07-31 one, [ADR 0026 §§7, 9, 10](adr/0026-planning-canvas-rendering.md)) — **an item node stops being a placeholder, and the canvas gets four ways in.** §3.1's amendment is the shape of it: an item node now has **two provenances**, *referenced* (a research item or palette card the project already holds, whose file the canvas never touches) and *owned* (a photograph ingested into `canvas_assets/` at the project root, because it exists nowhere else) — **the distinction is storage, not drawing**, and `referenceId`/`ownedPath` are its two disjoint sidecar fields. A card shows the item's **real title, kind glyph and photograph** through a `CGImageSource` downsampling path and a path-keyed bounded cache (tripwire 22), is **measured from its picture's own aspect ratio** so §7A.3's width-authoritative rule reaches a second content type, **resizes like any other card**, and has **its own inspector arm**. Two things fell out of the cache being bounded and are worth carrying: **a card's SHAPE outlives its pixels** — a permanent path→aspect memo, never evicted, so an eviction cannot move the writer's layout — and the servicing schedule **must not feed itself**, because resolve → miss → ticket → service → evict → resolve is a loop with no exit above ~85 pictured cards, and a pending-set signature does not close it (the evicted tail rotates, so every pending set genuinely differs). **Four routes put something on the canvas and every one is a *caller* of one ingestion pair**: a research row dragged out of the binder (a *referenced* node — and a second drop of the same item REVEALS rather than re-creating, because the id is derived and a re-insert would silently discard that node's membership, mark, width and z), a Finder file or a **browser bitmap** through `DropClassification` — never `.dropDestination(for: URL.self)`, which rejects a rendered bitmap with CoreTransferable error 0, nothing logged, nothing on screen — an **Inbox capture** by drag or by command for all three kinds (§8A.4; the drag payload is deliberately *prefixed*, because the canvas is the first drop target reading two id spaces and an inbox ULID is not tellable apart from a research id), and Claude's `add_canvas_scraps`, which predates them. A drop is a legitimate geometric reading and the only kind tripwire 31 licenses — it joins by the node's **centre**, at creation — and the card is **born measured**, so `joinTarget` has a centre to read at all. **An owned picture promotes**, to a research asset or as an image appended to an existing palette card, while a referenced one still does not; the palette row writes a **contribution** and never the mark, which is the structural guard against being offered *Rewrite* over a card holding swatches, sensory notes and other images. **A region promotes with the pictures in it** (§6's 2026-07-29 amendment; the palette-card row only, and a `.new` promotion only — a rewrite is about the prose), and §6.3's record now covers **any home member whose content went in, words or picture** — Denver's ruling, 2026-07-31, after a photograph went into a card while its own card still said *"Not promoted yet"*. **`⌘\` gives the canvas the whole window** (§8A.3): the canvas is the CONTENT column, so `.detailOnly` — the value that reads as obviously right — hides the canvas itself, and what works is `.doubleColumn` together with the inspector already closed. Never automatic on entering Plan, because you need the binder open to drag things in. The wall and the canvas share one update pass, and the collapse **takes over** the palette's stash so that both orders converge — convergence, rather than a chosen winner, being the only property that can be true independently of SwiftUI's delivery order. `list_canvas` gains one optional `provenance` field and **the path is deliberately nowhere on the wire** (nothing in the catalogue reads a file by project-relative path, so it would dangle in exactly the field a reader would feed to another tool) — a widening of an existing read, so **tools stay at 54**; sidecar at schema 8 (`ownedPath` on `NodeDTO`, additive-optional like every bump since 4). *Left open, on the record, and both decisions rather than gaps:* **Claude cannot see an owned picture's pixels** — the missing piece is an image response keyed on a canvas node id, a new read tool and a new response shape — and **a region holding only pictures is still refused as "empty"**, with a sentence that is false of it; closing that is three per-target changes (`blockedReason` taking the selected target, `plan`'s region-arm guard becoming per-target, `validate`'s `emptyBody` guard admitting a picture-only card) and is a decision about what "empty" means per row, so it is asserted rather than argued. The overlapping-banner problem is unchanged and still owed: **two tasks reached for a fourth transient overlay and both declined** — Task 11 took an alert, Task 12 put its sentence in the pane the writer was already looking at — so one banner host for the window remains its own slice.
  - ✓ **Deleting a scrap — settled, and 1C-b took it.** The gap was real: 1C-a built `CanvasScene.remove`, its inverse and the `"Delete Scrap"` undo step, exercised all three, and shipped no production caller — so a stray double-click left an empty card that ⌘Z reclaimed only until the writer clicked away. ⌫ is now the caller. **Left open at M1C's close:** ⌫ is the *only* route to deleting a scrap. That was recorded as a consequence of a scrap having no inspector to put a Delete button in; **1C-c2 gave it one and deliberately left the button out**, so the reason has changed and the gap has not — adding it for symmetry with the region and line arms would be a design change wearing a tidy-up's clothes. An Edit-menu Delete item reaches outside `Maugham/Canvas/` and wants deciding whole.
- ✓ **1A — the spine** (2026-07-31, branch `feat/m1a-spine`; spec `docs/superpowers/specs/2026-07-31-m1a-spine-design.md`) — **the milestone that completes M1**, and it is a migration as much as a feature: the writer's craft intent stops being a research note and becomes a `Statement`. A statement is a manifest registry entry (`id`, `kind`, `scope`, `path`) whose CONTENT is an ordinary `Document` living in the open at the project root — `intent.md`, `intent/<slug>.md`, `visual-language.md` — so history, undo, rewind and cross-device merge arrive built rather than invented, and §2.4's falsifiable claim held: an intent edit is an ordinary `typingBurst` and there is no new `OpKind`. **Scope is found in the manifest, never by path prefix**, which is what kills the shipped defect rather than patching it: `craftIntentItem(forPieceId:)` located intent by a Collection loose piece's research prefix, nil for a novel chapter — so a chapter's intent was written into shared `research/` where the lookup never looked and the next promotion minted a second copy of the writer's prose. There is no prefix here, so there is nothing to be nil. **Two right-pane surfaces** — Intent (`⌘⌥N`), whose scope follows the binder selection with the project's one click away, and Visual Language (`⌘⌥V`), project-scope and able to take pictures by drop or `⌘V`. **Absence is valid and mints nothing**: an undeclared scope shows an empty editor that creates the file on the first keystroke, not a Create button and not a nag — the UI agreeing with what `read_craft_intent` has always told Claude. **Adoption runs once, on the first open by a build that has statements** (spec §5): every legacy craft-intent note is found role-first with the filename as fallback, scoped by containment then by a `linkedResearchIds` record naming exactly one document then by the project, read BEFORE anything is created, concatenated oldest-first rather than chosen between, and TRASHED rather than deleted — and it re-points any canvas mark that named the note it moved. **The manifest is at schema 4 and an older build REFUSES to open a migrated project rather than degrading it** (`decodeGuardingSchema` on every real read path, including the phone's), so **the release is paired Mac + phone and enforced rather than merely documented**; an un-migrated project still reads exactly as it shipped, statement-first with the legacy note as fallback, through the shared MaughamCore lookups. The phone READS a statement and does not edit one. Claude's side: `read_craft_intent` (widened to any manuscript document) and the new `read_visual_language` (tools 54 → 55) answer through one `statementText(of:)` — the single spelling of ADR 0018's two branches, so two readers cannot disagree about which text is real — and `list_all_links` / `find_references` scan statements, because adoption carries a legacy note's `[[…]]` across verbatim and a migration may not silently cost a shipped capability. Canvas promotion retargets onto statements and the old craft-intent seam is deleted. *Left open, on the record:* **⌘⌥F does not reach a statement** — `ProjectSearchEngine` walks `manifest.structure` and `manifest.research`, and widening it means a `SearchDocumentSource` case, a replace arm that must route through the op log under `lockStatementOpen`, and a navigation arm that must set the right pane's scope, which is exactly the `Open`-sets-scope machinery this branch built over three fix rounds and then **reverted by ruling** (`search_text` is unaffected: it filters to `.manuscript`, so it never returned a craft-intent research note either). That revert is the second open edge — `Open` on a promoted card routes to the Intent pane but does not set its scope, so Open on a chapter's intent can show the project's; what the three rounds established is recorded in `ProjectWindow.openPromotedArtifact` and `Maugham/Canvas/AREA.md` for whoever attempts it again. And **a statement outlives the document it is about**: nothing prunes one when its chapter is deleted or trashed, so the file, its manifest entry and its op log stay behind, unreachable by any scope — not loss (the prose stays in the open and trash-undo restores a working pairing) but a growing leak with an `ArtifactIndex` entry titled bare "Craft Intent".
- ✓ **Research restructuring — scope moves, collection-pane parity, multiselect** (2026-07-16, branch `feat/research-restructuring-2026-07-16`) — reorganizing research was blocked three ways: no way to move an item between shared research and a collection piece's research folder (scope was purely path-derived, `CollectionResearchPane`'s cross-section drops were silently ignored); the collection pane rendered research flat (groups undecorated, no `DisclosureGroup`, drop-into-group unhandled) where the plain `ResearchView` had full tree rendering; and both surfaces were single-selection only. Fixed with one batch, scope-aware store API (`ProjectStore.moveResearchItems`, `ResearchMoveTarget` — shared / group / piece, ADR-0010 typed seam, one `RenamePlan` through the existing typed mover) plus a batch `deleteResearchItems`; `ResearchView`'s tree rendering extracted into a shared `ResearchTree.swift` (`ResearchTreeNode` + `ResearchSelectionSync`) and adopted by `CollectionResearchPane`, closing the parity gap — nested groups, drop-into-group, and cross-section scope-move drags now work in collections too; `Set<String>` multiselect on both surfaces with whole-selection drag and a "Move to ▸" / batch-Delete context menu. New MCP `move_research_item` (47 → 48). Rode along: a latent bug in the pre-existing single-item cross-group move that silently orphaned a note's sibling `<slug>_assets/` folder (and its image refs) on move, fixed at the shared plan-builder level. Spec `docs/superpowers/specs/2026-07-16-research-restructuring-design.md`. *Out of scope, by design:* moves stay non-undoable (research structure ops aren't op-log backed, consistent with existing research mutations); no phone surface (no research UI there); batch moves are not FS-atomic (validated up front, same exposure as today's single moves).

**Phase 4a — Screenplay Intelligence (IDE-like editing):**
- • Inline character autocomplete — NSTextView-driven inline ghost-text with Tab-to-accept (carry-forward from 3b where NSPopover proved too brittle)
- • Slugline reuse — suggest previously-used `INT. KITCHEN — DAY` when typing a new heading; avoids drift across drafts
- • Fountain prefix completion — `I` at line start offers `INT.` / `I/E.` / `INT/EXT.`; `E` offers `EXT.` / `EST.`; transitions get `FADE OUT:` / `CUT TO:` etc.
- • Outline minimap (optional) — slim secondary sidebar with section/scene structure

**Prose-mode parallel:**
- • Inline character/place autocomplete in prose mode — uses parsed `[[Wiki Links]]` from 2c plus proper-noun frequency

**Screenplay editing depth:**
- ✓ Dual dialogue (2026-05-26, shipped in v0.4.0) — `^` for side-by-side speakers; trailing-`^` cue fades, dual-second blocks render with deeper indents, page-count treats the pair as max-of-pair. Merge `c0676cf`.

**Visual reference:**
- ✓ **Craft intent + sensory palette** (2026-07-09, branch `feat/craft-intent-sensory-palette`) — reframed the mood-board sketch as an intent-first loop rather than a detector: an optional freeform craft-intent doc (project-scope, or per-piece in a collection) states what a story needs sensorially, a sensory-palette wall (`BinderSegment.palette`, adaptive card grid, ⌘⌥7 in the right pane) gathers subject-keyed cards (kind/swatches/senses/images) as research assets under `research/palette/`, and a Claude "sense pass" (new `sense-pass` guide topic) audits intent + palette + manuscript over 3 new read-only MCP tools (`read_craft_intent`/`list_palette_cards`/`read_palette_card`, 44 → 47) through the existing annotation membrane. **Card-editor revision (2026-07-10):** cards are edited visually, not as raw markdown — drag/paste images into an image well, pick swatches with the colour picker or sample them off a reference image with the eyedropper, tag sensory notes with sense chips, and a freeform body field for anything else; the model regenerates the on-disk markdown from the card on every edit (the file stays human-readable but is derived, like the manifest). Spec `docs/superpowers/specs/2026-07-09-craft-intent-sensory-palette-design.md`. *Deferred follow-ons:* a mechanical sensory-lint layer (would false-positive against deliberately spare stories — intent-relative measurement stays the open problem); a freeform canvas presentation (today's wall is a card grid, not a spatial board); a phone surface (cards are Mac-only for now, **addressed below**); palette-**group** rename identity (renaming the `research/palette` group itself, distinct from the already-covered card rename, **addressed below**).
- ✓ **Palette everywhere — role identity + phone capture/read** (2026-07-11, branch `feat/palette-phone-role-identity`) — closed both deferred follow-ons from the milestone above in one pass, bundled because they share MaughamCore surface. **Role identity:** new `ResearchItem.role: ResearchRole?` (additive-optional, ADR-0015 `.unknown`-sentinel pattern) gives the palette group and craft-intent doc a durable identity that survives rename — role-first lookups (`PaletteLookup` in MaughamCore) fall back to the legacy path/filename match and lazily self-heal (stamp + save) on a fallback hit, no migration; the Mac wall/sidebar now show the group's live (possibly renamed) title. `PaletteCard`/`PaletteCardParser`/`PaletteCardRenderer` moved from Mac-only `Maugham/Models/` into `Packages/MaughamCore/` (tripwire 19) alongside the new `PaletteConvention`/`PaletteLookup` shared constants/lookups. **Phone capture:** an optional aim row on the Capture tab (`PaletteAimPicker`) points a photo/voice/text capture at a palette subject plus an optional sense tag — never required, threaded through two new additive `InboxEntry` fields (`palette_subject`/`sense`). **Phone read:** the Read tab's project drill gains a read-only Palette section (card list → card detail with swatches, sense-grouped notes, body, and — via the new eviction-safe `PhoneImageLoader` seam — images) plus a project-scope Craft Intent row; palette cards no longer leak into the phone's ordinary Research section. **Mac promote:** the Inbox pane (⌘⌥6) gains "Promote to Palette: card title" (direct, when the capture's aimed subject matches one card) and a `PalettePickerSheet` (search existing cards, or mint a new one from the subject) — text/audio become sense-tagged sensory notes, images land in the card's image well. MCP `promote_inbox_entry` gains `palette_card_id`/`palette_subject` as two more mutually-exclusive destination params (tool count unchanged, 47 — params only). Spec `docs/superpowers/specs/2026-07-10-palette-phone-and-role-identity-design.md`. Designed as a paired Mac+phone release (cross-surface schema). *Still out of scope:* phone card editing (needs op-logging cards, its own milestone); a fifth phone tab; freeform canvas; extract-palette-from-image; Claude proposing palette additions via annotations.

**Project Statistics polish for screenplays (parked 2026-07-02, smoke finding adjacent):**
- • Screenplay Project Statistics show novel-shaped content — the "Words by chapter" section renders a single row (the one `.fountain` doc), which reads as "one chapter" and invites a click that navigates somewhere unhelpful (the click itself is fixed — it now lands on the Scenes navigator, `BinderSegment.documentHome(for:)` — but the stats display is still wrong-shaped). Polish: for screenplay projects, replace words-by-chapter with scene-shaped stats — scenes with per-scene page lengths (the `sceneSummaries()` / `formatPagesCompact` machinery from the Scene Navigator already computes this), total page count vs page target, and click-a-scene-row → navigate-to-scene. Small, self-contained; pick up with any stats/UI-polish batch.

**Right-pane onboarding polish (carry-forward from the editing milestone):**
- ✓ Segment-picker tooltips (2026-05-22, `DetailPaneToggle.segmentPicker` `.help()`) — closes the smallest-fix half of the original carry-forward. *Still open:* empty-state hints that point across the sibling segments ("looking for action buttons? Press ⌘⌥A") — Annotations (action surface with Accept/Reject/Archive) and History (read-only forensic log) still read as a generic "right pane mode" picker beyond the tooltip.

**Comprehensive ⌘Z undo (op-log-backed):**
- ✓ **Unified ⌘Z undo across every op kind** (shipped **v0.18.0** + phone-v0.5.0, 2026-07-10, branch `feat/unified-undo`; [ADR 0023](adr/0023-unified-oplog-backed-undo.md)) — ⌘Z now spans every writer-initiated mutation by appending compensating ops (append-only invariant held; undos appear in the History timeline): annotation reject/archive/edit/withdraw (new `annotationReopen` op kind — also closes the History-Rewind "un-archive" carry-forward), all six task ops incl. Archive-All-Done as one grouped undo, inline checkbox flips, and History Rewind restores (grouped compensating restore + lifecycle re-accepts + task-window closer; redo re-runs the restore). One stack per window: op-undo actions interleave with native typing undo on the window's `NSUndoManager` via `OpUndoRegistrar`; inverse decisions live in compiler-exhaustive factories (`AnnotationInverse` in MaughamCore — shared with the phone's new Reopen — and `TaskInverse` Mac-side). Deliberate boundary: text-touching actions start a fresh undo baseline (the unconditional D1 clear rule — the v0.16.0 ⌘Z-crash class); non-text actions stack deep. Schema v3 (paired Mac+phone release). Spec `docs/superpowers/specs/2026-07-09-unified-undo-design.md`.

**Author's IDE — analytical layers (tracking, lookup, lint):**

A new arc of "code-tool-style" author surfaces. Each layer is structured author-owned side-data anchored to paragraph IDs, surfaced as a right-pane mode, op-log derived, and MCP-readable. Tasks shipped first as the pathfinder (v0.3.0); symbol DB / lint / writing analytics reuse the same architectural shape.

- ✓ **Tasks — the issue-tracker layer** (shipped v0.3.0, 2026-05-25; MCP `list_tasks`/`get_task` included) — unified task surface combining three capture paths: inline `- [ ]` markdown checkboxes in any `.md`, Fountain `[[todo: …]]` / `[[done: …]]` boneyards in `.fountain`, and pane-created tasks (no text representation, ops only). One right-pane segment (⌘⌥5) with **Document / Project** scope filter, status filter (Open / Done / Archived / All), drag-and-drop **priority** reorder (rewindable, ops), single-level parent/child nesting (drag-to-nest), in-pane checkbox toggle that **rewrites the underlying text** for inline tasks, click-row-to-navigate, and `+ New task` for pane-created entries. MCP read-only (`list_tasks` / `get_task`). Pathfinder establishes the typed cross-area seam (`TaskKind` / `TaskStatus` / `TaskAnchor`), the op-log derivation pattern (mirrors `AnnotationDeriver`), the synthetic `__project__` doc-id approach for project-scope ops, and the editor-side clickable-checkbox interaction that future analytical layers can reuse.

---

## Group 2 — Claude integration

AI assist for drafting, transcription, and project understanding.

**Shipped:**
- ✓ MCP Foundation (2026-05-16) — live-only Unix-socket bridge to Claude Desktop with 14 tools (8 read + list_research + list_documents_by_tag + list_all_links + add_note + link_research + unlink_research), the one-click Set up Claude Desktop sheet, and the Settings toggle. See [ADR 0003](adr/0003-mcp-live-only-unix-socket.md) (transport) and [ADR 0004](adr/0004-mcp-foundation-scope.md) (scope).
- ✓ Editing — annotations + history viewer (2026-05-19) — Claude-as-collaborative-editor end-to-end. 4 new OpKind cases (claudeComment / claudeQuery / claudeCraftNote / claudeArchive) + 6 new MCP tools (add_comment / add_suggested_change / add_query / add_craft_note / list_annotations / get_annotation; registry grew 14 → 20). New AnnotationsPane (⌘⌥A) with Accept/Reject/Archive/Reply per kind, stale-confirm alert, and reject-reasoning capture. HistoryPane (replaces CheckpointBrowserPane) shows unified op + checkpoint timeline with filter pills (All / Checkpoints / Edits / Annotations / External). Structured MCP error envelopes (`isError: true` with paragraph_not_found / prior_text_capture_failed factories). Tag `milestone-editing`. The "manuscript is yours" membrane held: Claude proposes via the annotation layer; the writer disposes via the UI.
- ✓ **Skills over MCP** (2026-07-18, branch `feat/skills-over-mcp-2026-07-18`) — Maugham now carries two task procedures as Agent Skills — `transcribing-notebooks` (the notebook-photo transcription workflow, now run through Claude Code after the 2026-07-17 Claude Desktop image regression) and `editing-pass` (reads `read_craft_intent` first, then works the annotation layer) — single-sourced at `docs/skills/<name>/SKILL.md` (agentskills.io flat-frontmatter format, loaded by the new `Maugham/Help/SkillIndex.swift`) and served across three surfaces: the emerging **SEP-2640 Skills Extension** (`skills/list`/`skills/get`/`resources/read` for `skill://` URIs, one pinned seam `Maugham/MCP/SkillsExtension.swift` against the still-unmerged draft PR #2640 — these are protocol methods, not tools, so the **48-tool count is unchanged**; no shipping MCP client consumes the extension yet), `get_help` (topic `"skills"` = index, a skill's name = its full body; one nudge sentence each on `read_document`/`add_comment`), and a new **Claude Code** section in the MCP setup sheet (copyable variant-aware `claude mcp add …` command + a one-click bootstrap-skill installer with staleness detection, writing a router skill to `~/.claude/skills/maugham/SKILL.md` that tells Claude Code to fetch the current procedure via `get_help` rather than improvise). Phone and `Packages/MaughamCore` untouched. Spec `docs/superpowers/specs/2026-07-18-skills-over-mcp-design.md`.

**Open:**
- • **Handwritten note import** — drag photos of handwritten pages in, Claude transcribes to `.md` using phone-camera filenames as ordering hints, page-by-page accept/edit/reject UI, automatic placement into manuscript or research. The annotation-layer approval UX from the editing milestone is the model.
- • **Project-level Claude prompt templates** — curated prompts like "Brainstorm character motivations for this scene" / "Find continuity errors in Chapter 3", pre-wired to MCP read-tools so Claude is grounded.
- ✓ **Voice notes / Whisper transcription** (2026-05-30, via the iPhone companion) — record voice in the phone Capture tab → inbox → WhisperKit transcribes → promote to research. *Still open:* a Mac-side drag-audio-file-in entry point (the shipped path is phone-capture, not desktop drop).
- • **Read-only Claude Code companion view** — sidebar in Maugham showing Claude responses without leaving the writing context.
- • **Human reviewer / collaborator layer (single-author lock)** — generalize the annotation membrane from "Claude proposes, writer disposes" to "any **non-author** proposes, the **author** disposes." A transferable **author lock** governs who may edit the manuscript; everyone else — human collaborators *and* Claude — operates in the existing annotation layer (comments, queries, suggested changes, surfaced in `AnnotationsPane` with Accept/Reject/Archive). **Design direction (provisional, 2026-06-06):**
  - **Three roles.** *Project owner* — persistent, singular (whoever created the project). *Author* — the current per-document lock-holder; the only one who may edit that doc. *Reviewer* — everyone else (human collaborators and Claude), annotation layer only.
  - **Lock = a transferable baton.** One holder per document; only the holder can edit. Only the holder can **release** (or flip themselves to review mode); once released, another collaborator may **claim** it. The **project owner** — and only the owner — can **force-takeover** a held lock (warned override), so an unreachable lock-holder can't freeze the project.
  - **Per-document granularity** (rides the per-doc op-log / per-device-JSONL grain), with **project-level "Claim all / Release all"** so the solo-writer case is one action and the co-authored collection case (Alice owns story 1, Bob owns story 2) works in parallel.
  - **Attribution:** reuse the existing annotation `OpKind`s; add an `author`/`source` provenance field so a named human reviewer is distinguished from Claude in the pane. Identity is a per-device display name set in Settings — no accounts.
  - **Access models, both in scope:** (1) shared iCloud-Drive folder, each collaborator on their own Mac, comments sync via per-device JSONL ([ADR 0012](adr/0012-per-device-jsonl-partitioning.md)); (2) a local **reviewer-mode toggle** on the writer's own Mac (the degenerate single-device case of the same lock).
  - **Deferred questions:** how owner identity is established and stamped on a shared folder (candidate: at project creation, alongside the minted `ProjectManifest.id`); where the lock lives for collection *project-references* vs loose docs; whether Claude's annotations should be visually grouped separately from human ones. Brainstorm to full spec before scoping.
  - **VCS-steals to fold in when scoping** (surfaced in the 2026-06-07 backup brainstorm): **patch commutation / real merge** (Darcs/Pijul) to replace crude LWW-by-paragraph for multi-author concurrent edits; **signed generations/checkpoints** (git/hg signed tags) so a collaborator's contribution carries verifiable provenance (ties to the **Backup & integrity** signing primitive); **blame / annotate** per-paragraph who/when-last-changed (the op log already holds per-op device/session/at).
  - **Skew-aware LWW + same-paragraph conflict surfacing is owned here** (deferred out of the 2026-06-07 hardening milestone, which shipped only merge/derive *determinism* — see `Maugham/OpLog/AREA.md`). Starting point: the audit's 0.2 (skew-induced cross-device LWW loss) + the `prior`-snapshot divergence-detection idea in `docs/superpowers/notes/2026-06-07-codebase-audit.md`; the authored-but-skipped `CrossDeviceIntegrationTests` case 4 records the scenario.
  - **Supersedes** the deferred "Shared folder collaboration" surface — the author lock *is* the locking/conflict-avoidance model that surface said it needed.

**MCP correctness:**
- ✓ **`read_document` / `add_comment` paragraph-sequence skew** (flagged 2026-06-28, via Claude Desktop; fixed by ADR 0018, shipped v0.12.4) — `add_comment` rejected a paragraph id (`paragraph_not_found`) that `read_document` had just returned, because `read_document` read a stale `.md` verbatim for closed docs while `add_comment` derived fresh from the op log. `read_document`'s closed-doc path now derives from the op log too (`DerivedManuscript`), so both walk one `sequence` and agree by construction. This was the motivating bug for ADR 0018.

- ✓ **`list_all_links` / `find_references` open-doc freshness** (ADR 0018 follow-up, shipped 2026-07-01 in the op-log-spine-hardening branch) — both now take the live `Document`'s in-memory state when the doc is open (the `store.documentStore?.document(for:)` branch `read_document` / `list_scenes` use), so a `[[wiki]]` typed-but-not-yet-flushed in an OPEN editor is no longer missed. The earlier "~750ms" framing understated the lag it closed: an open doc's ops land at the **burst window's close (30s idle / 90s cap / force-flush)**, not at the 750ms autosave — the autosave writes the `.md` + pending mirror but appends no ops. Consistency, not correctness.

**Annotation-layer follow-ups (carry-forwards from the editing milestone):**
- • **Sub-paragraph range anchors for suggested_change** — today an annotation anchors a whole paragraph; for tight edits ("change this clause") the writer wants character-range precision. Op schema needs a range field; UI needs inline highlight.
- • **Inline annotation marks in the editor** — gutter glyphs or margin chips next to paragraphs with open annotations, so the writer sees what Claude flagged while editing rather than only in the side pane.
- • **Bulk annotation operations** — Accept-all / Reject-all from the AnnotationsPane, filtered by kind. Today every annotation is one click.
- • **Cross-document annotation views** — surface annotations across every doc in a project, not just the active one. Useful when Claude reviews a whole novel and leaves notes per chapter.
- • **`craft_principles.md` aggregation** — accepted `craft_note` annotations live only in the op log today; future Claude sessions read them via `list_annotations(kind:craft_note, status:accepted)`. A project-level digest file would make them human-readable too and let the writer edit/curate them. See `docs/superpowers/specs/2026-05-19-editing-annotations-history-design.md` §1.4 for the deferred design.

---

## Group 3 — Publishing flow

Delivery, sharing, and mixed-media compilation.

**Shipped:**
- ✓ **Publishing pipeline** (merged to main 2026-05-29) — PDF (bundled tectonic/LaTeX) + EPUB (HTML/CSS), driven by per-project Claude-authored LaTeX templates + a small `config.json`. `EMISSION.md` body-emission contract; per-piece `style_file` overrides; fonts via fontspec; `Exports/` view in the binder. MCP surface 20 → 40 tools. Specs: `docs/superpowers/specs/2026-05-26-publishing-pipeline-design.md`, `…/2026-05-29-publishing-feedback-design.md`.
- ✓ **Mixed-content collection** (shipped 2026-05-17, tag `milestone-mixed-content-collection`) — `ProjectType.collection` made functional: a single project holding both "loose" mixed-content docs (each declares its own `mode: prose` / `mode: fountain` in the manifest) AND references to standalone Maugham projects, coexisting in one binder with per-item icons/affordances. Plus `read_document` MCP went polymorphic on images (crop-on-demand). 630 tests passing. *Still-open follow-ups:* "split a loose Collection doc out into its own standalone project later"; submission-tracker semantics for loose items vs. referenced projects.
- ✓ **Translation layer** (2026-07-22, branch `feat/translation-layer`) — a second Claude-parallel data plane ([ADR 0024](adr/0024-translation-layer.md)) for translated editions, structurally separate from both the manuscript and the annotation layer: per-paragraph `TranslationRecord`s in a per-device-JSONL sidecar (`.maugham/translations/`, ADR-0012 discipline), freshness (`fresh`/`stale`/`missing`) always derived from a server-stamped source-paragraph hash, never stored. New MCP surface — `write_translation` (all-or-nothing batched writes, `verbatim: true` for chrome that shouldn't be hand-copied, construct-parity drift warnings), `read_translation` (status-filtered, budget-enforced), `translation_status` (per-doc/project-wide coverage + `open_queries`) — 48 → 51 tools; `add_query` separately gained an optional `language` tag to mark a query as a translator question (params only, no count change). `compile`/`republish` gained `language`+`allow_stale`: a blocking coverage gate refuses a translated edition with stale/missing paragraphs (itemized per piece) unless explicitly overridden, a zero-layer guard refuses unconditionally when a language has no translations at all, and Fountain pieces get an element-drift warning when translated text tokenizes to a different screenplay structure than the source. Language-suffixed templates (`template.es.tex`, `styles.es.css`, per-piece style files) resolve automatically when present, falling back to the base file — template CONTENT is the writer's job, not the gate's. Editor gained a read-only Translation Review mode (View menu picker, stale/missing badges, ⌘⌥8 pane with source text + query-reply) built on the same membrane-flip shape as `setLockEditing`; closed a real buffer-identity race in the process (`EditorSurface.reconcileTextBuffer` must flip the membrane before the buffer-swap decision — see the Editor AREA.md tripwire). New `translation-pass` skill; guide topic `translation-review.md`. Spec `docs/superpowers/specs/2026-07-22-translation-layer-design.md`.
- ✓ **Publish-pipeline improvements** (2026-07-23, PR #17, shipped v0.25.0) — driven by the first two real books through the pipeline (*Playlist, Volume One*'s EN and ES editions), which surfaced every place publishing still needed an external tool or a throwaway version. A per-piece `sections.<docId>.include` flag (`PublishConfig`, additive-optional) lets a subset of the binder compile, publish and gate-check as a first-class versioned edition — drafts for a future volume no longer block today's book, and excluded pieces don't emit, don't appear in the contents, and don't gate a translated edition's coverage check. `preview_compile` gained `language` + `allow_stale` parity with `compile`, and `compile` gained `dry_run` (answers "would this edition build" with no output, Publication, or version bump) — no more junk versions minted just to check a translated template. New `read_preview_page` closes the same read-the-rendered-page loop `read_publication_page` gives a real Publication, but for preview output, so Claude no longer shells out to poppler between edits (MCP surface 51 → 52 tools). Also: `EMISSION.md` now refreshes on every compile/preview stamped with the generating app version so it can't drift from the installed pipeline; the fountain title block routes through a `\screenplaytitleblock` hook a per-piece style file can redefine; and three field-found LaTeX defects are fixed (chapter titles missing from custom-ToC-style tables of contents, a `\scenebreak` centering leak into following paragraphs, and a hard line break swallowing a line starting with `[`). Spec `docs/superpowers/specs/2026-07-23-publish-pipeline-improvements.md`; release notes `docs/release-notes/v0.25.0.md`.

**Compile (cross-type):**
- ✓ Compile to **PDF + EPUB** — shipped via the publishing pipeline (2026-05-29). Still open: Word / plain-text output.
- • Markdown manuscript export for novels — Shunn standard (Times New Roman 12pt, double-spaced, 1" margins) for short-fiction submissions
- ✓ EPUB cover image handling — `config.cover.path` (+ `epub_specific_path`); embedded by the EPUB packager.
- ~~**Clean export to `Exports/`** (anchor-stripping)~~ — **superseded by ADR 0019 (clean `.md` on disk, shipped in the clean-`.md` milestone).** The manuscript files on disk are now *already* standard anchor-free Markdown/Fountain (no `<!-- ¶XXXXXX -->`, no `<!--t-XXXXXX-->`), portable to any text editor, `git diff`-clean, and pandoc-able — so a separate "export an anchor-stripped copy" action is no longer needed. *Residual (small, deferred) idea:* a **filtered** export — drop `- [x]` done items and/or `[[todo: …]]` segments from a generated copy — which ADR 0019 does NOT do (the live file keeps those). Reframe as "filtered export" if a writer ever wants it; not anchor-related.

**Screenplay-specific production polish:**
- • FDX export and import (Final Draft binary format)
- ✓ Scene numbers (`INT. KITCHEN - DAY #4A#`) — shipped in the CommonMark + Fountain expansion milestone (2026-07-06, Group 1 above): parsed into `FountainLine.sceneNumber`, faded in the Mac editor, right-aligned in published PDF/EPUB. Not shown on the phone Read tab (see `cross-surface-contracts.md` — a recorded free divergence, not a contract).
- • MORE / CONT'D markers across page breaks
- • Revisions — color-coded change marks per draft

**Submission workflow (speculative — confirm interest before scoping):**
- • Submission tracker — "this story is at Magazine X (sent 2026-04-12, awaiting response)"; per-item state machine, deadlines, reminders.

---

## Group 4 — Foundations & safety

Reliability the writer doesn't think about until it bites. Not glamorous, but each item builds Maugham's "trust me with your novel" credibility.

**Day-to-day reliability:**
- ✓ **Scoped window events (ADR 0021)** — shipped (2026-07-02, this branch). Every surviving `maugham.*` name (see `Maugham/Events/MaughamEvent.swift` for the current set) now flows through the typed `MaughamEvent` wrapper (post declares `.keyWindow`/`.document`/`.project`/`.allWindows`; receive helpers own each filter + a closed-window liveness guard) with a tripwire forbidding raw posts/subscriptions outside it; fixes `toggleInspector`'s double-window toggle, cross-window scene/find/paragraph navigation, and cross-window HistoryPane/checkpoint reloads by construction; 2 dead posts with no receiver deleted (`maughamOpLogChanged`, `maughamInboxChanged`). The companion scene-storage-release spike (closed-window view-graph retention) ran timeboxed on the same branch — outcome: documented framework cost (ARC no-cycle proven headlessly; dev-gated `CoordinatorLeakProbe` + a user-runnable footprint recipe kept for the GUI-only half; see `docs/superpowers/notes/2026-07-02-scene-storage-spike.md`). See [ADR 0021](adr/0021-scoped-window-events.md).
- ✓ **Zombie-window teardown mitigation (workaround 1, 2026-07-02)** — the spike's retain-root trace pinned SwiftUI's `static GraphHost.sharedGraph` as the retainer of a closed `WindowGroup` scene's view graph (so `dismantleNSView` never runs on ⌘W), and showed the payload is almost entirely OUR `@State`. Since the ROOT scene view's `.onDisappear` (`ProjectWindow`) fires on close, we empty the zombie ourselves — measured & iterated in three parts: (1) `ProjectWindow.onDisappear` scorches its heavy `@State` (`store`/`documentStore`/`lastParsedScript`) under an `isLive` guard and `MaughamTextView.viewWillMove(toWindow: nil)` detaches the coordinator on the close path `dismantleNSView` misses (→ 0 live coordinators); (2) `DocumentStore.close()` drains its document registry (a nested `@State`'s stored `DocumentStore` kept it alive — a NESTED view's `.onDisappear`, e.g. EditorHost's, does NOT fire on window close, so its unregister was unreliable); (3) `Document.close()` husks its own O(doc) in-memory state (paragraphs/op-log-mirror/caches) because EditorHost's `@State` box for the `Document` stays retained by the dead scene and can't be nil'd from outside. EditorHost.onDisappear remains a belt for the segment-switch abandonment case only. Expected residual drops from tens of MB per closed window to SwiftUI's AttributeGraph husk (100s of KB). **Future escalation** (its own milestone, NOT this workaround): explicit `NSWindowController` hosting to actually release the scene — trigger to schedule it is a manual footprint A/B still showing material residual after this mitigation, or long-session RAM creep from repeated open/close of large projects.
- ✓ Trash & undo for binder operations — shipped under Group 1's research polish milestone. See [ADR 0006](adr/0006-trash-and-undo.md).
- • Snapshots — versioned manuscript saves with labels ("before-rewrite", "agent submission"). Lighter-weight than History Rewind: an explicit named bookmark rather than a scrub-anywhere capability. The two pair well — Snapshots is the user-curated index, History Rewind is the open-ended exploration.
- • **Manuscript recovery options (thoughtful auto-repair)** — scheduled 2026-08-09 (Denver, on landing the RULING-54 op-log refusal: "so the author isn't just stuck with an unopenable file — give them some options to get to some of the data but non destructively"). The refusal is the FLOOR: a document whose op-log file is unreadable now refuses to open with the file named (the alternative was opening shorter and letting autosave truncate the `.md` — the mapped cascade), but the writer's only remedies today are fix-the-permissions-yourself or restore-from-backup. The milestone designs the ladder above the floor, every rung non-destructive: **open read-only from the readable files** (a clearly-labelled partial view — no autosave, no ops appended, so nothing can supersede the unreadable file's paragraphs when it returns); **wait-and-retry for iCloud** (a dataless stub is transient — detect and say so rather than presenting it as a permissions problem); **quarantine-and-continue** (move the unreadable file aside into `.maugham/conflicts/` untouched, open from the rest, and offer a merge when it becomes readable — the conflict-backup machinery already half-exists); and **guided restore** (surface the backup system's single-document restore, currently a deferred follow-on of v0.8.0, as the refusal's own suggested action). Precedents to build on: the manifest-shadow self-heal (a corrupt manifest no longer means "can't open"), the segment salvage path (readable-but-corrupt already recovers what it can), and RULING-4's recoverability clause. What it must never do: write anything derived from a partial view over durable state — the read-only rung is load-bearing for every other rung.
- • **Research protection** — scheduled by RULING-48 (2026-08-09; ruled into existence by Denver 2026-08-02: "this is pushing me to a research protection milestone in future"). RULING-24 made the protection tiers explicit — the work (op log + checkpoints), research (recoverable, not versioned), ingested/derived (none) — and the register's reconciliation found the middle tier's stated safety net didn't exist: research notes have no op log, `CheckpointCapture` never enumerates `manifest.research`, so recovery was entirely Time Machine's. **The bridge already shipped** (2026-08-09): a promotion Rewrite sends the note's or palette card's current text to the visible Trash before writing (`TrashSubject.priorVersion`, restorable for the 30-day window), and RULING-48 ratifies it as the line-holder. **The milestone decides what tier-2 protection means**: version research text only (`.document`-kind is separable from heavy assets at the type level — the option the economics permits), extend ⌘S snapshots to research content, generalise never-overwrite-only-supersede, or ratify-and-say-so. Open questions + the full finding: `register/history/MILESTONE-research-protection.md`. Related: RULING-24, RULING-4, RULING-15, RULING-7; PRINCIPLE-4's ROI-parking of versioning work lifts when this is scoped.
- ✓ **Backup & integrity** (shipped **v0.8.0**, 2026-06-07) — an **integrity primitive** (Merkle manifest; surface + quarantine the previously-silent op-log parse-skip; conflict-twin + dangling-checkpoint-pointer + semantic garbage-paragraph-id checks) + a **filesystem-only backup system** (Backups Settings tab → any local/cloud-synced/external folder; safe-by-default; per-project keying by minted `ProjectManifest.id` so several projects can share one destination; per-destination retention; atomic copy-on-write generations; skip-unchanged via a content signature that ignores per-save checkpoint churn; **integrity-checked before every backup so corruption can't propagate**; per-project "backups paused" banner) + **restore** (`RestoreWindow` reached from the banner and File → Restore from Backup…; restore-**beside** never-overwrite; integrity badges; auto-bisect-to-good). Complements (doesn't replace) Time Machine; encryption deferred to the destination. Spec `docs/superpowers/specs/2026-06-07-backup-and-integrity-design.md`; plans under `docs/superpowers/plans/2026-06-07-*`. *Replaced the old "iCloud version-history surfacing / Time Machine note" sketch.* **Manifest-shadow recovery** shipped post-v0.8.0 — a corrupt/truncated `project.maugham.json` now self-heals on open from a verified (checksummed) shadow under `.maugham/`, so a damaged manifest no longer means "can't open the project." **Remaining deferred follow-ons** (capture for later, roughly in priority): (1) **single-document restore** — op-log surgery to pull one doc's content from a generation back into the live project as new rewindable ops; the marquee piece, wants its own milestone; (2) **derive-and-compare** — re-derive the manuscript and flag structural breakage to catch valid-but-wrong corruption; needs care so legit external `.md` edits don't false-positive; (3) **essential/full file classification** — trim regenerable artifacts (`Exports/`, build outputs) from *remote* backups; pure bandwidth optimization, lowest urgency.

**Future-proofing:**
- • **Manifest schema versioning** — `project.maugham.json` is at `schemaVersion: 1`. Define migration story before it evolves (e.g., when 4a adds pageTarget per-item). A coordinated rename pass to clean up the [ID prefix inconsistency](adr/0007-id-prefix-no-migration.md) would fit naturally here.
- • Performance pass — long-haul project simulation (100k words, 30 chapters): editor responsiveness, Project Statistics, cross-document operations stay O(scale)-aware. **Typing-latency leg shipped 2026-06-10** (branch `oplog-growth`, found during the M2 seal smoke at 70-page single-file scale): per-keystroke 325 ms → ~40 ms at 100 KB via (a) windowed typography application (whole-`NSTextStorage` restyle 227 ms → 0.8 ms, per-character equivalence-pinned; dual-dialogue `^` state folded into token identity), (b) `setFullText` parse-once (4 whole-doc parses → 1) + indexed exact-match tier (O(N²) → O(N), deterministic stored-order FIFO), (c) scanner ASCII fast paths + `TaskAnchorAlignment` unchanged-paragraph early-out (differential-pinned vs verbatim pre-fix oracles). Round 2 (live `sample` of the running app — headless probes can't see these): killed two more hidden per-keystroke whole-doc Fountain parses (word-count bookkeeping + the ProjectWindow metrics mirror, now debounced), nativized the NSString-backed `textView.string` at pipeline entry (foreign strings pay per-char objc_msgSend, 5–20×), debounced the per-keystroke script broadcast to the Scenes sidebar + one-pass cached `sceneSummaries()` (tripwire 4). Also smoke-found + fixed: **paragraph-id birthday collisions at paste scale** (`ParagraphID.mintUnique` at all mint sites + first-wins legacy-dup tolerance in TaskAnchorAlignment — a dup either trapped `Dictionary(uniqueKeysWithValues:)` or silently merged two paragraphs' op-log histories). **Remaining floor, confirmed by a third live sample at 140 pp: `FountainTokenizer`'s whole-doc per-keystroke walk (linear, Debug-amplified) + `ElementGutterView.draw` per-line redraw work (~5%) — future item: tokenizer buffer rewrite + gutter draw caching**, with `MaughamTests/Performance/TypingLatencyProbeTests` as the re-baselining probe (stage chunks at `/tmp/maugham-perf-probe`) and `sample <pid>` during live typing as the verification method of record. The focus-dim incremental rework (audit item 6) folds in here too — measured at only 2.4 ms at 70 pp, low priority. **Typing-perf milestone shipped 2026-06-10 (v0.11.0, merge `2754f3a` + fix `853940d`)**: FountainTokenizer + ParagraphParser rewritten as single buffer passes over a documented `LineRecord` seam (future incremental tokenization re-derives from it), each arbitrated by a frozen verbatim-reference differential oracle in the test targets (grammar changes must update BOTH); restorePairs candidate shingle/bigram sets memoized per-Document (semantics-identical); footer metrics computed from the keystroke's own parse on the debounce trailing edge (the pause-edge re-parse is deleted); FountainScript `==` rejection gates; gutter draws bounded to the visible range. 120 pp ~142→37 ms, 250 pp ~227→73 ms Debug (Release ≈ 2.5–3×, faster); live 553 KB sample post-fix: typing-window main-thread work −8×. **Follow-up (small, filed)**: the SESSION word-count split in `DocumentStore.recordEditorTextWrite` still runs per keystroke on the binding side (~22 ms at 553 KB, ~5 ms at 120 pp — invisible to headless probes; found by live sample take-3, details in `docs/superpowers/notes/2026-06-10-typing-perf-baseline.md` §verification). Fix shape: defer/debounce the count off the keystroke (session semantics are 30-min-idle-windowed; keystroke-exact words unnecessary) — but `recordEditorTextWrite` is regression-scarred (its doc-comment records the 2026-05-19 silent loss of session bookkeeping); keep recordSessionActivity's timer-arming per keystroke and pin with a test that the words still land. **2026-06-09 audit punch-list disposition (items 1–5 shipped 2026-06-10, merge `38b2c5d`):** item 7 (`author` provenance on `Op.Provenance`) and item 9 (surface same-paragraph divergence via `prior`-snapshot detection; retires the 0.4 silent-collision residue) → **collaboration milestone** (9 is its first deliverable, spec first); item 8 (perf fixture + restoreComments indexing + script-notification debounce) → **done** (v0.10.0); item 10 (live-tail append framing, audit 0.6 residue) → **standalone reliability backlog item, downgraded**: M2's seal threshold bounds the unframed exposure to the live tail's ≤512 KB window and segments carry checksums — it maps to neither perf nor collaboration. N3 (updater rename-fallback brick window) remains known-and-unranked, deliberately untouched.
- ⤴ **Op-log compaction (Automerge/CRDT-style)** — **superseded** by [ADR 0016](adr/0016-op-log-growth-without-compaction.md): truncation breaks the append-only invariant and dangles checkpoint pointers / pre-horizon rewind / Merkle signatures. Replaced by the growth plan below.
- • **Op-log growth plan ([ADR 0016](adr/0016-op-log-growth-without-compaction.md))** — bound disk, sync churn, and derive time *without* deleting history: (1) ✓ **sequence keyframing** (M1, 2026-06-09, branch `oplog-growth`) — emit `sequence` on a burst only when ordering changed (+ first-burst-after-load + every-50 keyframe floor); kills the dominant redundancy for single-file screenplays. Fixture: novel op-log −39%, screenplay −78% / load 208→97 ms; the <5% sequence-share budget holds for typical drafting (the perf fixture's aggressive 1-in-7-burst reorder cadence is fully decomposed in `docs/superpowers/notes/2026-06-09-oplog-growth-m0-baseline.md`); (2) ✓ **sealed compressed segments** (M2, 2026-06-09, branch `oplog-growth`) — rotate each per-device JSONL at a size threshold into immutable SHA-256-checksummed LZFSE `.mzseg` segments; iCloud uploads each once (no more whole-history re-upload per burst) and the checksum closes most of the torn-append window (audit 0.6). Scope (T14): never the legacy unsuffixed file, never another device's tail, Mac-only seal; the phone reads segments for free through the shared MaughamCore helpers (`.mzseg`/seal grep tripwires on both targets); (3) **derived-state cache** under `.maugham/cache/` — pure deletable cache for load time, never truth. Sequence after the 100k-word perf fixture (performance pass) quantifies which terms dominate; keyframing first.

**Distribution & onboarding:**
- ✓ Production release pipeline (2026-05-23) — first installable Maugham as a `.dmg` from GitHub Releases + Tier 1.5 auto-update (silent background download, banner nudge on each ProjectWindow, state-derived **Maugham → Check for Updates…** menu hosted as a separate Window scene) + tag-triggered CI on `macos-15` with `latest-stable` Xcode + scripts/cut-release.sh local pre-flight + dev/stable variant coexistence (`BuildVariant.current` enum drives bundle id, display name, support folder, MCP socket path, Claude Desktop config key, MCP `serverInfo.name`, and updater on/off — Debug builds run as `Maugham Dev` at `com.maugham.Maugham.dev` with their own state and their own `maugham-dev` MCP entry). Version is tag-derived (CI rewrites `CFBundleShortVersionString` from the tag). Tag `milestone-production-release`; v0.2.0/0.2.1/0.2.2 cut through the pipeline (four shakedown bugs caught + fixed); 873 tests passing.
- ✓ **Code-signing & notarization** (shipped v0.6.0, 2026-06-03) — Developer ID Application cert + hardened runtime; CI notarizes and staples, so downloaded `.dmg`/`.zip` launch Gatekeeper-clean (no right-click → Open). Shipped alongside an **in-place auto-updater** (verified `.zip` → detached-helper swap → relaunch, or applied silently on next ordinary quit) that replaces the earlier reveal-the-`.dmg`-in-Finder behavior. Dev builds stay ad-hoc (`com.maugham.Maugham.dev`, updater disabled). Tag `milestone-mac-autoupdate`; spec `docs/superpowers/specs/2026-06-01-mac-auto-update-design.md`.
- ✓ **Onboarding — welcome tour + sample projects + in-app help** (shipped **v0.12.0**, 2026-06-16) — first-run 8-card `WelcomeCarousel` (gated by `UserPreferences.hasCompletedWelcome`; re-openable via **Help → Welcome to Maugham**) that forks into a hands-on **sample Novel / Screenplay** (`SampleProjectBuilder` copies a bundled seed under `Maugham/Resources/Samples/`, opened via the standard `Document.load`→`Bootstrap` path; the sample's own chapters double as a guided tour and seed `- [ ]` tasks). The user guide is split into `docs/guide/*.md` + `index.json` (single source), bundled into the app and read by BOTH the in-app **Maugham Help** window (⌘?, `HelpWindow`/`HelpTopicIndex`/`GuideMarkdownView`) AND a new read-only **`get_help`** MCP tool (43→44). Help-menu actions route through a shared `OnboardingModel` so they work even when the Welcome window is closed. README rewritten writer-first. Spec/plan `docs/superpowers/{specs,plans}/2026-06-16-onboarding*`; merge `9efa872`. *Still open from the original sketch:* a clearer New Project sheet + better empty states; tour illustrations are drawn-in-SwiftUI with slots for real screenshots later.
- • Project templates — Three-Act Novel, Hero's Journey, Short Story, etc.; pairs with distribution since templates are onboarding-flavored

---

## Group 5 — iPhone companion (mobile surface)

A phone surface for what the desk app can't reach — capture out-and-about, read on the go, triage Claude's annotations away from the desk. Built on iCloud Drive + the op log; the manuscript stays Mac-only-for-editing. Plan: `docs/superpowers/plans/2026-05-24-iphone-companion-v1.md`; ADR [0012](adr/0012-per-device-jsonl-partitioning.md) (per-device JSONL).

- ✓ **Mac groundwork** (merged 2026-05-29, merge `98128d1`) — extracted `Packages/MaughamCore` (Foundation-only shared substrate) + minted `ProjectManifest.id`; per-device JSONL partitioning so phone + Mac never conflict-twin a shared file; capture **inbox** (`InboxPane` ⌘⌥6, badge/promote/audio/edit/trash) with **WhisperKit** re-transcription behind a `Transcriber` seam; MCP 40 → 43 tools (`list_inbox`/`read_inbox_entry`/`promote_inbox_entry`).
- ✓ **iOS app — Phases D0–F** (merged 2026-05-30, merge `3fff8b5`) — the `MaughamPhone` four-tab app: **Capture** (text/photo/voice → inbox; on-device speech draft), **Read** (Markdown blocks + semantic Fountain), **Annotation review** (Accept/Reject/Archive op-log writes; opt-in Face ID gate). iCloud-Drive eviction handling; security-scoped bookmarks; shared `MaughamCore`. Two anti-duplication consolidations (`Deriver` + `MarkdownDisplayFilter` promoted to MaughamCore) + the `ScreenplayEmphasis` cross-surface contract. Smoke-verified on the simulator — six real bugs found + fixed (see `MaughamPhone/AREA.md`). Mac 1467 / phone 126 tests green.
- ✓ **Phase G — TestFlight pipeline + AppIcon** (shipped; first release `phone-v0.1.0` build 854, 2026-05-31) — `phone-v*` tag namespace, `phone-release.yml` GH Actions → TestFlight (archive → `-exportArchive` upload, altool-free on purpose), `cut-phone-release.sh`, signing secrets, M+¶ app icon. Five on-device bugs surfaced via the dry-run-is-the-integration-test loop; a follow-up doc-id contract fix shipped in `phone-v0.1.1`. Setup: `docs/release-notes/phone/SETUP.md`.
- ✓ **Cross-surface contract enforcement** (merged 2026-06-03; v0.6.1 / phone-v0.1.2) — a three-tier model (shared-impl / contracted-divergence / free-divergence) with action-triggered guards (MaughamCore choke-points + reach-around grep tripwires) so the phone can't reimplement what the Mac owns. Registry: `docs/superpowers/notes/cross-surface-contracts.md`. Fixed two real bugs (phone Fountain emphasis dropped; Mac task-anchor leak). Merge `ce46402`.
- ✓ **Annotations drill-down + show-resolved** (shipped phone-v0.2.0, 2026-07-02) — the Annotations tab is now a project → chapter → notes drill-down (binder-order chapters under group headers, single-doc skip, unmapped docIds surface under "Other") with a global **Open / All** toggle that brings back fully-triaged chapters/projects for read-only review (dimmed Resolved section + status chips). Phone-local: `AnnotationsStore` (@Observable grouped tree) + pure `groupByChapter`/mode-filters; no MaughamCore/schema change. Spec `docs/superpowers/specs/2026-07-01-phone-annotations-drilldown-design.md`, plan `docs/superpowers/plans/2026-07-01-phone-annotations-drilldown.md`. Whole-branch review caught two emergent bugs on the newly-reachable resolved path (read-only review had to touch `AnnotationDetailView` — the plan's "untouched" assumption was wrong; and leaf/middle now re-slice from the store for mid-stack count freshness). On-device smoke passed.
- ✓ **Annotations undo / reopen** (shipped phone-v0.5.0, 2026-07-10, in the unified-undo milestone — [ADR 0023](adr/0023-unified-oplog-backed-undo.md)) — resolved annotations gain **Reopen** (rejected/archived → the new `annotationReopen` op via the shared MaughamCore `AnnotationInverse` factory) and **Reopen & Revert** (accepted → full `claudeAcceptRevert` text revert with drift-confirm, Mac-Revert parity — the "status-only" stance from the original sketch was deliberately replaced with full revert, a user decision 2026-07-09). Schema v3; paired with Mac v0.18.0.
- • **Polish backlog** (uncommitted, may change) — inbox preview in the Capture tab (a small read-only list/count of pending `.maugham/inbox/` entries for the selected project). See the plan's Phase H backlog.

---

## Deferred surfaces (not on the roadmap)

Considered and explicitly de-prioritized. Each gets a fresh brainstorm if/when prioritized.

- • **iPad companion** — read-only first, then drafting. Separate engineering bet on a different surface; not a feature of the desktop Mac app.
- ⤴ **Shared folder collaboration** — **superseded** by the **Human reviewer / collaborator layer (single-author lock)** in Group 2. The author lock is exactly the locking / conflict-avoidance model this surface said it needed; promoted from "deferred" to an active, scoped roadmap item.
- • **Goal-tracking calendar widget** — macOS widget extension; separate target from the app proper.

---

## Sequencing notes

- **Group 1's Tasks milestone** is the pathfinder for the analytical-layer arc and an immediately-useful daily-flow improvement; pickable independent of Phase 4a. The three subsequent layers (symbol DB, lint, writing analytics) are not on the roadmap yet — each gets a fresh brainstorm once Tasks is live and the shape is proven.
- **Group 1's screenplay intelligence (Phase 4a)** is the natural next drafting-flow milestone — picks up the inline-autocomplete and slugline-reuse work that was carry-forwarded from 3b/3c.
- **Group 2's manuscript-edit loop is closed** as of the editing milestone (2026-05-19): Claude can propose via the annotation layer; the writer disposes via the AnnotationsPane. The next AI-assist milestones are pickable in any order — Handwritten note import reuses the proposal/approval UX from the editing milestone, so it's the most natural follow-up if AI assist is the priority. The **human reviewer / collaborator layer** generalizes that same membrane to human collaborators behind a single-author lock — it's the largest open Group 2 bet and supersedes the old deferred shared-folder surface.
- **Group 3 (Compile)** is the "I want to send this to my agent" feature. Premature while still drafting; pick it up when there's something to ship.
- **Group 4 (Foundations)** items are pickable any time. **History Rewind** is the most interesting new candidate — the op log infrastructure makes it cheap to compute, and the "time travel over your own writing" affordance lands somewhere between Snapshots and the milestone-editing history view. Distribution becomes urgent only when Maugham needs to leave the dev Mac. Schema versioning becomes urgent only when a non-additive manifest change forces it.
