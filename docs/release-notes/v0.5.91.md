# Maugham 0.5.91

_Released 2026-06-02 — THROWAWAY DRY-RUN BUILD, NOT A REAL RELEASE_

This is a disposable build used to prove the Developer ID signing + notarization
pipeline on real Apple infrastructure (re-run after a transient CI network drop in
v0.5.90, with a hardened submit-then-poll notarize step). It will be deleted. Do not
install it as a stable release.

## What's being tested
- Developer ID Application signing in CI
- notarytool acceptance of the hardened-runtime build (WhisperKit + tectonic + maugham-mcp)
- the hardened notarize step surviving transient runner network drops
- inside-out nested-code signing + staple
- Gatekeeper-clean first launch (no right-click → Open)
