# MaughamPhone 0.0.2

_Released 2026-05-31_

## What's new
- Second pipeline dry run — verifies the iCloud folder-pick fix on a real TestFlight
  build. Not a feature release; delete after verifying.

## Fixes
- Picking the iCloud projects folder failed on the first TestFlight build with
  "Couldn't open folder because it doesn't exist." Fixed: security-scoped access is
  now started before the bookmark is minted.

## Known issues
- Throwaway build. The first real release is `phone-v0.1.0`.
