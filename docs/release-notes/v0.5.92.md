# Maugham 0.5.92

_Released 2026-06-03 — THROWAWAY DRY-RUN BUILD, NOT A REAL RELEASE_

This is a disposable build used to prove the in-app in-place auto-update round trip
(v0.5.91 → v0.5.92): download notarized `.zip`, verify, swap the running app via the
detached helper, relaunch. It will be deleted. Do not install it as a stable release.

## What's being tested
- "Restart & Update" → quit → swap → relaunch as 0.5.92
- "Dismiss" → silent install on next ordinary quit
- detached helper survives app exit; no relaunch race
