# Issue #35 — statement-rollback follow-ups (well guard, failed-save restore, shared stat, filing hash)

**Issue:** https://github.com/dtrouton/Maugham/issues/35
**Spec authority:** `Maugham/Stores/AREA.md` (the ingest pair "leaves nothing behind on failure, in either order", `:103`; the derived-never-spelled well name, `:102`; the non-zero-size rule, `:142`); RULING-8 (a second code path may not answer the same question differently); `register/RECONCILE.md` + `register/scripts/flip-claim.py`'s own lifecycle docstring.
**Branch:** `worktree-issue-35-statement-rollback`, off main `d110c449`.

## Findings, re-verified 2026-08-27

- **Item 1 — real; the issue's "sound by construction" needs a second check.** `ProjectStore.rollbackUnusedStatement` (`Maugham/Stores/ProjectStore+Statements.swift:322-356`) refuses on open (`:328`), words (`:330-332`), file bytes (`:334-337`), unknown row (`:339-340`). The drop side (`addImage(toStatement:)`, `ProjectStore+StatementAssets.swift:104-122`) writes the picture into the well via `ImagePasteHandler.saveAndReferenceFile` (`:116`) with NO gate, then suspends before `appendToStatement` takes the gate (`ProjectStore+Statements.swift:485`); the mint side (`StatementEditorHost.mintAndBind:757`) takes the gate at `:326`. Window confirmed. A guard placed with the four refusals narrows it, but `saveManifest()` at `:345` is a real suspension, so the picture can land after the guard and before `removeItem` at `:354` — the check must also run immediately before the destructive step. There is NO production well-URL derivation outside the `private` `ImagePasteHandler.destination(forNoteAt:in:ext:)` (`Maugham/Editor/ImagePasteHandler.swift:147-174`, which also creates the directory); `StatementImageIngestTests.swift:69-75` has a test-side copy whose comment warns against a third spelling.
- **Item 2 — half real, half declined.** `insert(at: row)` at `:350` uses an index captured at `:339` across `await saveManifest()` — trap risk on a concurrent removal; fix `min(row, count)`. The `manifest.modified = previouslyModified` restore at `:351` is the SAME shape as `commitProductionRoles` (`ProjectStore+ProductionRoles.swift:179-193`, `:190`), pinned by `ProductionRoleStoreTests.swift:398-400` ("the stamp goes back with the row it was made for"). **Ruling: keep the stamp restore** — dropping it in one verb and not its sibling is RULING-8's shape; if Denver wants the stamp policy changed it is one question over both verbs. Nothing tests the rollback's failed-save path today; the idiom is a real `0o500` project dir (`ProductionRoleStoreTests.swift:374-411`).
- **Item 3 — real, "byte-for-byte" corrected.** `statementFileHasBytes` (`ProjectStore+Structure.swift:616-620`, `private`, one caller `:533`) vs the rollback's stat at `:334-337`: same `stat`, same key, same `?? 0`, inverted polarity. Sharing = drop `private`, call `!statementFileHasBytes(statement)`; keep `fileURL` for `removeItem`.
- **Item 4 — real, plus a sibling field.** `register/reconciliation/Inbox.filings.json` claim `M8-IN-004`: `fix.commit` (`:60`) AND `fix.date` (`:61`) both hold two-halves prose; every other row is a bare 8-char hash + `YYYY-MM-DD`. `a9cbba32` is confirmed the S7 asset-half fix (2026-08-12); `6955c2d8` was the card half. `flip-claim.py` has an optional `fix.note` field for narrative. Rows are hand-edited; `_summary` is regenerated by `python3 register/scripts/flip-claim.py recompute --module Inbox`. **Bonus:** `flip-claim.py repoint` matches `a.old` / `a.old[:10]` but filings store 8-char hashes, so a post-rebase repoint given a full sha exits "not cited" — fix in the same commit.

## Global constraints

1. One derivation of a statement's well URL, owned by `ImagePasteHandler` (the saver); `destination` calls it; the guard calls it; the test-side copy in `StatementImageIngestTests` is replaced by the production one (it is `internal`, same target) — no third spelling.
2. A refusal leaves everything as it was: row present, stamp unchanged, file present, well untouched.
3. Every refusal/guard test has a control (an empty or absent well still rolls back).
4. The stamp-restore policy is unchanged and now TESTED for this verb (mirror `ProductionRoleStoreTests:374-411`).
5. Rule 10: the verb's doc-comment refusal list (`ProjectStore+Statements.swift:291-311`) and the miniature at `ProjectStore+StatementAssets.swift:142-144` gain the fifth refusal; `Maugham/Stores/AREA.md:103` gains one clause; no prose counts.
6. `TripwireGrepTests.statementOpenGateTakers` census (`:3236-3243`) unchanged — the guard lives inside the existing gate.
7. Register edits by hand + `recompute`; never hand-edit `_summary`.
8. `./scripts/test.sh` per task; full gate before merge.

## Task 1 — the verb: well guard (both checks), safe re-insert, shared stat

**Files:** `Maugham/Editor/ImagePasteHandler.swift`, `Maugham/Stores/ProjectStore+Statements.swift`, `Maugham/Stores/ProjectStore+Structure.swift`, `Maugham/Stores/ProjectStore+StatementAssets.swift` (comment), `Maugham/Stores/AREA.md:103`; tests `MaughamTests/StatementStoreTests.swift` (`:220-350`, fixture `loadedNovel(named:)`), `MaughamTests/StatementImageIngestTests.swift` (`well(besideStatementAt:in:)` `:69-75`).

1. `ImagePasteHandler`: extract `static func wellURL(forNoteAt noteURL: URL, in projectURL: URL) -> URL` (internal; the pure derivation, no directory creation) from `destination`, which now calls it and then creates the directory. Replace the test-side copy with a call to it.
2. `ProjectStore`: `func statementWellHoldsAnything(_ statement: Statement) -> Bool` — `wellURL(...)` exists AND `contentsOfDirectory` has at least one regular file (a stray `.DS_Store` should not count — filter hidden files; say so in the comment). Fifth refusal in `rollbackUnusedStatement` beside the other four, AND a second check immediately after `saveManifest()` succeeds and before `removeItem`: if the well filled during the save, re-insert the row at `min(row, count)`, restore the stamp, `saveManifest()` again, return false — comment why (the drop writes its picture ungated and suspends before its gate; the save is the one suspension after the guard).
3. `insert(at: min(row, manifest.statements.count))` in the failed-save restore; stamp restore stays; comment cites `commitProductionRoles` as the sibling and the test that pins the stamp there.
4. `statementFileHasBytes` → `internal`; the rollback calls `guard !statementFileHasBytes(statement) else { return false }`; commit message says "same stat, inverted polarity" not "byte-for-byte".
5. Tests (TDD):
   - `test_rollbackRefusesWhenThePicturesWellIsNotEmpty` — mint an empty statement, drop one picture through `addImage(toStatement:)` WITHOUT appending the ref (the race's first half), `rollbackUnusedStatement` → false; row present, file present, well file present.
   - `test_rollbackRefusesAPictureThatLandedDuringItsOwnSave` — the second check: simulate by writing a file into the well from a hook that runs during `saveManifest` (if there is no seam, use the `DocumentStore` presenter callback or make the test drop the file between the guard and the save via a `Task` racing on the main actor — say which and why it is deterministic; if no deterministic shape exists, test the second check by calling it directly as a static/pure predicate and say so). Row re-inserted, stamp restored, file present.
   - `test_rollbackStillRemovesAStatementWithAnEmptyOrAbsentWell` — the control (both: no well dir; an empty well dir).
   - `test_aFailedSaveDuringRollbackPutsTheRowAndTheStampBack` — the `0o500` idiom; row back at its index, `manifest.modified` equals the pre-call stamp, `.md` still on disk, return false. Plus the concurrent-removal shape if it can be expressed (remove a LATER row during the save — if not expressible, assert `min` via a smaller manifest fixture and say so).
   - `StatementImageIngestTests` still green with the shared derivation.
6. Docs: the two refusal lists + AREA.md:103 (rule 10).

**Commit:** `fix(statements): rolling back an unused statement refuses when its well holds a picture, and puts the row back safely when the save fails (#35)`

## Task 2 — the register row, and `repoint`'s hash-length blind spot

**Files:** `register/reconciliation/Inbox.filings.json` (M8-IN-004), `register/scripts/flip-claim.py`.

1. M8-IN-004: `fix.commit` → `"a9cbba32"`, `fix.date` → `"2026-08-12"`, the two-halves narrative → `fix.note` ("card half 6955c2d8 (2026-08-09); asset half a9cbba32 (2026-08-12, #29 S7) completed the claim"). Run `python3 register/scripts/flip-claim.py recompute --module Inbox`; confirm `_summary` unchanged (the row was already COMPLIES) and the file's formatting matches the script's (`indent=1`, trailing newline). Then run whatever generator RECONCILE.md / the script docstring pairs with `recompute` (`23-generate-rulings.py`, `27-generate-state.py` — read them; run only what reads filings) and commit their output if it changed.
2. `repoint`: match on the stored hash's own length — compare `a.old` against each cited hash by the SHORTER prefix (`old.hasPrefix(cited) || cited.hasPrefix(old)` shape), so a full sha repoints an 8-char citation. Add a doctest-style check or a tiny `--dry-run` assertion if the script has a test harness (read `register/scripts/` for one); else demonstrate with a dry run in the report.
3. Grep every module's filings for other prose `fix.commit`/`fix.date` values (`python3 -c` over the JSON, or `grep -n '"commit": "[^"]*[ (]'`) and fix any found the same way; list them.

**Commit:** `chore(register): M8-IN-004 cites one hash and one date; repoint matches the hash length filings store (#35)`
