# Two loops P3 — the polish and the docs

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** close the two-loops milestone. The File menu names each persona's own verb (Author: *Check Writing* / *Reread*; Review: *Run Round* / *Fresh Eyes*), the two Claude-facing skills describe the check/round split and the first reader, the branch's inherited warnings and the one literal catalogue count are gone, and the roadmap flips the milestone •→✓ with everything the spec still leaves open named as a carry rather than implied as shipped.

**Architecture:** the menu learns the key window's persona the way it already learns whether the canvas selection is promotable — a `FocusedValueKey` published by `ProjectWindow` with `.focusedSceneValue` and read by a small `View` in the File menu (`FocusedPromoteButton`'s shape, `MaughamApp.swift:~505`). The title is a pure function of `Persona?` so the four cells are pinned without a window; the EVENTS the buttons post do not change (`MaughamEvent.postCompilerRun()`/`postCompilerFreshEyes()` — `CompilerRunModifier` still mints the `RunKind` from the persona, ADR 0031), so nothing about which loop runs moves — only what the writer reads before pressing. Everything else in P3 is prose, a skill, and hygiene.

**Tech Stack:** Swift/SwiftUI; XCTest via `./scripts/test.sh` (no Core changes expected); tripwire 33 on every new test.

**Spec:** `docs/superpowers/specs/2026-09-05-two-loops-two-readers-design.md` §7 (P3's list) and §8; P1/P2 handoff carries in `docs/superpowers/notes/2026-09-05-two-loops-handoff.md`. **Inventory (2026-09-06):** the supersession notes in both prior specs are already written; `register/` and the four guides carry no stale pre-two-loops vocabulary; `docs/skills/editing-pass/SKILL.md` is stale by omission (it never described the split), not by error. So P3 is four tasks, not the spec's longer list.

**Branch:** `claude/two-loops-p3-2026-09-06` off local `main` after P2 is merged.

## Global constraints

1. **The menu title is the only thing that changes about ⌘R/⌘⇧R.** The two buttons still post `.maughamRunCompiler`/`.maughamFreshEyesCompiler` to `.keyWindow`; `CompilerRunModifier` still mints the kind from the persona; `PersonaKeyspaceTests`/`DocSyncTests` still see ⌘R/⌘⇧R bound once. A test that a Review-titled *Run Round* press reaches the harness as `.round` already exists (P1 Task 1) — do not duplicate it; pin the TITLE.
2. **Titles, verbatim.** `Persona.author`, `.plan`, `.publish` and `nil` (no key project window) → **"Check Writing"** / **"Reread"**. `.review` → **"Run Round"** / **"Fresh Eyes"**. `nil` takes Author's wording because ⌘R with no project window does nothing and Author's is the default persona (`Persona.default`).
3. **Not P3's, and not built here (say so in the docs):** a Review surface for a round's conformance strains (spec carry #1 — Denver to rule between "the cockpit's letter disclosure grows a Conformance part" and "a round's strains mint as annotations"); `retireSession()` on a kind change (carry #6 — only if a round after a long check session reads "tired"); a first reader per piece; personality dials; a Review-side unread badge; the book letter.
4. **Rule 10 doc sweep travels with the code**; Task 4 is the full pass. Guides describe what ships.
5. **Nothing under `Maugham.xcodeproj/`, `.agents/`, `.codex/`, `AGENTS.md`, `.superpowers/` is committed.** `./gen.sh` after adding any file.

---

## Task 1 — the File menu names the persona's own verb

**Files:** `Maugham/MaughamApp.swift` (`:~154-167` — the two `Button("Check Writing")`/`Button("Fresh Eyes")` items become one `FocusedRunButtons` view (`FocusedPromoteButton`'s shape at `:~505-517`): `@FocusedValue(\.persona) private var persona`, two buttons whose titles are `RunMenuTitles.check(for: persona)`/`.cold(for: persona)`, same two shortcuts, same two event posts; the comment block above them (`:~152-163`) rewritten — it still says "The same trigger read cold … Beside Check Writing because it is the same act asked for differently", which two-loops made false: it is two verbs); new `struct FocusedPersonaKey: FocusedValueKey { typealias Value = Persona }` + `extension FocusedValues { var persona: Persona? }` beside `FocusedProjectURLKey` `:~488`; new `Maugham/Models/RunMenuTitles.swift` (`enum RunMenuTitles { static func check(for: Persona?) -> String; static func cold(for: Persona?) -> String }` — exhaustive `switch` over `Persona?`, so a fifth persona is a compile error here); `Maugham/Views/ProjectWindow.swift` (`.focusedSceneValue(\.persona, persona)` beside `.focusedSceneValue(\.canvasPromotable, …)` `:~5134` — `persona` is the window's `@State` `:196`); `docs/guide/compiler.md` (`:~6-7` "Press **⌘R** — *Check Writing* —" stays Author's; `:~413` "**⌘⇧R** — *Fresh Eyes* — and the **Reread** button" → *Reread* is the menu's word in Author now; one sentence: in Review the same keys read *Run Round* and *Fresh Eyes*); `docs/guide/review-passes.md` (the Rounds and Fresh Eyes section names the File menu's Review titles); `docs/guide/right-pane.md:~182` and `docs/guide/reference.md:~15-16` (the shortcut table: `⌘R` — Check Writing in Author, Run Round in Review; `⌘⇧R` — Reread in Author, Fresh Eyes in Review); `Maugham/Views/KeyboardShortcuts.swift` (the in-app cheatsheet, if it lists ⌘R/⌘⇧R — `DocSyncTests.test_paneShortcutsDocumentedInTheInAppCheatsheet` reads it); `CLAUDE.md` (the Compiler cell's parenthetical "the File menu still titles both "Fresh Eyes" (the per-persona rename is P3's)" → done); tests `MaughamTests/RunMenuTitlesTests.swift` (new — `./gen.sh`; the four `Persona` cells + nil for both functions; `Persona.allCases` asserted so a fifth persona fails here too), `MaughamTests/PersonaKeyspaceTests.swift` (green by construction — `⌘R`/`⌘⇧R` are still bound once in the menu file; if its source scan keys on the literal `Button("Check Writing")`, re-point it at `RunMenuTitles`), `MaughamTests/DocSyncTests.swift` (green after the doc edits; if the shortcut census wants the exact title string, the reference table carries BOTH titles per key).

**Requirements:** the two events posted are byte-identical to today (pin by a source census on `MaughamApp.swift`: `postCompilerRun()` and `postCompilerFreshEyes()` each appear exactly once); the `FocusedRunButtons` view has no `.disabled` (a check with no project window is already a no-op at the receiver); the mounted-window premise is NOT tested (a focused-value round trip needs a key window — tripwire 33's spirit; the pure titles are the pin). `./scripts/test.sh`.

Commit: `feat(menu): the File menu names each persona's own verb — Check Writing / Reread in Author, Run Round / Fresh Eyes in Review`.

## Task 2 — the two skills describe the split

**Files:** `docs/skills/editing-pass/SKILL.md` (a short new section — **Two loops** — before the pass doctrine: Author's ⌘R is a *check* (the coach's or the first reader's, unstamped notes, no round, `read_first_reader` for who she is), Review's Run Round is a *round* (a stage editor's, numbered, pass-stamped); when Claude is asked to review in a pass's voice it is acting as a ROUND's editor and should read the whole piece, not a diff; when asked to respond as a reader it is a CHECK's reader and reports what happened in it, never a fix — one paragraph each, no restatement of the guides, cross-reference `docs/guide/compiler.md#who-reads-a-check` and `review-passes.md#rounds-and-fresh-eyes`); `docs/skills/maugham-bootstrap/SKILL.md` (`:~21-27` already names `read_lessons` and `read_first_reader`; add the one clause that a piece's `read_craft_intent` and the first reader's statement are read by DIFFERENT readers — the lessons ledger is never briefed to the first reader — so a Claude playing her must not cite it); `Maugham/MCP/AREA.md` (the SEP-2640 skills paragraph, if it summarises what each skill teaches). Tests: `MaughamTests/SkillsExtensionTests` or whichever suite serves the bundled skills (grep `editing-pass` under `MaughamTests`) — green; a skill file is served verbatim, so a census may pin a heading — run it.

**Requirements:** every sentence checked by grep against the built code (the skill is what a Claude READS before acting — a false claim here is a wrong action, not a stale doc). `./scripts/test.sh`.

Commit: `docs(skills): the editing-pass and bootstrap skills know there are two loops and two kinds of reader`.

## Task 3 — hygiene the branch inherited

**Files:** `Maugham/Stores/DocumentStore.swift:~821` (`case .inbox(let kind …)` — unused binding → `_`); `Maugham/Views/StatementPane.swift:~466, ~479` (`error as? CustomStringConvertible` always succeeds — `(error as CustomStringConvertible).description` or `String(describing: error)`, whichever the surrounding code's intent is: the message shown for a failed proposal adopt/discard; keep the displayed text identical — pin with the existing `StatementPane` proposal tests); `MaughamTests/MCP/Tools/StatementProposalToolTests.swift:~100` (the `XCTAssertEqual(MCPToolCatalog.all.count, 60)` literal → assert the two proposal tools are IN the catalogue (by name) and drop the count — `DocSyncTests.test_toolCountSyncedAcrossDocsAndCatalog` already guards the count dynamically, so a literal here is a second spelling that goes stale on every new tool); the P1 fixture note: `MaughamTests/Fixtures/legacy-sidecar-*.json` are redacted real shapes — verify a `README` or the test doc comment says so (P1 did; confirm, don't duplicate). Warning census after: `touch` the three files, `xcodebuild … -configuration Debug build`, grep `warning:` for them → zero.

**Requirements:** no behaviour change; the three warnings are the only ones in any two-loops branch file (P2's census). `./scripts/test.sh`.

Commit: `chore: three inherited warnings and one literal catalogue count leave the two-loops files`.

## Task 4 — the roadmap flips, the docs close, the handoff records the carries

**Files:** `docs/roadmap.md` (the two-loops row `:~271`: • → ✓, "P1 merged 2026-09-05, P2 merged 2026-09-06, P3 merged <date>"; the carried-to-P3 clause becomes the milestone's OPEN CARRIES, named: a Review surface for a round's conformance strains (Denver to rule between the two options), `retireSession()` on a kind change, and the spec §8 exclusions; the editorial-letter row's §4.1 clause stays); `docs/problem-map.md` (the first-reader row is ✓ — verify; any row about "feedback while drafting" that should now cite the check/round split); `docs/product.md` (the honest what-is-built overview — one paragraph: Author's check vs Review's round, the coach and the first reader, `read_first_reader`; grep `compiler\|Le Guin\|round` and correct); `docs/constitution.md` (nothing to change unless a falsification condition names the coach as reading unassigned pieces — grep `coach\|unassigned`); `CLAUDE.md` (the Compiler cell: remove "P3's" forward references; the "Releases"/milestone lines if any name two-loops as in progress); `Maugham/Compiler/AREA.md` + `Maugham/Views/AREA.md` (any "P3 will"/"carried to P3" sentence → done or → named carry); the spec's status line (`P3 built <date>; milestone COMPLETE; open carries: …`); `docs/adr/0031-…md` (a one-line dated note under Consequences that the menu titles now follow the persona — the last of the mode-error patches gone); `docs/superpowers/notes/2026-09-05-two-loops-handoff.md` (a **P3 built** section: what landed, the rulings, and a **Denver decisions owed** list: (1) the round-strains surface — the two options with a recommendation; (2) whether to `retireSession()` on a kind change; (3) whether to tag — the milestone is whole once he smokes P2+P3; the three-plan smoke consolidated into one script). Tests: `DocSyncTests`, `TripwireGrepTests`, `PersonaKeyspaceTests`.

- [ ] Whole-branch review (small diff; a mid-tier reviewer suffices) — the focused risk is the FocusedValue: does `ProjectWindow` publish it on the same view the canvas key is published on, so both windows keep independent titles; and are the docs' shortcut tables consistent with the menu (DocSync).
- [ ] `./scripts/test.sh full`; a Release build (`ProjectWindow.body` gained one modifier); warning census on the branch files → zero.
- [ ] Merge to local `main` unpushed; Denver smokes P2 + P3 together (the consolidated script in the handoff); then push and decide the tag.

**Smoke (Denver, P3's part):** Author → File menu reads *Check Writing* / *Reread*; ⌘2 → Review → the same menu reads *Run Round* / *Fresh Eyes*; ⌘1 back → Author's titles return; a second project window in Review beside an Author window: each window's menu reads its own persona when it is key. Claude Desktop: ask for a pass-voiced review of a chapter and confirm the skill has it read the whole piece as a round's editor; ask for a first-reader response and confirm it never proposes a fix.

Commit: `docs: two loops COMPLETE — the roadmap flips, the carries are named, the handoff records P3`.
