# Issue #42 — compiler review loop: `latestRound`'s contract (R1) and the cross-lane re-raise on the since-line (F-H)

**Issue:** https://github.com/dtrouton/Maugham/issues/42 — Denver's ruling on F-H: **surface** cross-lane matches in the since-line, don't merely document the under-count.
**Spec authority:** `Maugham/Compiler/AREA.md` (round ring, the shadow, the dedupe); ADR 0029 (the report is materialized; the mint is Maugham's); the constitution's honesty principle (the line the writer reads first must not under-report what the round engaged). `SinceLastRound` stays pure and store-free (`RoundHistory.swift:179-183`).
**Branch:** `worktree-issue-42-since-line`, off main `d4504929`.

## Findings, re-verified 2026-08-27 (R1 corrected)

- **R1 — reproduces, but the fix as filed would be a regression.** `DiagnosticsStore.latestRound` (`Maugham/Compiler/DiagnosticsStore.swift:437-444`) reads `byDoc` directly; `standingRound` (`:422-425`) and `replace` (`:211-213`) read `finishedContent` (the preview shadow, `:124-126`). The sweep's "no live trigger" is stale: **the Review cockpit strip calls it mid-preview** (`AnnotationsPane.swift:632-636`, `cockpitRound`, observing `diagnostics.version`, which every arriving section bumps at `:280`). Today that returns N — the round in flight — so the strip reads "Line · Lish · round N" while round N streams, and `runHelp`'s tooltip (`ReviewRoundCockpit.swift:269-279`) names N+1 as the next press, which is right. Through `finishedContent` both would be one behind during the run. The mint (`CompilerOrchestrator.swift:598`) is guarded by `!isRunning` (`:489`, `:430-434`). **Ruling: `latestRound` keeps reading the standing content — it answers "which round is this lane on, including one in flight" — and that contract gets a doc comment and a pinning test; the N+1 hazard is closed by the mint's guard, which also gets pinned.** `AREA.md:768-769` is false ("the one reader both `beginRun`'s minting and the pane's `sinceLastRoundLine` go through" — `sinceLastRoundLine` never calls it; the cockpit does).
- **F-H — reproduces exactly.** The mint dedupes against the whole open compiler queue with no lane filter (`CompilerEnvironment+Project.swift:262-269`) and records nothing about a match (`continue`, `minted` untouched, returns `Int`). `SinceLastRound.compute` filters `reviewPassId == passId` (`RoundHistory.swift:237`). Repro: Structural round 1 mints X; Line round 1 then round 2 re-raise X → nothing minted, X counted nowhere for L; L round 2's line reads "Since round 1: 0 resolved · 0 persisting · 0 new". Author's This-check and the unread badge also miss it. `Outcome.new`'s doc comment (`RoundHistory.swift:208-213`, "a re-raise … is counted once, as persisting") is false cross-lane; `docs/guide/compiler.md:130-137`'s bold sentence over-claims the same way. `compute` cannot derive the count from what it receives; the mint must hand it over. `CompilerRun` (`Diagnostic.swift:79-153`) has a hand-written additive decoder (`:196-199`); `sinceLastRoundLine` (`RoundNarrative.swift:86-102`, the ONE assembler) already receives `run: CompilerRun?`. Scope: the mint sees only `outcome.mintable` (questions + reader reports), never conformance strains — the count is of NOTES.

## Global constraints

1. **Every existing since-line assertion stays green untouched** (`DiagnosticsPaneTests:1193-1329`, `CompilerRunCommandTests:4677-4731`, `:1545`): the sentence is byte-identical when the cross-lane count is 0 — the suffix appears only when N > 0.
2. `SinceLastRound.compute`'s signature and purity are unchanged; the count rides `CompilerRun`, additive-optional, and the assembler reads it beside `outcome`.
3. The count is produced at the ONE place the dedupe happens; no second fingerprint scan.
4. Singular/plural forms live in one place beside each other.
5. R1: no behaviour change to `latestRound`; the contract is stated where the function is and pinned by a test that calls it WHILE a preview stands.
6. Rule 10: `RoundHistory.swift:208-213`, `Compiler/AREA.md:768-769` (+ its dedupe section), `docs/guide/compiler.md:130-137`, `docs/guide/review-passes.md:102-107`, the `CompilerRun` field's doc comment — corrected in the task that changes them; no prose counts.
7. Tripwires 8/13/23; `./scripts/test.sh` per task; full gate before merge.

## Task 1 — R1: `latestRound` says what it answers, and a test asks it mid-preview

**Files:** `Maugham/Compiler/DiagnosticsStore.swift` (`latestRound` doc comment); `Maugham/Compiler/AREA.md:768-769`; tests `MaughamTests/RoundHistoryTests.swift` (MARK "The round-numbering read", `:612-663`; preview fixtures `:545-582`).

1. Doc comment on `latestRound`: it deliberately reads the STANDING content, not the shadow — it answers "which round is this lane on, the one in flight included", which is what the Review cockpit strip asks every section (`AnnotationsPane.cockpitRound`); the mint asks it only under `!isRunning` (`CompilerOrchestrator.runRequested`), which is what keeps a run from numbering itself against itself — name `standingRound` as the reader that DOES go through the shadow and why the two differ. No code change.
2. Tests: `test_latestRound_answersTheRoundInFlightWhileAPreviewStands` — `replace` a Line round 1, `preview` a Line round 2 (no `replace`), `latestRound(forPass: "line")` == 2 while `standingRound` == 1; `discardPreview` → both 1. And `test_theMintNeverAsksLatestRoundWhileARunIsStanding` — pin the guard: with a run in flight, `runRequested` (or whatever `beginRun`'s entry is) refuses before minting (find the existing `isRunning` refusal test and extend, or assert on the orchestrator's state; say which).
3. `AREA.md:768-769`: the true reader pair is the mint (guarded) and the Review cockpit (mid-preview by design); `sinceLastRoundLine` reads `run.round` and the history.

**Commit:** `docs(review): latestRound answers the round in flight — stated, and pinned mid-preview (#42 R1)`

## Task 2 — F-H: a finding already open in another lane is counted on the since-line

**Files:** `Maugham/Compiler/CompilerOrchestrator.swift` (`mintAnnotations` closure type `:294`; `finish` `:1119-1144` where `minted` flows into `record(… mintedNotes:)`), `Maugham/Compiler/CompilerEnvironment+Project.swift:239-317`, `Maugham/Compiler/Diagnostic.swift` (`CompilerRun` + decoder `:196-199`), `Maugham/Compiler/RoundHistory.swift:201-213`, `Maugham/Compiler/RoundNarrative.swift:86-102`, `Maugham/Compiler/AREA.md`, `docs/guide/compiler.md:130-137`, `docs/guide/review-passes.md:102-107`. Tests: `MaughamTests/CompilerRunCommandTests.swift` (two-lane end-to-end — `setActivePass` at `:4685`; `test_aRepeatedQuestionMintsNothingASecondTime` `:4346-4370` as the one-lane control), `MaughamTests/DiagnosticsPaneTests.swift` (`sinceLastRoundLine` unit tests `:1193-1288`), `MaughamTests/RoundHistoryTests.swift`, any `CompilerRun` codec test.

1. The mint returns a small value instead of `Int`: `struct MintOutcome: Equatable, Sendable { let minted: Int; let openInOtherLanes: Int }` (the second: findings this round raised whose fingerprint matched an open compiler note whose `reviewPassId` differs from this run's — a match in the SAME lane is the "persisting" case and is not counted here). Compute it at `:262-269` — the `taken` set becomes a `[fingerprint: reviewPassId?]` map so the branch knows the matched note's lane; no second scan. Every caller of the closure adapts (the test-side stubs too — grep `mintAnnotations:` in `MaughamTests`).
2. `CompilerRun` gains `openInOtherLanes: Int?` beside `mintedNotes`, additive-optional through the hand-written decoder (an older sidecar decodes to nil); `finish` records it.
3. `RoundNarrative.sinceLastRoundLine`: when `run?.openInOtherLanes ?? 0 > 0`, append ` · 1 also open in another lane` / ` · N also open in other lanes` (one pluralisation pair, next to each other, in `RoundNarrative`). Zero → the string is unchanged (GC1).
4. Tests (TDD):
   - `CompilerRunCommandTests.test_aFindingRaisedAgainInAnotherLaneIsCountedOnTheSinceLine` — Structural round 1 raises X (minted); switch to Line; Line round 1 raises X (deduped, nothing minted, `run.openInOtherLanes == 1`); Line round 2 raises X again → the cockpit/pane line reads exactly `"Since round 1: 0 resolved \u{00b7} 0 persisting \u{00b7} 0 new \u{00b7} 1 also open in another lane"`; X is still exactly one note, in lane S.
   - The one-lane control: `test_aRepeatedQuestionMintsNothingASecondTime` gains `openInOtherLanes == 0` and its since-line unchanged (a same-lane re-raise is `persisting`, not this).
   - `RoundNarrative`/`DiagnosticsPaneTests`: unit tests for the suffix — 0 → no suffix (byte-identical to today's asserted strings), 1 → singular, 2 → plural.
   - `CompilerRun` codec: a run encoded without the field decodes with nil; round-trips with a value.
5. Docs: `RoundHistory.swift:208-213` (`Outcome.new`'s comment — a re-raise in the SAME lane is persisting; in ANOTHER lane it is counted on the line as "also open in other lanes", from the mint's own record); `AREA.md`'s dedupe paragraph + the `:768` fix from Task 1 stays; `docs/guide/compiler.md:130-137` — the bold sentence gains "…and if that note lives in another pass's lane, the line says so: '1 also open in another lane'"; `docs/guide/review-passes.md:102-107` one clause. State the scope honestly in AREA.md: the count is of notes (questions/reader reports); a conformance strain re-raised across lanes is report-side and not counted.

**Commit:** `feat(review): the since-line counts a finding already open in another lane (#42 F-H)`
