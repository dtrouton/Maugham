# M1A — The Spine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make intent and visual language one op-logged artifact — freeform prose with history, at project and per-document scope — and give visual language a reader in the same milestone.

**Architecture:** A `Statement` is registered in a new `ProjectManifest.statements` section and carried as an ordinary `Document`, so history, undo, merge and conflict handling arrive built rather than invented. It is edited in two right-hand panes that mount `EditorSurface` directly. Existing craft-intent research notes are adopted once, on open, through the op log's sanctioned bootstrap path.

**Tech Stack:** Swift 6, SwiftUI + AppKit, MaughamCore (local SPM, Apple frameworks only), TextKit 2, XCTest.

**Spec:** `docs/superpowers/specs/2026-07-31-m1a-spine-design.md`. The spec is authoritative; where this plan and the spec disagree, the spec wins and the plan is wrong.

---

## How this plan is written, and why it looks different

**This plan carries contracts, symptoms and verified signatures — never function bodies.** That is a project rule (CLAUDE.md, `memory/feedback_plan_code_is_a_liability.md`) and it overrides the generic plan-writing convention of pasting implementation and test code inline. The reason is measured: on M1C, plan-supplied function bodies became the shipped defect, and four separate implementers had to correct plan text written against an API that did not exist yet.

So each task gives you:

- the **files**, with `file:line` for every existing thing you must touch;
- the **contract** — what must be true when you are done;
- the **test's subject and its falsification** — what the test asserts, and what change to production code must make it fail. Name the test what the task names it; the name is how the next task's reviewer finds it.
- the **signatures you will consume**, quoted only where they were read out of the tree on 2026-07-31 and cited.

**You write the test body and the implementation.** If a contract here is wrong, or a signature has drifted, **say so and stop** — refusing a stated ruling you can falsify is the standard here, not insubordination. Two implementers did exactly this on the previous slice and both were right.

**Tasks 7–10 were deliberately requirements-only, and Task 6 re-derived them on 2026-08-01** against the code tasks 1–5 actually built. They are now **Tasks 7–12** (the old Task 9 carried four unrelated things and was split). Everything below the "HALF TWO" heading is in the same shape as tasks 1–5 and is safe to implement; the places where the requirements-only text turned out to be wrong are marked **WAS WRONG** in the task that inherited them, rather than silently corrected.

---

## Global Constraints

Copied from CLAUDE.md and the spec. Every task's requirements implicitly include this section.

- **The op log is the source of truth.** `.md` on disk is derived and anchor-free (ADR 0019). External `.md` edits are discarded, never ingested.
- **`Bootstrap.run` must be called from any new manuscript load path.** The contract surface is `Document.load`; route new load paths through it and construct `Document` no other way. `BootstrapWiringTests` enforces this.
- **Plain text on disk.** Manuscript-and-statement content lives in the open at the writer's paths; only derived state goes under `.maugham/`.
- **MCP never mutates manuscript text.** This slice adds no write tool of any kind.
- **Move/delete of user-editable content goes through the typed `DocumentStore` mover** — `relocate` / `relocateUserContent` / `trash` (tripwire 14, grep-enforced by `TripwireGrepTests.test_noRawMoveOfUserContentOutsideTypedMover`).
- **Anything touched by both Mac and phone goes through a shared `MaughamCore` implementation** (tripwire 19). The phone must not reimplement what the Mac implements.
- **No parallel observable state on `EditorHost`; no 4th caller of `EditorSurface.applyExternalText`** (tripwires 6, 7).
- **No bare `ParagraphID.mint()` in production** — use `mintUnique(excluding:)` (tripwire 23).
- **4-char alphabet-restricted paragraph ids in tests crossing the `.md` ↔ op log boundary** — `[0-9a-hjkmnp-tv-z]` (tripwire 8).
- **No prose counts of list members.** Name the members, or point at the array. A number in prose about a list is a defect when written (`memory/feedback_prose_counts_are_unmaintainable.md`).
- **`Maugham.xcodeproj/` is generated.** Run `./gen.sh` after adding any file; never hand-edit `project.pbxproj`; never commit anything under `Maugham.xcodeproj/`.
- **macOS 14 floor, iOS 17 floor.** Both stay for this milestone.

### Build and test commands

```bash
./gen.sh                                    # after ANY new file. Before ANY count you quote.

xcodebuild -project Maugham.xcodeproj -scheme Maugham test CODE_SIGNING_ALLOWED=NO

xcodebuild -project Maugham.xcodeproj -scheme MaughamPhone \
  -destination 'platform=iOS Simulator,name=iPhone 17' test CODE_SIGNING_ALLOWED=NO

cd Packages/MaughamCore && swift test        # MaughamCore's OWN tests — see below
```

Narrow the two `xcodebuild` runs with `-only-testing:MaughamTests/SuiteName`. **Suite paths are flat.** A folder-shaped path runs zero tests and exits 0 — which reads exactly like green.

**A third command, corrected during Task 1 and verified against `project.yml`:** the `Maugham` scheme's test targets are `[MaughamTests]` only (`project.yml:213-219`), and the generated `MaughamCore` scheme has no test action. So the two `xcodebuild` commands **build** MaughamCore but never **run** `MaughamCoreTests`. A MaughamCore test suite is reached only through SPM:

```bash
cd Packages/MaughamCore && swift test --filter SuiteName
```

This does not silently pass — naming a suite `xcodebuild` cannot see errors loudly rather than reporting zero-tests-green. CLAUDE.md's build-flow section describes the `Maugham` scheme as covering "Mac app + MaughamCore", which is true of the build and not of the tests; that drift is recorded for the milestone's doc sweep.

**A MaughamCore change still owes all three runs** — the two schemes for the surfaces that consume it, and `swift test` for its own suites.

For a full Mac run, add `-skip-testing:MaughamTests/MCPServerLifecycleTests`: three MCP tests depend on wall-clock progress and fail under a loaded suite while passing in isolation. Apply that discriminator before believing a red run is about your branch. See `docs/superpowers/notes/2026-07-29-mcp-clock-dependent-tests.md`.

---

## File structure

**New, in `Packages/MaughamCore/Sources/MaughamCore/`:**

| File | Responsibility |
|---|---|
| `Statement.swift` | The `Statement` value type, its `Kind` and `Scope`, and their ADR-0015 decoders. Nothing else. |
| `StatementLookup.swift` | Pure scope→statement lookups, shared by Mac and phone. Mirrors `PaletteLookup`'s shape and its no-stamping discipline. |

**New, in `Maugham/`:**

| File | Responsibility |
|---|---|
| `Stores/ProjectStore+Statements.swift` | Find-or-create by scope, path minting, manifest registration. Replaces `ProjectStore+CraftIntent.swift`. |
| `Stores/ProjectStore+StatementAdoption.swift` | The one-time, schema-gated adoption of legacy craft-intent notes. **Built as a `ProjectStore` extension, not the free-standing `Stores/StatementAdoption.swift` this table originally named** — it needs `createStatement`, `deleteResearchItems` and the manifest. |
| `Views/StatementEditorHost.swift` | Owns a statement's `Document` and mounts `EditorSurface` with the single sanctioned binding. |
| `Views/StatementPane.swift` | The pane chrome for both `.intent` and `.visualLanguage`: scope resolution, the empty state, the switch to project scope. |
| `MCP/Tools/VisualLanguageTools.swift` | `read_visual_language` (Task 9). |

**Modified (line numbers re-verified 2026-08-01 at `5950c84`, Task 6).** Half
one's rows are as built; half two's are as re-derived.

| File | Change |
|---|---|
| `Packages/MaughamCore/Sources/MaughamCore/ProjectManifest.swift:29` | `currentSchemaVersion` 3 → 4; add `statements`; extend the memberwise `init` at `:122`. |
| `Maugham/OpLog/Document+Load.swift:307` | `resolveDocId` learns `statements` after `structure`. |
| `Maugham/Models/DetailSegment.swift:18-19,41-42,57-58` | Two cases, two icons, two help strings. |
| `Maugham/Models/Persona.swift:88-91,94-100` | Two registry entries; edit down the reserved-for-later comment. `.references` stays reserved. |
| `Maugham/MaughamApp.swift:236-242` | Two View-menu items, ⌘⌥N and ⌘⌥V. |
| `docs/guide/reference.md:37-38` | The two shortcuts (guarded by `DocSyncTests`). |
| `Maugham/Canvas/PromotionPerformer.swift:498-530` | Task 7 — retarget the intent arm; the defect comment is `:499-511`, not `:503-512`. |
| `Maugham/Canvas/Promotion.swift:339-361,1243-1263` | Task 7 — `ArtifactIndex` and the destination sentence. **Not in the original table.** |
| `Maugham/Views/InspectorView.swift:109-125`, `Views/PieceInspector.swift:107-126` | Task 8 — the two inspector arms. **Not in the original table; they were unlisted consumers of the deleted seam.** |
| `Maugham/MCP/Tools/CraftIntentTools.swift` | Task 8 — `read_craft_intent` widens to any document scope, and derives from the op log. |
| `Maugham/Stores/ProjectStore+CollectionPieces.swift:334-355` | Task 8 — decide whether a promoted piece carries its intent. **Not in the original table.** |
| `Maugham/MCP/MCPTool.swift:36-91` | Task 9 — register `read_visual_language` in `MCPToolCatalog.all` (the array, whose last entry is `:90` — the original table's `:82` is `ReadCraftIntentTool`). |
| `docs/skills/maugham-bootstrap/SKILL.md` | Task 9 — the visual-language section. |
| `Maugham/MCP/Tools/CanvasTools.swift:86-144` | Task 10 — `list_canvas` reports the reference projection. |
| `Maugham/Canvas/RegionBinding.swift:3-11` | Task 10 — correct the §4.4 citation and the "consumed in 1A" claim. |
| `MaughamPhone/Read/BinderView.swift:27-33,103-115`, `Read/PaletteLoading.swift:30-34` | Task 11 — read statements first, legacy note as fallback. |
| `Maugham/Editor/ImagePasteHandler.swift:31-40` | Task 12 — validate what the saver is handed. **`Editor/`, not `Stores/`, which is where this table said it was.** |
| `Maugham/Canvas/CanvasDrop.swift:368-410` | Task 12 — `isIngestableImage` is the canvas's check, and it is here rather than in `DropClassification`. |
| `MaughamTests/TripwireGrepTests.swift:2114-2118` | Task 12 — the image-saver caller census. |

**Deleted:** `Maugham/Stores/ProjectStore+CraftIntent.swift` (**Task 8**, once
every consumer has moved — Task 7 moves promotion, Task 8 moves the two
inspectors and `read_craft_intent`).

---

# HALF ONE — build this first

## Task 1: The `Statement` type and the manifest section

**Files:**
- Create: `Packages/MaughamCore/Sources/MaughamCore/Statement.swift`
- Modify: `Packages/MaughamCore/Sources/MaughamCore/ProjectManifest.swift:29` (`currentSchemaVersion`), `:72-82` (stored properties), `:122-135` (memberwise init)
- Test: `Packages/MaughamCore/Tests/MaughamCoreTests/StatementTests.swift`

**Interfaces:**
- Consumes: nothing.
- Produces: `Statement` with `id: String`, `kind: Statement.Kind`, `scope: Statement.Scope`, `path: String`. `Kind` is `.intent | .visualLanguage | .unknown(String)`. `Scope` is `.project | .document(String) | .unknown(String)`. `ProjectManifest.statements: [Statement]`. Every later task depends on these exact names.

**Contract:**

1. `ProjectManifest.currentSchemaVersion == 4`.
2. `statements` is a non-optional `[Statement]` on the manifest, defaulting to `[]` in the memberwise init so no existing construction site breaks. It must **decode as empty when the key is absent** — a schema-3 manifest has no such key and must still open.
3. `Kind` and `Scope` are **lossless** on unknown values, following `ResearchRole` (`ResearchItem.swift:18-51`) and *not* the lossy `ItemType`/`AssetKind` shape. Read that type's doc comment before writing yours; it explains why identity-bearing enums preserve the raw string and benign ones don't. A statement is the writer's prose — there is something to preserve on behalf of a newer build.
4. `Scope` carries an associated value, so like `ResearchRole` it cannot be a `String`-raw enum and the `Codable` conformance is hand-written. On-disk shape for a document scope is your choice; state it in the type's doc comment and pin it with a round-trip test.

**Why the bump is not ceremony** (put this in the doc comment, because the next person will ask): `statements` decodes as absent-tolerant, so an older build would read the project happily and then re-save the manifest **without the section**, destroying the registry that points at the writer's intent files. `decodeGuardingSchema` (`ProjectManifest.swift:112`) is what prevents that.

- [ ] **Step 1: Write the failing tests**

In `StatementTests.swift`:

- `test_anUnknownKindSurvivesARoundTrip` — encode a manifest containing a statement whose `kind` raw string this build does not know, decode, re-encode, and assert the raw string is **byte-identical**. Falsified by making `Kind` a plain `String`-raw enum with a `?? .intent` fallback.
- `test_anUnknownScopeSurvivesARoundTrip` — the same for `Scope`.
- `test_aSchemaThreeManifestWithNoStatementsKeyStillDecodes` — decode a manifest JSON fixture with **no `statements` key at all** and assert `statements == []` and no throw. Falsified by making `statements` non-optional without a decoding default.
- `test_aSchemaFourManifestIsRefusedByAThreeSupportingBuild` — call `decodeGuardingSchema` on a fixture stamped `"schemaVersion": 5` and assert `SchemaTooNewError(found: 5, supported: 4)`. This is the paired-release guarantee; assert the numbers, not just that it threw.

**Control:** at least one test in this file must pass against unmodified code, so a green run cannot mean "the file never compiled into the target."

- [ ] **Step 2: Run them and watch them fail**

```bash
./gen.sh
cd Packages/MaughamCore && swift test --filter StatementTests
```

**Corrected during Task 1.** An earlier draft of this step routed through `xcodebuild -scheme Maugham -only-testing:MaughamCoreTests/…`, which does not work: `MaughamCoreTests` is not among that scheme's test targets (`project.yml:213-219`) and the generated `MaughamCore` scheme has no test action. MaughamCore's own suites are reachable only through SPM. See Global Constraints.

Expected: compile failure on `Statement` being undefined. That is a legitimate red.

- [ ] **Step 3: Implement `Statement.swift` and extend the manifest**

- [ ] **Step 4: Run the whole MaughamCore surface**

Both schemes. A MaughamCore change must be tested against both — they are independent.

- [ ] **Step 5: Commit**

```bash
git add Packages/MaughamCore/Sources/MaughamCore/Statement.swift \
        Packages/MaughamCore/Sources/MaughamCore/ProjectManifest.swift \
        Packages/MaughamCore/Tests/MaughamCoreTests/StatementTests.swift
git commit -m "feat(core): Statement type and the manifest statements section at schema 4"
```

---

## Task 2: `resolveDocId` learns statements

**Files:**
- Modify: `Maugham/OpLog/Document+Load.swift:307-339`
- Test: `MaughamTests/StatementDocIdTests.swift`

**Interfaces:**
- Consumes: `ProjectManifest.statements`, `Statement.path`, `Statement.id` (Task 1).
- Produces: nothing new. This changes an existing internal function's behaviour only.

**Contract:**

`resolveDocId(for:)` currently matches the URL's project-relative path against `manifest.structure` via `findItemByPath`, and falls through to `"doc-\(StableHash.fnv1a64Hex(relativePath))"` when nothing matches (`:314-329`). It gains a **`statements` lookup after the `structure` lookup and before the hash fallback**, returning the statement's manifest `id`.

**The symptom this prevents, stated so you can reproduce it:** without this, a statement's op log is keyed on a hash of its path. Rename the document a statement belongs to, let the path change, and the statement's entire history is orphaned — the file re-bootstraps as a fresh document with no past. This is tripwire 22, which has now been hit twice in this codebase.

**Scope discipline:** this is the whole of the milestone's contact with `Maugham/OpLog/`. That area is the cleanest in the codebase and `Maugham/OpLog/AREA.md` says not to refactor it structurally. One lookup, in one function. If you find yourself editing a second file under `OpLog/`, stop and report why.

- [ ] **Step 1: Write the failing test**

`test_aStatementKeepsItsHistoryAcrossAPathChange` — build a project fixture with a statement registered in the manifest, load it as a `Document`, append an op, change the statement's `path` in the manifest, load again, and assert the derived text still carries the op. Falsified by removing the `statements` branch: the second load returns a hash-keyed id, finds no log, and derives empty.

Add `test_anUnregisteredPathStillFallsBackToTheHash` as the control — an unknown path must still return `doc-<hash>`, because test fixtures depend on it (`:335-338` says so).

- [ ] **Step 2: Run it and watch it fail**

```bash
xcodebuild -project Maugham.xcodeproj -scheme Maugham test CODE_SIGNING_ALLOWED=NO \
  -only-testing:MaughamTests/StatementDocIdTests
```

Expected: the round-trip test fails with empty derived text. If it fails some other way, understand why before proceeding — an unexpected red is information.

- [ ] **Step 3: Implement the lookup**

- [ ] **Step 4: Run `MaughamTests/BootstrapWiringTests` and the op-log suites alongside your own**

The load path is shared. A change here that breaks bootstrap wiring must be caught now, not at the whole-branch review.

- [ ] **Step 5: Commit**

---

## Task 3: Find-or-create a statement by scope

**Files:**
- Create: `Maugham/Stores/ProjectStore+Statements.swift`, `Packages/MaughamCore/Sources/MaughamCore/StatementLookup.swift`
- Test: `MaughamTests/StatementStoreTests.swift`
- Read first: `Maugham/Stores/AREA.md`; `Packages/MaughamCore/Sources/MaughamCore/PaletteConvention.swift` (the shape to mirror); `Maugham/Stores/ProjectStore+CraftIntent.swift` (the seam being replaced — **do not delete it in this task**)

**Interfaces:**
- Consumes: `Statement`, `ProjectManifest.statements` (Task 1).
- Produces:
  - `StatementLookup.statement(in:kind:scope:) -> Statement?` — pure, no stamping, no side effects. Mac and phone both call it (tripwire 19).
  - `ProjectStore.statement(kind:scope:) -> Statement?` — the Mac's reading wrapper.
  - `ProjectStore.createStatement(kind:scope:) async throws -> Statement` — idempotent find-or-create.
  - Tasks 5, 7, 8 and 9 all call these by these names.

**Contract:**

1. **Lookup is by `scope`, in the manifest.** There is no path prefix and no filename convention in the read path. This is what kills the defect rather than fixing it — see the spec's §3 and the comment block at `PromotionPerformer.swift:498-512`, which documents the defect from the inside.
2. `createStatement` is **idempotent**: called twice for the same `(kind, scope)` it returns the same statement and creates no second file.
3. Paths are minted per the spec's §2.2 table — `intent.md`, `intent/<slug>.md`, `visual-language.md` — with `<slug>` derived from the document's title **at creation and never re-derived**. Identity is the manifest `id`; the path is free to drift after a rename.
4. **Absence is valid.** `statement(kind:scope:)` returns nil without side effects, mints nothing and logs nothing. `read_craft_intent`'s shipped description already promises Claude that absence is *"a valid, deliberate state — do not invent a standard on their behalf"*; the store must not contradict its own MCP surface.
5. A scope that names a document id not in `manifest.structure` **throws** rather than silently falling back to project scope. The old seam documented this discipline (`ProjectStore+CraftIntent.swift:28-31`) and it is worth keeping.

`StatementLookup` mirrors `PaletteLookup` (`PaletteConvention.swift:15-46`): pure lookups, no stamping, so the Mac can wrap them with whatever healing it needs and the phone can use them read-only.

- [ ] **Step 1: Write the failing tests**

- `test_creatingTwiceForOneScopeReturnsTheSameStatement` — the idempotency contract. Falsified by dropping the find in find-or-create.
- `test_aNovelChapterGetsItsOwnIntent` — **this is the defect's grave.** Create an intent for a novel chapter, then look it up, and assert it is found. Falsified by restoring a path-prefix lookup: `ResearchScope.pieceResearchPrefix` is `guard piece.pieceKind == .loose` (`ResearchScope.swift:139`), so a novel chapter returns nil and the assertion dies.
- `test_lookupOfAnUndeclaredScopeMintsNothing` — assert nil, and assert the manifest and the project directory are byte-unchanged afterwards. Absence must be free.
- `test_anUnknownDocumentScopeThrows` — not nil, not project scope.

- [ ] **Step 2: Run them and watch them fail**

- [ ] **Step 3: Implement**

- [ ] **Step 4: Run both schemes** — `StatementLookup` is MaughamCore and the phone builds it too.

- [ ] **Step 5: Commit**

---

## Task 4: Adopt existing craft-intent notes, once

**Files:**
- Create: `Maugham/Stores/StatementAdoption.swift`
- Modify: `Maugham/Stores/ProjectStore.swift:221-275` (the load path)
- Test: `MaughamTests/StatementAdoptionTests.swift`

**Interfaces:**
- Consumes: `ProjectStore.createStatement(kind:scope:)` (Task 3); `Document.load` (unchanged); `DocumentStore.relocateUserContent(affectedPaths:perform:)` — verified at `DocumentStore.swift:586`.
- Produces: the adoption entry point, called from `ProjectStore.load`.

**Contract:**

1. **The gate is the on-disk schema version.** Adoption runs when the manifest reads `schemaVersion < 4`; the post-adoption save writes 4. Gating on "has no `statements` section" instead would re-scan forever a writer who legitimately has no intent.
2. **The content arrives as a bootstrap op**, through the path `Document.load` already takes for an imported plain file with an empty op log — the `needsBootstrap` branch, which `Document+Load.swift:104-110` calls the sanctioned import read. Adopted intent therefore has history starting at adoption, not no history.
3. **Duplicates concatenate, oldest first**, separated by a blank line. The defect in the spec's §3 has been minting second copies; picking a winner discards the writer's prose. Concatenating is recoverable, choosing is not.
4. **Detection is role-first with the filename as fallback** — `ResearchRole.craftIntent`, then `PaletteConvention.craftIntentFileName` — mirroring `PaletteLookup.craftIntentItem`'s two tiers (`PaletteConvention.swift:23-32`), so a note the writer renamed is still found.
5. **The note's file moves through the typed mover.** `DocumentStore.relocateUserContent(affectedPaths:perform:)` closes, flushes and unregisters before your `perform` runs. A raw `moveItem` here is tripwire 14 and is grep-enforced: the 750ms autosave will recreate the file at the old path afterwards, leaving a phantom.
6. **Failure must not block the open.** A project that cannot be adopted still opens, with the legacy note untouched and an error logged. Losing access to your manuscript because an intent note was odd is a worse outcome than un-adopted intent.

**The precedent to follow, and it is exact:** `ProjectStore.load` already performs a one-time, on-open backfill — the project-id backfill at `:263-275`. Read its comment, including why it deliberately leaves `modified` untouched (*"backfilling an identifier is not a content edit"*). Adoption is a content move, so decide and state whether `modified` should shift; do not leave it accidental.

- [ ] **Step 1: Write the failing tests**

- `test_adoptionRunsOnceAndNotAgain` — open a schema-3 fixture twice; assert the statement's content is not doubled and no second file exists. Falsified by removing the schema gate.
- `test_aWriterWithNoIntentIsNotRescannedForever` — a schema-3 project with **no** craft-intent note opens, is stamped 4, and a second open does no scanning work. This is the ambiguity the spec's §5 closed; it needs its own assertion.
- `test_twoNotesForOneScopeAreConcatenatedOldestFirst` — the defect's leavings. Assert **both** bodies are present and assert the order. A test that only checks "both present" passes on a bug that reverses them.
- `test_adoptedContentArrivesAsOps` — assert the statement's derived text comes from `.maugham/ops/`, not from the `.md`. Falsified by writing the file directly and skipping bootstrap — which would look identical on screen and have no history at all.
- `test_aFailedAdoptionStillOpensTheProject`.

- [ ] **Step 2: Run them and watch them fail**

- [ ] **Step 3: Implement**

- [ ] **Step 4: Run `MaughamTests/TripwireGrepTests` explicitly**

The typed-mover census (`test_noRawMoveOfUserContentOutsideTypedMover`) must be green. If it fails, you moved a file the wrong way.

- [ ] **Step 5: Commit**

---

## Task 5: The two panes, and the mounted editor

**Files:**
- Create: `Maugham/Views/StatementEditorHost.swift`, `Maugham/Views/StatementPane.swift`
- Modify: `Maugham/Models/DetailSegment.swift`, `Maugham/Models/Persona.swift:79-108`, `Maugham/MaughamApp.swift` (View menu), `docs/guide/reference.md`
- Test: `MaughamTests/StatementPaneTests.swift`, `MaughamTests/StatementEditorMountTests.swift`
- Read first: `Maugham/Editor/AREA.md` — all of it, before you touch anything.

**Interfaces:**
- Consumes: `ProjectStore.statement(kind:scope:)`, `ProjectStore.createStatement(kind:scope:)` (Task 3).
- Produces: `DetailSegment.intent`, `DetailSegment.visualLanguage`, and the two `Persona.panes` entries. Task 9's docs sweep depends on the shortcut letters you choose.

**Contract:**

1. **Two `DetailSegment` cases**, each with its `systemImageName` and `helpText` (the pattern is at `DetailSegment.swift:23-51`), and two entries in `Persona.panes` — `.intent` → plan, author, review, publish; `.visualLanguage` → plan, review, publish. That mapping is **already reserved in the registry comment** at `Persona.swift:79-82`; edit the reservation down as you consume it.
2. Adding a right-pane surface is one enum case plus one registry entry. It must touch neither `DetailPaneToggle`, the shortcut table, nor `ProjectWindow` — `Persona.swift:64-66` says so explicitly and that is what the extension point is for. **If you find yourself editing `ProjectWindow`, the registry is not being used as intended.**
3. `PersonaPaneRegistryTests.test_everyPersona_matchesTheDesignMatrix` checks the whole matrix rather than a row. It was swept row-wise twice and lost a cell each time. Update the matrix, not a row.
4. Each pane needs a `⌘⌥` letter joining the existing `⌘⌥I/R/O/A/H/T/B/P/L` space, plus its line in `docs/guide/reference.md` — guarded by `DocSyncTests`, which will fail loudly if you skip it.
5. **`ContentUnavailableView` needs `.frame(maxWidth: .infinity, maxHeight: .infinity)` and the enclosing `VStack` needs `alignment: .top`** (tripwire 15; recurred 4+ times; canonical examples are `HistoryPane`, `AnnotationsPane`, `OutlinePane`). Grep-enforced by `test_contentUnavailableViewAlwaysChainsFullFrame`.

**The mount, which is the risky half:**

6. **Mount `EditorSurface`, not `EditorHost`.** Verified signature: `EditorSurface` is an `NSViewRepresentable` taking `@Binding var text: String` plus one `configuration: EditorSurfaceConfiguration` (`EditorSurface.swift:142-147`). `EditorSurfaceConfiguration` groups `presentation` / `callbacks` / `paragraphProviders` / `reviewProviders` (`:14-60`).
7. `StatementEditorHost` owns the `Document` and supplies **exactly** the sanctioned binding — `get: { doc.displayText }`, `set: { doc.setFullText($0) }` — and nothing else. `EditorHost`'s own doc comment (`EditorHost.swift:12-14`) records that `setFullText` writes `displayText` exactly once at the end, and that this is what keeps the milestone-1e binding-loop race closed. A second host with a single binding is not parallel state on the first (tripwire 6); a second *mirror* of the text would be.
8. Configure for prose: **wiki links on** (`[[Chapter 9]]` resolves; `list_all_links` already scans non-manuscript bodies since 1C-c2), **no element gutter, no focus dim, no typewriter scroll**, prose mode always. A screenplay's intent is prose about a screenplay.
9. **No new caller of `EditorSurface.applyExternalText`** (tripwire 7). There is one production call site and this is not the second. `TripwireGrepTests.test_applyExternalTextHasExactlyOneProductionCallSite` is a census and will tell you.
10. Scope follows selection: the selected document's intent, or the project's when no document is selected, with the other one click away. Visual language is project-scope, so it simply shows. An empty scope shows an empty editor that mints on first keystroke — **not** a "create intent" button and not a nag.

- [ ] **Step 1: Write the failing tests**

In `StatementEditorMountTests.swift` — **these must drive the real delivery path.** A test that hand-calls `setFullText` proves nothing about the mount; the previous milestone shipped 22 green undo tests against a ⌘Z that could not reach the stack.

- `test_aKeystrokeInTheMountedPaneReachesTheOpLog` — deliver text through the mounted surface as the editor delivers it, and assert an op lands in `.maugham/ops/<statementId>.jsonl` with `kind == .typingBurst`.

  **This test also settles the spec's §2.4 claim that no new `OpKind` is needed.** Assert the kind explicitly. If it is anything else, the spec is wrong and you should say so before continuing.

- `test_undoInTheStatementPaneRevertsThroughTheOrdinaryRegistrar` — ⌘Z through the real undo manager, not a direct call to an inverse factory.
- `test_theMountIsWhatCarriesTheKeystroke` — **the disable experiment.** Patch out the mount, assert the patch applied (a patch that silently matches nothing runs against unmodified code and reports green, which reads exactly like "the test cannot see its subject"), and assert the keystroke test now fails.

In `StatementPaneTests.swift`: the persona matrix, the scope-follows-selection rule, and that an undeclared scope renders an empty editor and mints nothing until a keystroke.

Plus one pin for a known, accepted harmless behaviour (spec §8) — `test_aCheckboxInAStatementDerivesAnAnchorNothingDisplays`. A `Document` derives `- [ ]` checkboxes into tasks, but the Tasks pane enumerates structure documents, so a checkbox typed into an intent produces an anchor no surface shows. That is out of scope to change and in scope to **pin**, so the next person to find it reads a test instead of filing a bug.

- [ ] **Step 2: Run them and watch them fail**

- [ ] **Step 3: Implement**

- [ ] **Step 4: Full Mac suite, plus a Release build**

```bash
xcodebuild -project Maugham.xcodeproj -scheme Maugham -configuration Release build CODE_SIGNING_ALLOWED=NO
```

The Release type-check budget is stricter than Debug, and this task adds views. A Release-only build failure shipped to CI on v0.8.0 exactly this way. Also heed any `unable to type-check this expression in reasonable time` diagnostic — that class of SourceKit warning is real, unlike the rest.

- [ ] **Step 5: Commit**

---

## Task 6: Re-derive the second half against the built code

**Files:**
- Modify: this plan, tasks 7–10.

**This task exists because deferring it was measured.** On 1C-d, tasks written only as requirements and re-derived after the first half was built produced ~90 signature citations of which an independent reviewer found **zero wrong** — while the pre-written half was corrected four times by implementers who found reality differed. Three separate times a pre-written plan would have deleted another plan's work.

**Contract:**

1. Read what tasks 1–5 actually built. Not what this plan said they would build.
2. Rewrite tasks 7–10 in the same shape as tasks 1–5: files with `file:line`, contracts, the test's subject and its falsification, and consumed signatures **quoted only if you read them out of the tree that day**.
3. **Split task 9 if it is too big.** It currently carries two MCP tool changes, a skill section and a `list_canvas` widening. The ~10-task cap is a guideline; a task nobody can review in one pass is worse than eleven tasks.
4. Note every place the requirements below turned out to be wrong. That list is worth more to the next milestone than the plan is.

- [x] **Step 1: Read the built code for each area tasks 7–10 touch**
- [x] **Step 2: Rewrite tasks 7–10 in place** — done 2026-08-01. Task 9 was split, so they are now Tasks 7–12.
- [x] **Step 3: Commit the re-derived plan**

**Done. The findings are at `.superpowers/sdd/2026-07-31-m1a-spine/task-6-report.md`**, whose
most important section is every place the pre-written requirements were wrong.
The two that would have cost the most: the requirements named neither
`InspectorView` nor `PieceInspector` as consumers of the seam Task 7 was told to
delete, and they said nothing at all about what happens to a promotion's **mark**
when the artifact it names stops being a research item.

---

# HALF TWO — re-derived against the built code on 2026-08-01 (Task 6)

**Every `file:line` below was read out of the working tree at `5950c84` on
2026-08-01.** Where the pre-written requirements were wrong, the correction is
stated in the task rather than silently applied — the list of what a
requirements-only draft gets wrong is worth more to the next milestone than this
plan is. Those corrections are marked **WAS WRONG**.

**Task 9 was split into three** (now Tasks 9, 10 and part of Task 8), because it
carried two MCP tool changes, a skill section and a `list_canvas` widening that
have nothing to do with each other. Tasks 7–10 became **7–12**. The ~10-task cap
is a guideline; a task nobody can review in one pass is worse than twelve tasks.

**Ordering matters between 7 and 8 and nowhere else.** Task 8 deletes
`ProjectStore+CraftIntent.swift`, which Task 7's subject still calls. Tasks 9–12
are independent of each other and of 7–8, except that Task 12 needs Task 5's
Visual Language pane, which is built.

---

## Task 7: Promotion appends into the statement

**Files:**
- Modify: `Maugham/Canvas/PromotionPerformer.swift:498-530` (`performCraftIntent`), `:776-781` (`intentPiece`)
- Modify: `Maugham/Canvas/Promotion.swift:339-361` (`ArtifactIndex.kind(of:)` and `isCraftIntent`), `:1258-1263` (`craftIntentDestination`)
- Test: `MaughamTests/Canvas/PromotionPerformerTests.swift` (existing craft-intent assertions at `:364`, `:454`, `:694`, `:856` all unwrap `store.craftIntentItem(forPieceId: nil)` and must be rewritten against the statement), `MaughamTests/Canvas/PromotionPieceRoutingTests.swift:288-347`
- Read first: `Maugham/Canvas/AREA.md`; `Maugham/Views/StatementEditorHost.swift:244-307` (`reconcile`, whose doc comment states the two-Documents-on-one-path hazard this task can reintroduce)

**Interfaces:**
- Consumes, by exact name, from Task 3 (`Maugham/Stores/ProjectStore+Statements.swift`):
  - `ProjectStore.statement(kind:scope:) -> Statement?` — `:21`, pure, mints nothing.
  - `ProjectStore.createStatement(kind:scope:) async throws -> Statement` — `:37`, `@discardableResult`, idempotent.
  - `Statement.id`, `Statement.path` (project-relative) — `Packages/MaughamCore/Sources/MaughamCore/Statement.swift:124-129`.
  - `Statement.Scope.project` / `.document(String)` — `Statement.swift:85-90`.
- Produces: nothing new. This retargets one arm of an existing performer.

**Contract:**

1. **`performCraftIntent` resolves its destination by scope, not by research
   prefix.** `PromotionTarget.intentStatement` keeps its name
   (`Promotion.swift:72`), its precedence rules and its behaviour. It calls
   `store.createStatement(kind: .intent, scope: …)` and the scope comes from the
   plan's source.

2. **`intentPiece` becomes a scope, and its narrowing rule dies with the defect.**
   Verified today, `PromotionPerformer.swift:776-781`: `intentPiece` returns a
   piece id **only** when `store.researchRouting(forDocumentId:)` answers
   `.pieceFolder`, and returns nil for everything else. That guard exists solely
   because the old lookup could not find a novel chapter's note — the reason is
   spelled out in the comment at `:502-511`. A statement is found by scope, so
   every routing now scopes to its own document. **This is the milestone's
   headline case arriving in the promotion path**: a scrap promoted to intent on
   a novel chapter must land on `.document(chapterId)`, not on the project.

3. **Appending goes through the op log, which removes the flush dance.** Delete
   `try? await store.documentStore?.flushPendingSave()` at `:515` and the
   `readBody` / join / `write` sequence at `:516-520`. That call exists so that
   what is appended to is what is on disk; an op-log append has no such hazard.
   Deleting it is part of the task, not an optimisation.

4. **The defect comment goes with the defect.** `PromotionPerformer.swift:499-511`
   documents the split-intent bug from the inside. It is **thirteen lines at
   `:499-511`** — the pre-written text said *"The fifteen-line comment at
   `:503-512`"*, which is a prose count over a comment and is wrong in both the
   count and the range (**WAS WRONG**). Delete it; a comment explaining a bug
   that no longer exists is worse than no comment. `Promotion.swift:1243-1247`
   carries a second copy of the same reasoning inside `craftIntentDestination`'s
   doc comment (*"lands where `craftIntentItem(forPieceId:)` never looks"*) and
   must go the same way.

5. **`craftIntentDestination` must now name a piece on every routed row.**
   `Promotion.swift:1258-1263`, verified today, returns the piece's title **only**
   for `.routed(_, _, .ownResearch)` and says *"the project's craft intent"* for
   every other row. That is `intentPiece`'s narrowing rule as a sentence. Once
   contract 2 lands, the copy is wrong for a novel chapter — the sheet would
   promise the project's intent and the performer would write the chapter's.
   `PromotionResult.confirmation(for:)` reads `plan.destinationDescription`
   rather than restating it (`PromotionPerformer.swift:44`), so fixing the one
   function fixes both sentences. `Promotion.researchNoteDestination`
   (`:1209`) is **not** yours: research routing is unchanged.

6. **THE MARK IS THE HARD PART, and the pre-written text did not mention it at
   all** (**WAS WRONG — by omission**). `mark(item.id, …)` at `:526` writes the
   artifact's id into `CanvasNode.promotedItemID`. Today that is a **research
   item** id, and every reader of a mark resolves it through
   `ArtifactIndex.over(research:)` (`Promotion.swift:320`), which is built
   from `manifest.research` and nothing else. A **statement id is not in that
   index**. Three verified consequences, and you must decide and pin an answer
   for each:
   - `ArtifactIndex.isCraftIntent` (`:356-361`) tests `role == .craftIntent` or
     the `craft-intent.md` filename on a *research item*. After this task
     nothing produces one, so `ArtifactKind.craftIntent` (`:170`) becomes
     unreachable from `over(research:)`.
   - `Promotion.hasDanglingMark` (`:760-765`) is `artifacts.title(of: mark) == nil`.
     An unresolvable statement id makes it answer **true**, and its one caller
     (`:732`) then refuses a line promotion with *"What one of these cards
     produced is no longer in the project"* — false, and the writer is told to
     re-promote something that worked.
   - `PromotedArtifactSection.provenance(promotedItemID:contributedToItemID:title:)`
     is fed by `ScrapInspector.swift:66-69` and `RegionInspector.swift:222-225`
     through an `artifactTitle` resolver. An unresolvable id renders
     `.artifactMissing` — the inspector tells the writer their intent was
     deleted.

   `existingArtifact` is **not** on this list and that is verified, not assumed:
   `updatableTargets` is `[.researchNote, .paletteCard]` (`Promotion.swift:605`)
   and `existingArtifact` returns nil for anything else (`:793`), so no Rewrite
   is ever offered for an intent. Leave that alone.

   **Two shapes are available and one is a trap.** Teaching `ArtifactIndex` about
   statements (an `over(research:statements:)`, or a second entry source) keeps
   the mark meaningful and keeps every reader honest — but `ArtifactIndex` is
   deliberately pure and takes no `ProjectStore` (`:280-297`), so the statements
   must arrive at the one construction site, `ProjectWindow.swift:2330`. Not
   writing a mark at all is the trap: the mark is what draws the stripe on the
   card and what `CanvasAccessibility` speaks, so a silent removal makes a
   promoted card look un-promoted. **State which you chose and why, in the code.**

7. **THE LIVE `Document` COLLISION, also unmentioned by the pre-written text**
   (**WAS WRONG — by omission**). A statement's `Document` is **deliberately not
   registered** with `DocumentStore` — `StatementEditorHost.swift:201-208` says
   why (registering it would put it in `allOpenDocuments()`, which
   `ProjectStore+Tasks.swift:201` iterates to build the project Tasks
   aggregation, contradicting spec §8). So `documentStore.document(forDocId:)`
   (`DocumentStore.swift:742`) **cannot find an open statement**, and a promotion
   that opens its own `Document` on the statement's path can be the *second* live
   `Document` on that path — each with its own `PendingBuffer` writing the same
   file. That is the exact hazard `StatementEditorHost.reconcile`'s doc comment
   (`:244-266`) describes and designs against.

   **It is reachable, not theoretical:** `.intent` is a **●** pane of the Plan
   persona (`Persona.swift:94-100`), which is the persona the canvas lives in. The
   writer can have the Intent pane showing project intent in the right column and
   promote a scrap to craft intent from the canvas in the centre column of the
   same window.

   Decide the seam and say so in the code. A `ProjectStore`-owned accessor that
   both the pane and the performer go through, or an event the open pane services,
   or a documented ordering — but not "it will probably be fine". Whatever you
   choose, **`Document.load` stays the only construction path** (hard invariant;
   `BootstrapWiringTests`).

8. **Tripwire 32 is satisfied by not changing it.** `PromotionPerformer.swift` is
   already a member of the undo-bracket census
   (`TripwireGrepTests.swift:1980-1990`) with `mutateFromInspector`, and `mark`
   (`:806-820`) is the only scene write in the arm. **Count the array in that
   test rather than any prose count** — including this one. Run
   `test_theCanvasUndoBracketIsReachedFromAnotherColumnByOneVerbOnly`.

9. **`plan.contributors` handling is unchanged**, and the comment at `:521-525`
   explaining why it is passed rather than `[]` stays.

10. **`ProjectStore+CraftIntent.swift` is NOT deleted here.** Task 8 deletes it,
    after the last consumer moves. Do not delete a file another task still calls.

**A note the ledger owes you:** adoption (Task 4) **trashes** the legacy note, so
between Task 4 and Task 8 `read_craft_intent`, `performCraftIntent` and the
phone's Read tab all find nothing. Nothing is lost — the prose is in the
statement's op log — but **no interim smoke of this branch is meaningful**. Do
not conclude intent is broken from a partial branch.

- [ ] **Step 1: Write the failing tests**

In `MaughamTests/Canvas/PromotionPerformerTests.swift` (rewriting the four
existing craft-intent assertions rather than adding beside them):

- `test_aScrapPromotedOnANovelChapterLandsOnThatChaptersIntent` — **the defect's
  grave, on the promotion path.** Falsified by restoring `intentPiece`'s
  `.pieceFolder` guard: the scope becomes `.project` and the assertion dies.
- `test_promotingTwiceAppendsRatherThanReplacing` — both bodies present, in
  order. A test that only checks "both present" passes on a bug that reverses
  them.
- `test_thePromotedIntentArrivesAsOps` — assert the appended text is in
  `.maugham/ops/<statementId>.jsonl`, not written straight to the `.md`. This is
  what makes contract 3 checkable; a direct file write looks identical on screen
  and has no history.
- `test_aCardPromotedToIntentStillResolvesItsMark` — whichever shape you chose
  for contract 6, this asserts the outcome the writer sees: the card's inspector
  says what it became, and a line promotion between two such cards is **not**
  refused as dangling. Falsified by leaving `ArtifactIndex` research-only.
- `test_promotingWhileTheIntentPaneIsOpenDoesNotOpenASecondDocument` — contract
  7's assertion. `StatementMountFixture` (`MaughamTests/StatementMountFixture.swift`)
  mounts a real pane in a real window and has `settle(_:expectingOpsFor:)` and
  `ops(forDocId:)`; use it rather than hand-driving. **State in the test what
  failure looks like** — the honest failure here is lost keystrokes, not a crash.

In `MaughamTests/Canvas/PromotionPieceRoutingTests.swift:288-347`: the guard
whose stated consequence was *"`craftIntentItem(forPieceId:)` cannot find it"* is
now false. Rewrite it to assert the new rule rather than deleting it.

**Control:** at least one assertion in each file must pass against unmodified
code, so a green run cannot mean the file stopped compiling into the target.

- [ ] **Step 2: Run them and watch them fail**

```bash
xcodebuild -project Maugham.xcodeproj -scheme Maugham test CODE_SIGNING_ALLOWED=NO \
  -only-testing:MaughamTests/PromotionPerformerTests \
  -only-testing:MaughamTests/PromotionPieceRoutingTests
```

Suite paths are flat. A folder-shaped path runs zero tests and exits 0.

- [ ] **Step 3: Implement**

- [ ] **Step 4: Run the canvas suites and `TripwireGrepTests` explicitly**

- [ ] **Step 5: Commit**

---

## Task 8: The last craft-intent readers move, and the seam is deleted

**Files:**
- Modify: `Maugham/Views/InspectorView.swift:109-125` (the project's `Section("Craft Intent")`), `:9` (`onOpenCraftIntent`)
- Modify: `Maugham/Views/PieceInspector.swift:107-126` (`craftIntentSection`), `:48` (`onOpenCraftIntent`)
- Modify: `Maugham/Views/ProjectWindow.swift:1280`, `:1284`, `:1302` (the three sites passing `openResearchItem` as `onOpenCraftIntent`)
- Modify: `Maugham/MCP/Tools/CraftIntentTools.swift` (whole file, 41 lines)
- Modify: `Maugham/Stores/ProjectStore+Palette.swift:265-276` (the craft-intent arm of `healPaletteRolesEagerly`)
- Modify: `Maugham/Stores/ProjectStore+CollectionPieces.swift:334-355` (`writePromotedManifest`)
- **Delete:** `Maugham/Stores/ProjectStore+CraftIntent.swift`
- Delete or rewrite: `MaughamTests/ProjectStoreCraftIntentTests.swift` (every test in it drives the deleted seam), `MaughamTests/MCP/Tools/CraftIntentToolTests.swift:35`
- Modify: `docs/guide/sense-pass.md:10-11`, `docs/guide/getting-started.md:71-77`, `:163`
- Read first: `Maugham/Stores/AREA.md`, the statement seam and adoption sections at `:90-120`

**Interfaces:**
- Consumes: `ProjectStore.statement(kind:scope:)` and `ProjectStore.createStatement(kind:scope:)` (Task 3); `DetailSegment.intent` (Task 5, `Maugham/Models/DetailSegment.swift:18`); `MaughamEvent.maughamSetDetailSegment` with payload key `"segment"` carrying `DetailSegment.rawValue`, scoped `.keyWindow` — the spelling is `MaughamApp.postSegment` (`Maugham/MaughamApp.swift:340-344`).
- Produces: nothing new. This removes a seam.

**The pre-written text said only *"`ProjectStore+CraftIntent.swift` is deleted
once its consumers have moved"* and named `PromotionPerformer` and
`read_craft_intent`. It named neither inspector. WAS WRONG — the brief's file
list is not exhaustive, and this is the lesson Task 1 recorded arriving again.**
The production consumers of the seam, enumerated from the call sites today:

| Call site | What it calls |
|---|---|
| `Maugham/Canvas/PromotionPerformer.swift:512` | `createCraftIntent` — **moved by Task 7** |
| `Maugham/Views/InspectorView.swift:110`, `:118` | `craftIntentItem` + `createCraftIntent` |
| `Maugham/Views/PieceInspector.swift:110`, `:118` | `craftIntentItem` + `createCraftIntent` |
| `Maugham/MCP/Tools/CraftIntentTools.swift:30` | `craftIntentItem` |

Re-run the grep before you commit; do not trust this table. The two aliases
`ProjectStore.craftIntentFileName` / `craftIntentTitle` (`:11-12`) also die, and
they have test callers (`MaughamTests/StatementAdoptionTests.swift:29`, `:190`,
`:299`, `:552`, `:559` and others) — those should point at
`PaletteConvention.craftIntentFileName` / `craftIntentTitle`
(`Packages/MaughamCore/Sources/MaughamCore/PaletteConvention.swift:9-10`), which
stay: **adoption still needs them** (Task 4's filename fallback) and so does
`Promotion.ArtifactIndex.isCraftIntent` if Task 7 kept it.

**Contract:**

1. **The two inspector arms stop minting research notes.** Each shows whether the
   scope has an intent statement and offers to go to it. "Open Craft Intent" and
   "Add craft intent…" collapse into one affordance, because the Intent pane's
   own rule is that **absence is valid and an empty scope shows an empty editor
   that mints on the first keystroke** (spec §4.3; built at
   `StatementEditorHost.swift:16-20`). A separate "Add" button in the inspector
   would be the nag the pane deliberately does not have.

2. **The switch is a pane swap in the same column**, and that is worth
   understanding before you write it: `.inspector` and `.intent` are both
   `DetailSegment` cases, so the inspector is asking the right column to show a
   different segment of itself. `MaughamEvent.post(.maughamSetDetailSegment, …)`
   scoped `.keyWindow` is the one spelling (tripwire 21 — no raw
   `maugham.*` NotificationCenter post outside `MaughamEvent`).

3. **Check that a Collection piece selection reaches the pane's scope, and say
   what you found.** `StatementPane.effectiveScope`
   (`Maugham/Views/StatementPane.swift:41-58`) resolves `.document(id)` only when
   `activeDocumentId` names a `.document` item in `manifest.structure`;
   everything else falls to `.project`. `PieceInspector` renders for a Collection
   piece. If a piece selection does not arrive as `activeDocumentId`, sending the
   writer to the Intent pane shows them the **project's** intent while the button
   sat under that piece's heading — which is the class of wrongness this
   milestone exists to remove. Assert the answer either way.

4. **`read_craft_intent` widens to any document scope.** It already takes
   `item_id` (`CraftIntentTools.swift:16-19`) and its description already says
   *"Pass item_id for a collection loose piece"*; that sentence becomes *any
   manuscript document*. **Keep the absence-is-valid sentence verbatim** — *"that
   is a valid, deliberate state — do not invent a standard on their behalf"* — it
   is load-bearing and the store's own doc comments cite it
   (`ProjectStore+Statements.swift:17-20`). A widening of an existing read leaves
   `MCPToolCatalog.all.count` unchanged, as `list_all_links` did in 1C-c2, so
   `DocSyncTests.test_toolCountSyncedAcrossDocsAndCatalog` stays green with no
   doc edit.

5. **The read must derive from the op log, and the current annotation's REASON is
   about to become false.** `CraftIntentTools.swift:35` reads the `.md` directly
   under `// adr-0018-ok: craft-intent note read, not manuscript`. That
   justification held while intent was a plain research note. A statement is a
   `Document` with an op log, so tripwire 20 applies: derive it. The verified
   shape used elsewhere in this directory is
   `store.derivedCache.materialize(forDocId:in:)` /
   `.displayText(forDocId:in:)` (`Packages/MaughamCore/…/DerivedManuscriptCache.swift:73`,
   `:80`), as `DocumentTools.swift:100` and `ReferenceTools.swift:50` use it.
   **The open-doc branch those tools take is unavailable to you** — a statement is
   deliberately unregistered (`StatementEditorHost.swift:201-208`), so
   `documentStore.document(forDocId:)` returns nil for one. Say in the code that
   the derived read is therefore the *only* branch, and what the writer's
   unflushed typing burst means for freshness. If Task 7 built an accessor for
   its own collision (contract 7 there), use it and say so.
   `MCPResponseBudget.enforce` stays (`:36-39`).

6. **`healPaletteRolesEagerly`'s craft-intent arm.** `ProjectStore+Palette.swift:265-276`
   stamps `role = .craftIntent` on a note matched by filename. Adoption's own
   detection depends on that stamp having run — `Maugham/Stores/AREA.md` records
   that the filename fallback in adoption is unreachable through `load` *because*
   this heal runs immediately before it. **Do not remove this**; adoption is the
   last reader of `ResearchRole.craftIntent` and it still needs it. Add a comment
   saying so, because after this task it looks like dead code.

7. **A Collection piece promoted to its own project loses its intent, and that is
   a decision, not an accident.** Verified: `writePromotedManifest`
   (`ProjectStore+CollectionPieces.swift:334-341`) carries research items whose
   path has the piece's research prefix and rewrites them; a loose piece's
   craft-intent note lives in `pieces/<n>-<slug>/research/` and is carried today.
   After M1A that intent is a `.document(pieceId)` **statement** at
   `intent/<slug>.md` at the project root, which no prefix matches, and the new
   manifest takes `statements: []` by default (Task 1's memberwise default,
   `ProjectManifest.swift`). So the writer's intent silently does not travel.
   This was parked for Task 6 in the ledger and is now yours: **carry it, or
   refuse it and say so where the manifest is built.** Carrying is one entry in
   the new manifest plus one file move through the typed `DocumentStore` mover
   (tripwire 14 — `relocateUserContent(affectedPaths:perform:)`,
   `DocumentStore.swift:586`; the staging tree's `// internal-move:` exemption
   covers the staging step only). Pin whichever you choose.

8. **`ProjectStore+CraftIntent.swift` is deleted last, and the build tells you if
   you missed a caller.** Grep for `craftIntentItem`, `createCraftIntent`,
   `craftIntentResearchPrefix`, `ProjectStore.craftIntentFileName` and
   `ProjectStore.craftIntentTitle` across the whole tree — including
   `MaughamPhone/`, which has its own `PaletteLookup.craftIntentItem` calls that
   are **Task 11's, not yours**.

9. **The guide prose is false as of Task 4 and nothing fails loudly.**
   `DocSyncTests` guards the shortcut table and the tool count, not this prose.
   `docs/guide/sense-pass.md:10-11` says *"write a craft intent doc… Add one from
   the inspector (**Add craft intent…**)"*; `docs/guide/getting-started.md:74`
   says the craft intent is *"one document per project (or per piece, in a
   Collection)"*. Both describe an affordance this task removes. **Help surfaces
   describe what ships** (CLAUDE.md rule 7). `docs/skills/editing-pass/SKILL.md:22`
   mentions craft intent and should be checked too.

- [ ] **Step 1: Write the failing tests**

- `test_theProjectInspectorSendsTheWriterToTheIntentPane` — assert the posted
  event and its payload, not a closure call. **This must drive the real delivery
  path**: the previous milestone shipped 22 green undo tests against a ⌘Z that
  could not reach the stack.
- `test_aCollectionPieceInspectorLandsOnThatPiecesIntent` — contract 3's
  assertion, whichever way it resolves.
- `test_readCraftIntentAnswersForANovelChapter` — the widening. Falsified by
  restoring the `item_id`-is-a-loose-piece-only behaviour.
- `test_readCraftIntentStillSaysExistsFalseForAnUndeclaredScope` — absence stays
  valid, and stays free of side effects: assert the manifest and directory are
  unchanged afterwards.
- `test_readCraftIntentReadsTheOpLogAndNotTheFile` — contract 5. Falsify it by
  staling the `.md`: append an op, leave the file behind, and assert the tool
  returns the op's text. **A test that does not stale the file passes under both
  the bug and the fix** — that exact falsifiability defect was found and fixed on
  Task 2 of this branch.
- `test_aPromotedPieceCarriesItsIntent` *(or `…DeliberatelyDoesNot`)` — contract 7.

- [ ] **Step 2: Run them and watch them fail**
- [ ] **Step 3: Implement, deleting `ProjectStore+CraftIntent.swift` last**
- [ ] **Step 4: Full Mac suite plus a Release build**

```bash
xcodebuild -project Maugham.xcodeproj -scheme Maugham -configuration Release build CODE_SIGNING_ALLOWED=NO
```

This task edits `ProjectWindow.swift` and two inspectors; the Release type-check
budget is stricter than Debug and a Release-only build failure shipped to CI on
v0.8.0 exactly this way.

- [ ] **Step 5: Commit**

---

## Task 9: `read_visual_language`, and the line in the skill

**This is M1's unmet second protection.** Without it, visual language is an
artifact that does nothing for three milestones (spec §1, §6.3).

**Files:**
- Create: `Maugham/MCP/Tools/VisualLanguageTools.swift`
- Modify: `Maugham/MCP/MCPTool.swift` — `MCPToolCatalog.all` is the array at `:36-91`; the current last entry is `AddCanvasScrapsTool.self` at `:90`
- Modify: `docs/skills/maugham-bootstrap/SKILL.md` (47 lines today; one `##` section, *"A page the writer photographed"*, at `:24`)
- Modify: `CLAUDE.md` (the MCP row's `**NN tools**` phrase) and `Maugham/MCP/AREA.md` (the `## Tool catalogue (NN)` heading at `:9`, and the tool list — the Palette / craft intent group is at `:36-37`)
- Test: `MaughamTests/MCP/Tools/VisualLanguageToolTests.swift`
- Read first: `Maugham/MCP/AREA.md`; `memory/feedback_skill_authoring_intent.md`

**Interfaces:**
- Consumes: `ProjectStore.statement(kind:scope:)` (Task 3, `ProjectStore+Statements.swift:21`) with `kind: .visualLanguage`, `scope: .project`; `Statement.path`.
- Consumes the MCP helpers every tool in this directory uses: `decodeParams(_:from:)`, `resolveProject(_:in:)`, `MCPResponseBudget.enforce(_:hint:)` — see `CraftIntentTools.swift:28-39` for the canonical four-line shape.
- Produces: `ReadVisualLanguageTool` conforming to `MCPTool`, registered in `MCPToolCatalog.all`.

**Contract:**

1. **Project scope only.** Visual language is project-scope by construction:
   `StatementConvention.newPath` returns nil for `(.visualLanguage, .document)`
   (`Packages/MaughamCore/Sources/MaughamCore/StatementLookup.swift:38-52`) and
   `createStatement` throws `.statementHasNoStorage` for that pair. The tool
   takes `project_id` and nothing else.

2. **Absence is valid**, in `read_craft_intent`'s shape: `exists: false`, never
   an error.

3. **It returns the prose plus the referenced image paths** (spec §6.3). The
   prose derives from the op log — contract 5 of Task 8 applies here word for
   word, including that a statement is unregistered so there is no open-doc
   branch.

4. **There is no shared inline-image scanner to reuse, verified today.**
   `PaletteCard.inlineImagePaths(in:)` is **`private`**
   (`Packages/MaughamCore/Sources/MaughamCore/PaletteCard.swift:249`) and is
   section-gated to a card's `## Images` heading anyway;
   `MarkdownBlockParser.matchSoloImage(_:)` is `public`
   (`MarkdownBlockParser.swift:282`) but matches only a line that is *entirely*
   an image reference. Path resolution has one spelling —
   `ProjectStore.resolveImageRef(_:relativeTo:)`
   (`Maugham/Stores/ProjectStore+Palette.swift:214`) — and a second spelling of
   ref→path is the drift `ProjectStore+CanvasAssets.swift:59-70` warns about at
   length. **Decide whether to promote a shared scanner into `MaughamCore` or to
   use `MarkdownBlockParser` and accept that an inline image mid-paragraph is not
   reported, and say which in the tool's doc comment.** Whichever you choose,
   resolve through `resolveImageRef`.

5. **Paths, not pixels, and name the edge.** `list_canvas` deliberately reports no
   path (`Maugham/MCP/AREA.md:81`), on the grounds that nothing in the catalogue
   reads a file by project-relative path. A visual-language image path has the
   same property: Claude cannot turn it into pixels. Say so in the description
   rather than implying otherwise — `read_document` is the catalogue's only image
   reader and it takes a research item id.

6. **Derive the count from `MCPToolCatalog.all`, never a literal.**
   `DocSyncTests.test_toolCountSyncedAcrossDocsAndCatalog` (`:50`) parses
   `**NN tools**` out of CLAUDE.md and `Tool catalogue (NN)` out of
   `Maugham/MCP/AREA.md` and compares both to `MCPToolCatalog.all.count`; its
   planted-offender twin at `:81` derives its planted numbers from the live count
   for exactly this reason. **Run `./gen.sh` before quoting any count.** Expect a
   new tool to fail several tools-list tests —
   `MCPToolsListSmokeTest:19`, `MCPProtocolHandlersTests:81`,
   `MCPCatalogConsistencyTests` (three loops, `:42`, `:60`, `:71`),
   `ListMaughamToolsToolTests:16` — that is the catalogue working, not a
   regression.

7. **The skill section is intent-first, not a recipe.** A deliverable and what
   matters, never a numbered procedure — prescriptive skills degrade capable
   models (`memory/feedback_skill_authoring_intent.md`; the crop-seam line-loss
   incident). It tells Claude to read visual language before authoring a LaTeX or
   CSS template. **No new skill**: there is no publishing skill today and M4 owns
   the real publish surface. `docs/skills/maugham-bootstrap/SKILL.md` is already
   the router and already carries one task section in this shape — mirror it.
   **Verify every tool name and parameter you write against the tool's actual
   schema**, which is the specific failure mode that memory file records.

- [ ] **Step 1: Write the failing tests**

- `test_readVisualLanguageReturnsTheProseAndItsImages`
- `test_readVisualLanguageSaysExistsFalseWhenNoneIsDeclared` — and assert nothing
  was minted.
- `test_readVisualLanguageDerivesFromTheOpLog` — stale the `.md` first, as Task 8
  contract 5 requires, or the assertion cannot fail.
- `test_theToolCountIsDerivedFromTheCatalogue` is already
  `DocSyncTests`' job — run it, do not write a second one.

- [ ] **Step 2: Run them and watch them fail**
- [ ] **Step 3: Implement, then `./gen.sh`, then update the two doc counts**
- [ ] **Step 4: Run `MaughamTests/DocSyncTests` and the MCP suites**

Add `-skip-testing:MaughamTests/MCPServerLifecycleTests` for a full run; three
MCP tests depend on wall-clock progress and fail under a loaded suite while
passing in isolation. Apply that discriminator before believing a red run is
about your branch (`docs/superpowers/notes/2026-07-29-mcp-clock-dependent-tests.md`).

- [ ] **Step 5: Commit**

---

## Task 10: `list_canvas` reports the reference projection

**This task touches no statement and no craft intent.** It was bundled into the
pre-written Task 9 because both were "MCP", which is not a reason.

**Files:**
- Modify: `Maugham/MCP/Tools/CanvasTools.swift` — `Region` at `:86-104`, `Result` at `:116-125`, the description at `:129-144`, `describe(_:)` for regions
- Modify: `Maugham/Canvas/RegionBinding.swift:3-11` (the doc comment)
- Modify: `Maugham/MCP/AREA.md:81` (the `list_canvas` entry)
- Test: `MaughamTests/MCP/Tools/CanvasToolsTests.swift` (or the existing `list_canvas` suite — find it, do not assume the name), alongside `MaughamTests/Canvas/RegionBindingTests.swift`

**Interfaces:**
- Consumes: `RegionBinding.references(forPiece:in:) -> Set<CanvasNodeID>` — verified signature, `Maugham/Canvas/RegionBinding.swift:35`; `CanvasClaudeWrite.readScene(store:projectRoot:)`, the shared open-canvas/sidecar discriminator already used by this tool at `CanvasTools.swift:160`.
- Produces: one new field on the `list_canvas` response.

**Contract:**

1. **`RegionBinding.references(forPiece:in:)` has zero production callers, still
   true today.** The three non-test references are
   `RegionInspector.swift:416` (a comment saying exactly that),
   `CanvasScene.swift:67` (a comment) and its own definition. Its two rules —
   **residents only** and **unioned across regions** — are stated only in its doc
   comment.

2. **The data is live and the rule is dormant, which is worse than an unused
   function.** `list_canvas` already ships `bound_piece_id` on both nodes
   (`CanvasTools.swift:73`) and regions (`:96`), and `home_node_ids` /
   `appearance_node_ids` on regions (`:100-103`). A reader deriving a piece's
   references from `bound_piece_id` alone has no way to know that a *visitor*
   does not count — the fields are there and nothing says which to use.

3. **So the tool reports references as the projection defines them.** It is a
   widening of a shipped tool: **the count does not move**, and neither doc's
   count needs editing. Decide the wire shape (a per-piece map on `Result`, or a
   `reference_node_ids` on each region) and say why in the description, which is
   what Claude actually reads.

4. **Use the function, do not re-derive the rule.** A second implementation of
   residents-only-unioned-across-regions inside `CanvasTools.swift` is exactly
   what tripwire 19's reasoning forbids one layer down, and
   `CanvasMembershipTests:162` already names this function as the thing membership
   feeds.

5. **Correct `RegionBinding.swift:3-11`, and both halves of it are wrong.**
   Verified against `docs/superpowers/specs/2026-07-25-mode-based-ux-redesign-design.md`:
   - It cites **§4.4**, which does not exist — §4 stops at §4.3, and the sentence
     it quotes is §8's.
   - It says *"Produced here, consumed in 1A. The reference rail is 1A's work"*.
     §10 assigns the intent strip, pinned references and the assistant column to
     **M2**. `list_canvas` becomes the consumer; the rail does not.
   The `.references` segment reserved at `Maugham/Models/Persona.swift:88-91`
   **stays reserved** — Task 5 edited that reservation down for `.intent` and
   `.visualLanguage` only, and `.references → author, review` is still listed
   there. Do not consume it.

**The spec's second falsifiable claim is yours to attack** (spec §6.4, §11): *the
References pane belongs to M2, not 1A.* If building this convinces you the pane
belongs here, say so and stop — refusing a falsifiable ruling is the standard.
(The first claim, §2.4's *no new `OpKind`*, was attacked and **survived** on Task
5: a keystroke into the mounted pane produces ops whose kind set is exactly
`[.typingBurst]`, asserted by name.)

- [ ] **Step 1: Write the failing tests**

- `test_aVisitingCardIsNotOneOfThePiecesReferences` — a card appearing in a bound
  region but living elsewhere. **This is the whole point**; falsified by deriving
  from `home_node_ids ∪ appearance_node_ids`.
- `test_twoRegionsBoundToOnePieceUnionTheirReferences`.
- `test_aPieceWithNoBoundRegionReportsNoReferences` — the control, and it must be
  able to fail.

- [ ] **Step 2: Run them and watch them fail**
- [ ] **Step 3: Implement**
- [ ] **Step 4: Run the canvas and MCP suites**
- [ ] **Step 5: Commit**

---

## Task 11: The phone reads statements

**Files:**
- Modify: `MaughamPhone/Read/BinderView.swift:27-33` (`craftIntent`, `hasPalette`) and `:103-115` (the row)
- Modify: `MaughamPhone/Read/PaletteLoading.swift:30-34` (the craft-intent exclusion in `excludingPalette`)
- Test: `MaughamPhoneTests/PaletteLoadingTests.swift` (existing craft-intent fixtures at `:65`, `:74`), plus a new statement suite
- Read first: `MaughamPhone/AREA.md` — all of it, in particular the iOS tripwires (tripwire 18: don't assume a file is evicted; `startAccessing == false` is not a denial; don't `d_`-prefix a docId that already has one)

**Interfaces:**
- Consumes, from Task 1 and Task 3, both `public` in `MaughamCore`:
  - `StatementLookup.statement(in:kind:scope:) -> Statement?` — verified signature, `Packages/MaughamCore/Sources/MaughamCore/StatementLookup.swift:74-78`.
  - `Statement.path`, `Statement.Kind.intent`, `Statement.Scope.project` — `Statement.swift:33-137`.
  - `ProjectManifest.statements` (Task 1).
- Produces: nothing the Mac consumes. The phone reads; it does not edit statements in this milestone.

**Contract:**

1. **The phone reads the statement FIRST and falls back to the legacy note —
   it does not simply swap.** The pre-written text said *"Both move to
   `StatementLookup`"* (**WAS WRONG**). Adoption is Mac-only: it runs from
   `ProjectStore.load` (`Maugham/Stores/ProjectStore.swift:311`), and the phone
   has no `ProjectStore` and never writes a manifest
   (`Maugham/Stores/AREA.md`, role-identity section: *"Mac-only: the phone never
   writes the manifest, so it consumes `PaletteLookup` read-only with no
   healing"*). A writer who has not opened a project on the Mac since updating
   still has a schema-3 manifest with a `craft-intent.md` research note and no
   `statements` section. A straight swap shows that writer **nothing**, which is
   §2.5's second reason for the paired release re-created by the fix for it.

2. **One `MaughamCore` implementation, called by both surfaces** (tripwire 19).
   `StatementLookup` is already that implementation and the Mac already calls it
   (`ProjectStore+Statements.swift:22`). The phone must not add a local lookup,
   a local path constant, or a local scope parser — a stricter local doc-id
   parser is what shipped phone-v0.1.1's *"No open annotations"*. The reach-around
   greps in `MaughamPhoneTests/TripwirePhoneGrepTest.swift` catch known bad
   spellings; **the real net is a round-trip integration test** — one that builds
   a manifest the Mac's `createStatement` produced and reads it with the phone's
   code.

3. **A statement lives at the project root, not under `research/`.** The row's
   URL is `project.url.appendingPathComponent(statement.path)` — `intent.md`, not
   `research/craft-intent.md`. `BinderView:103-115` builds a `DocumentReaderView`
   from `intent.path ?? ""`; a `Statement.path` is non-optional
   (`Statement.swift:129`), so that `?? ""` disappears.

4. **The `excludingPalette` filter still needs its legacy arm.** `PaletteLoading.swift:32`
   removes the craft-intent research note from the ordinary Research section so
   it is not shown twice. Under contract 1 that note can still exist, so the
   exclusion stays — and it must not exclude anything when the statement is what
   is being shown, or a project with both would lose a research row for no
   reason.

5. **No section rather than an empty one.** `hasPalette` (`BinderView.swift:33`)
   already gates on `!paletteCards.isEmpty || craftIntent != nil`; keep that
   shape. A project with no intent shows no Craft Intent row.

6. **The display read stays a contracted divergence.** The phone renders the
   on-disk `.md` through `CoordinatedFileIO.coordinatedRead`, annotated
   `// adr-0018-ok: contracted display read` and registered in
   `docs/superpowers/notes/cross-surface-contracts.md`
   (`MaughamPhoneTests/TripwirePhoneGrepTest.swift:290`). A statement's `.md`
   is derived from its op log exactly as a manuscript's is, so this is the same
   divergence and needs no new argument — but **if you add a read, annotate it**,
   or `test_noManuscriptFileReadsOutsideReconciler` will fail.

7. **Visual language is not in this task.** The spec asks the phone for intent
   (§6.2) and nothing else. Adding it would be a scope change; if you think it is
   free, say so and let the controller decide.

8. **The registry**: this is a cross-surface contract. Check whether
   `docs/superpowers/notes/cross-surface-contracts.md` needs an entry for
   statements, and add it if so.

- [ ] **Step 1: Write the failing tests**

- `test_theReadTabShowsAnAdoptedIntent` — a manifest with a `statements` entry
  and no research note. Falsified by leaving the legacy lookup in place alone.
- `test_theReadTabStillShowsAnUnadoptedNote` — contract 1. **Falsified by the
  straight swap the pre-written text asked for**, which is the reason this test
  exists.
- `test_aProjectWithNeitherShowsNoIntentRow`.
- A round-trip: build the manifest through the Mac's `createStatement` and read
  it with the phone's code (contract 2). If that cannot be done in one target,
  say so and pin the JSON instead — but say so.

- [ ] **Step 2: Run them and watch them fail**

```bash
xcodebuild -project Maugham.xcodeproj -scheme MaughamPhone \
  -destination 'platform=iOS Simulator,name=iPhone 17' test CODE_SIGNING_ALLOWED=NO
```

Transient *"Busy / failed preflight checks"* is a flake — re-run; do not
`simctl shutdown all` before a launch.

- [ ] **Step 3: Implement**
- [ ] **Step 4: Both schemes, plus `MaughamPhoneTests/TripwirePhoneGrepTest`**
- [ ] **Step 5: Commit**

---

## Task 12: Fix the image saver, then let visual language take pictures

**Files:**
- Modify: `Maugham/Editor/ImagePasteHandler.swift` — **not `Maugham/Stores/`**, which is where this plan's file table and the pre-written task both put it (**WAS WRONG**). `saveAndReferenceFile(from:forNoteAt:in:)` is at `:31-40`.
- Modify: `Maugham/Canvas/CanvasDrop.swift:368-389` (`isIngestableImage`, and the comment that predicts this task)
- Modify: `MaughamTests/TripwireGrepTests.swift:2100-2200` — `imageSaverCallers` is the set at `:2114-2118`
- Modify: `Maugham/Views/StatementPane.swift` / `Maugham/Views/StatementEditorHost.swift` (the drop/paste surface)
- Test: `MaughamTests/ImagePasteHandlerTests.swift` (existing, `:29`, `:59`), plus the visual-language ingest
- Read first: `Maugham/Stores/AREA.md`, the canvas asset-well section

**Interfaces:**
- Consumes: `ImagePasteHandler.saveAndReference(image:forNoteAt:in:)` — verified at `Maugham/Editor/ImagePasteHandler.swift:12-16`; `saveAndReferenceFile(from:forNoteAt:in:)` — `:31-35`. Both `public`, both `@discardableResult`, both return a Markdown ref string.
- Consumes: `CanvasDrop.isIngestableImage(_:) -> Bool` — `Maugham/Canvas/CanvasDrop.swift:381`; `DropClassification.action(hasFileURL:canLoadImage:)`, `.fileURL(from:)`, `.image(from:)` — `Maugham/Views/DropClassification.swift:23`, `:56`, `:79`.
- Consumes: `ProjectStore.statement(kind:scope:)` / `createStatement(kind:scope:)` (Task 3) and `Statement.path`.
- Produces: validation at the saver, and image ingest on the Visual Language pane.

**Enumerate the callers from the call sites; here is today's enumeration, and
re-run it before you commit.** `ImagePasteHandler` has two entry points and they
are not equally exposed:

| Caller | Entry point | Can a non-image reach it? |
|---|---|---|
| `Maugham/Views/ResearchNoteEditor.swift:116` | `saveAndReference(image:)` | No — it takes an `NSImage`, re-encoded to PNG at `ImagePasteHandler.swift:18-23` |
| `Maugham/Stores/ProjectStore+Palette.swift:145` | `saveAndReference(image:)` | No, same reason |
| `Maugham/Stores/ProjectStore+CanvasAssets.swift:45` | `saveAndReference(image:)` | No, same reason |
| `Maugham/Stores/ProjectStore+Palette.swift:155` | `saveAndReferenceFile(from:)` | **Yes** |
| `Maugham/Stores/ProjectStore+CanvasAssets.swift:55` | `saveAndReferenceFile(from:)` | **Yes** |

And the callers of *those* two, which is where the hole actually is:

| Reaches `saveAndReferenceFile` via | Validates? |
|---|---|
| `Maugham/Views/Palette/PaletteCardEditor.swift:365` (a Finder drop on a card's image well) | **No** |
| `Maugham/Stores/InboxStore.swift:305` (`promoteToPaletteCard`, `.image` arm) | Existence only (`:300-303`) |
| `Maugham/Stores/InboxStore.swift:387` (`sendToCanvas`, `.image` arm) | Existence only (`:380-383`) |
| `Maugham/Canvas/PromotionPerformer.swift:604` (`performPaletteCardImage`) | Source is already an owned canvas asset |
| `Maugham/Canvas/CanvasDrop.swift:409-411` (the canvas's external drop) | **Yes** — `isIngestableImage` |

**Contract:**

1. **The canvas's check is NOT in `DropClassification`.** The spec (§7) and the
   pre-written task both say *"The canvas's own check sits upstream in
   `DropClassification`"* — **WAS WRONG**. `DropClassification` decides only
   *file-URL vs rendered-bitmap vs ignore* (`DropClassification.swift:23-27`); it
   never asks whether a file URL is an image, so a Finder drag of a `.txt`
   classifies as `.fileURL` and passes straight through. The real check is
   `CanvasDrop.isIngestableImage(_:)` (`CanvasDrop.swift:381-389`), called at
   `:410`, and **its own doc comment at `:368-378` already specifies this task**:
   it names the shared saver, says it *"takes `pathExtension` as given and
   validates nothing"*, and says the hole *"is probably wider than the canvas"*.
   Read it before you write anything.

2. **Fix it at the saver.** `saveAndReferenceFile` is the one entry point a
   non-image can reach. `isIngestableImage`'s shape is already right — the file's
   real type first (`URLResourceValues.contentType`), the extension second,
   because a file with no extension still has a type on disk and a drag from a
   sandboxed source may not. **Move it, do not copy it**; a second spelling is
   the drift.

3. **Then re-check every caller in one pass and decide, per caller, what a
   refusal means to the writer.** A thrown error inside
   `PaletteCardEditor.handleDrop`'s `try?` (`:365`) is a silent no-op — the same
   silence `importExternal`'s unconditional `true` produced. Say what each one
   shows. Then decide whether `CanvasDrop.isIngestableImage`'s call site is now
   redundant or defence in depth, **and record which** — the canvas refuses with
   a named `outcome.refused` list, which is a better message than a thrown error,
   so "redundant" is not the obvious answer.

4. **The symptom that makes this worth doing, from `CanvasDrop.swift:371-377`:**
   an unchecked drop copies a `.txt` into the well, mints an owned node, draws
   the photograph glyph and queues a decode that can only fail — and
   `CanvasThumbnails` records an entry for a failed decode
   (`CanvasThumbnails.swift:333`, then skipped forever by the
   `guard entries[key] == nil` at `:326`), so it is one permanently dead cache
   entry per mistake.

5. **Only then does visual language ingest images** (spec §7, umbrella §3.2:
   visual language is *mixed — images, references and prose*).

6. **There is already a census of the saver's callers and you must answer to
   it.** `TripwireGrepTests.test_theSharedImageSaverIsCalledFromTheSeamsThatOwnAWell`
   (`:2148`) asserts equality against `imageSaverCallers`
   (`:2114-2118`) — **count that set, not this sentence**. Its rule is that a
   caller must be *a seam that owns a well of its own*. So visual language either
   (a) becomes a fourth entry with a line saying which well, or (b) routes
   through an existing seam. Note that `ImagePasteHandler.destination`
   (`:45-53`) derives `<slug>_assets/` from the note's own filename, so
   `visual-language.md` yields `visual-language_assets/` with no literal anywhere
   — which is the same property
   `test_theCanvasAssetWellIsDerivedAndNeverSpelledInCode` (`:2166`) protects for
   the canvas. **Whichever you choose, add the planted-offender companion**: an
   assertion whose grep matches nothing looks exactly like an assertion that
   passes.

7. **The statement's markdown is op-log truth.** An image ref inserted into a
   statement is text in a `Document`, so it goes in through the same
   `Document.setFullText` binding every keystroke takes
   (`StatementEditorHost.swift:47-55`) — **not** by writing the `.md`. A direct
   file write is discarded on re-materialize (hard invariant; external `.md`
   edits are not honoured).

8. **Named and NOT in this task:** `importExternal` returns `true`
   unconditionally, which is what made a failed canvas drop silent rather than
   springing back. Touching it is a scope change — say so if you do.

**Watch for:** changing behaviour in a drop path wants its own smoke, and this
one changes three surfaces the writer already uses.

- [ ] **Step 1: Write the failing tests**

- `test_theSaverRefusesAFileThatIsNotAnImage` — in
  `MaughamTests/ImagePasteHandlerTests.swift`. Falsified by removing the check:
  the `.txt` lands in the well.
- `test_aTextFileDroppedOnAPaletteCardIsRefusedAndSaysSo` — the caller that has
  no check today, asserted through its own path.
- `test_theCanvasDropStillRefusesItInItsOwnWords` — contract 3's control; the
  canvas's named-file refusal must not regress into a generic thrown error.
- `test_anImagePastedIntoVisualLanguageLandsInItsOwnWell` — assert the path, and
  assert the ref reached the **op log**, not the `.md` (contract 7).
- `test_everySaverCallerIsAccountedFor` is already
  `TripwireGrepTests`' job — update the census, run it, and run its
  planted-offender twin at `:2189`.

- [ ] **Step 2: Run them and watch them fail**
- [ ] **Step 3: Implement**
- [ ] **Step 4: Full Mac suite plus `MaughamTests/TripwireGrepTests`, plus a Release build**
- [ ] **Step 5: Commit**

---

# Finishing

- [ ] **Whole-branch review.** After the per-task reviews, one review of the full branch diff. It has found a Critical that no per-task review could see in **every slice of this milestone** — the emergent interactions are between tasks, which is precisely what a per-task diff cannot show. Give the reviewer the ledger of what changed and why.
- [ ] **Sweep sibling docs in the same commit as the change.** `CLAUDE.md` (the per-area table's OpLog, Stores, Views, MCP and Canvas rows), `Maugham/Stores/AREA.md`, `Maugham/MCP/AREA.md`, `docs/roadmap.md`, `docs/product.md`, `docs/problem-map.md`. Rule 10: when a roadmap item flips •→✓, sweep for now-false claims. **Four specific drifts were found during half one and are logged in the ledger rather than fixed on the spot:** CLAUDE.md's Build-flow section calls the `Maugham` scheme "Mac app + MaughamCore", which is true of the build and false of the tests (Task 1); `Maugham/OpLog/AREA.md` does not record that `resolveDocId`'s structure-before-statements lookup order is now load-bearing, which is stated only inside its own test (Task 2); `Maugham/Stores/AREA.md`'s "Palette + craft-intent seams (2026-07-09)" paragraph still describes `ProjectStore+CraftIntent.swift` as live (Task 8 deletes it); and the guide prose named in Task 8's contract 9. **Whoever sweeps must also re-read `Maugham/Stores/AREA.md`'s statement section, which says the craft-intent seam "is still present and still has consumers until Task 7 moves them" — Task 8 is what moves the last of them.**
- [ ] **Add the M1A roadmap entry.** 1A has none today.
- [ ] **Manual smoke — Denver runs it.** Launch → open a project with a legacy craft-intent note → confirm it was adopted with its content intact → type into the Intent pane → ⌘Z → ⌘Q → relaunch → the words are there and the history shows them. Then the same on the phone.
- [ ] **The whole-milestone M1 smoke**, then flip M1C's roadmap `~` to ✓.
- [ ] **The paired Mac + phone release.** Both, together, as for schema v2 and v3. Not before M1 is whole.

## Reviewer notes

- **Reviewers write their verdict to a file before replying.** Agents went idle without reporting three times on the previous slice, and the file is what survived every time.
- **Reviewers that run beat reviewers that read.**
- Two claims in the spec were deliberately falsifiable and a reviewer should try the remaining one.
  - **No new `OpKind`** (§2.4) — **attacked on Task 5 and it SURVIVED.** A keystroke into the mounted pane produces ops whose kind set is exactly `[.typingBurst]`, asserted by name; `Deriver.appliesToManuscript` gains no case and `OpUndoRegistrar` gains no inverse. The 1A handoff note's assumption was wrong. The paired release still stands on Task 1's manifest bump and the phone's lookup (spec §2.5), not on an op kind.
  - **The References pane is M2's** (§6.4) — still open, and Task 10 is where it is tested. If it is wrong, the spec is wrong and should be corrected rather than worked around.
- **A spec sentence can be wrong about the tree, and one was.** §7 says the canvas's image check *"sits upstream in `DropClassification`"*; it does not — it is `CanvasDrop.isIngestableImage`, and `DropClassification` never asks whether a file is an image at all. Task 12 carries the correction. A reviewer who reads a spec citation without opening the file is reading a claim.
