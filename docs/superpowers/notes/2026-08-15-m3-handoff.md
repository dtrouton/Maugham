# M3 handoff — P1 merged; P2/P3 unwritten (rule 11)

*Session record, 2026-08-15. M3's spec:
`docs/superpowers/specs/2026-08-14-m3-review-passes-design.md`. P1's plan:
`docs/superpowers/plans/2026-08-14-m3-p1-spine-and-board.md`. P1 merged to
local main at `f19c6be9`, UNPUSHED. P2 (the queue) and P3 (the loop) are
deliberately unwritten — plan them against the built code.*

## Session state (read this first)

Local main (`5eae2092`) is origin/main (`0f4b0cea`, the v0.28.0 line)
plus: the M3 spec, the P1 plan, P1's merge (`f19c6be9`), the flake-docs
reopening, and this handoff — ALL UNPUSHED. v0.28.0 (Mac) is released and
live; nothing about M3 is on origin yet. Denver is smoking P1 now; his
finds land as fix commits on main (the 3a/3b precedent). After the smoke:
push is fine (schema v6 only binds the RELEASE pairing, not the push), P2
is planned next against built code (rule 11), then P3; M3's release is
the paired one.

Multi-session notes: the register/experiments-clone session was quiet
today; `git fetch` BEFORE any reconcile (the stale-ref near-miss,
2026-08-13); the machine hosts several sessions — gates queue on the
machine-global lock, and one session's ~100 zombie `loadrun.sh` processes
pinned load at 155 mid-P1 (kill only what you can attribute).

## Process residue this session (for the next controller)

- **Sonnet implementer subagents repeatedly stalled** by ending their turn
  to "wait" on background runs/Monitor notifications that never reach a
  subagent. The dispatch-prompt fix that worked: an explicit "FOREGROUND
  gates only; poll with repeated Read calls; every turn you end without a
  tool call in flight is a stall" block. Opus implementers mostly didn't
  need it. Carry the block into every dispatch.
- **The whole-branch reviewer drifted into fixing** (invited by an
  ambiguous "that's a gap to fix" in its brief). Recovery that preserved
  both the work and the audit trail: have it commit ONE labeled wave with
  found-vs-fixed split in the report, then an INDEPENDENT scoped re-review
  audits the wave. It worked, but brief reviewers with "report, don't
  fix" explicitly.

## What P1 built (pointers)

`ReviewPass` + `effectiveReviewPasses` (manifest, **schema v6** — the
paired Mac+phone release constraint binds at M3's RELEASE, recorded in the
manifest's schema contract); `PassState` (lossless `.unknown(String)`);
`ReviewStatus.derived` + one `StatusSwatch` (the free-string `status` has
zero production writers; legacy strings read as fallback, never rewritten);
`setPassState` + the `PassLadder` in both inspectors; `ActivePassMemory`
(ui-state, WRITTEN on chip click, read by nothing until P2's queue);
`ReviewBoardPane` at Review's project altitude (chips click-to-navigate,
context-menu-to-set; reference rows thin; groups preserved with depth);
the pass editor in Project Settings (delete-all → presets return).

## Denver's P1 smoke (recorded, NOT yet run)

- Review + project row → the board; groups indent; a Collection reference
  piece is a thin chip-less row; empty project → the placeholder.
- Chip click → that chapter opens (same host, no flash) and the pass is
  remembered; chip right-click menu sets in-progress/done/skip; the board
  redraws; the outline/corkboard/binder swatches agree with the ladder.
- The inspector's ladder (both novel piece + collection piece) sets and
  clears states; the projected status line reads right; ⌘Z is NOT
  expected to undo a pass state (manifest metadata, not an op).
- Pass editor in Project Settings: rename (states survive), add, delete,
  drag-reorder BY THE GRIP while clicking into the name TextField still
  edits (the untested drag×TextField combination — N2), delete-all →
  presets return with the footer explaining; a blank rename disables Save.
- A NEW project and a sample project both arrive with no dots anywhere.
- Publish/Author unchanged: corkboard in Author, the book in Publish.

## Decisions

1. **N1 RULED (Denver, 2026-08-15): the current behaviour stands.** An
   untouched Save writing the presets as a stored customization is
   consistent with saving-as-an-act (the checkpoint's own semantics) —
   Save means "this is my pass list now", even if it equals the presets.
   No P2 work; the whole-branch review's scenario is recorded as intended
   behaviour, not a defect.
2. The M3-release pairing: schema v6 means the phone release ships WITH
   M3's Mac release (the M1A pattern) — scheduling, not design; still open.

## Environment (both platforms' gates)

**The wall-clock MCP family is REOPENED** (CLAUDE.md build flow, top):
`MCPServerLifecycleTests` hung a full gate indefinitely on a QUIET machine,
disable-experiment-proven environmental (reproduces on unmodified parents).
Interim protocol: full minus the three MCP classes + those three in
isolation. **A root-cause hunt on main is owed** — start at the
2026-07-29 note's reopened section (hypothesis space recorded there).

## P2 carries (named, from P1's ledger)

- The board ships WITHOUT the open-notes column (tripwire 4 — counting
  opens every doc); P2's cross-document annotation machinery adds it with
  a cache. P2's first named carry.
- `ActivePassMemory` gains its reader (the queue pane filters by it).
- The advisory-order nudge ("structure still open on this piece") is P2's.
- N1's fix if Denver rules for no-op Save.
- Deferred minors dropped on merit at review: T6 report count, T7
  vertical containment.
