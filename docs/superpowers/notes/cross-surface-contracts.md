# Cross-Surface Contracts (phone ↔ Mac)

> You were likely sent here by a failing tripwire test. A tripwire fires when
> surface code reimplements something both surfaces share. Find the contract
> below, call its choke-point (Tier 1) or satisfy its contract type (Tier 2),
> and the tripwire passes. Spec: docs/superpowers/specs/2026-06-03-cross-surface-contracts-design.md

Tiers: **1** shared implementation (one MaughamCore impl; surfaces call it) ·
**2** contracted divergence (shared decision + contract test in both targets) ·
**3** free divergence (recorded as having no cross-surface invariant).

Status: ✅ done · 🔧 planned (this milestone) · 📋 recorded-no-action.
Full audit + execution order: `docs/superpowers/notes/2026-06-03-cross-surface-audit.md`.

| Contract | Tier | Choke-point / contract type | Test | Status |
|---|---|---|---|---|
| op-log filename ↔ docId (parse) | 1 | `OpLogStore.docId(fromOpLogFilename:)` / `docIds(…)` — recognizes both the `.jsonl` tail and the sealed `<docId>.<slug>.seg<NNNN>.mzseg` segment (ADR 0016) | `OpLogFilenameTests` + `OpLogFilenameContractTests` + `OpLogStoreSegmentTests` (both); tripwire `test_noReachAroundOpLogFilenameParsing` | ✅ |
| op-log filename (build) | 1 | `OpLogStore.opLogFileURL(forDocId:deviceSlug:in:)` | builder round-trip (both); tripwire on hand-rolled `.jsonl` | ✅ |
| op-log segment filename (build) | 1 | `OpLogStore.segmentFileURL(forDocId:deviceSlug:index:in:)` | `OpLogStoreSegmentTests` + `.mzseg` grep tripwires (both targets, plan Task 11) | ✅ |
| inbox manifest filename | 1 | `InboxManifest.inboxManifestURL(forDeviceSlug:in:)` | builder round-trip (both) | ✅ |
| manifest filename literal | 1 | `ProjectManifest.fileName` | referenced, not literal | ✅ |
| manifest date strategy | 1 | `ProjectManifest.makeEncoder()` / `makeDecoder()` | round-trip (both) | ✅ |
| op/inbox Codable + dateEncoding | 1 | `JSONLAppendStore.dateEncoding` + shared types | existing round-trip tests | ✅ |
| ULID / ParagraphID / DeviceSlug / Deriver / AnnotationDeriver / merge | 1 | MaughamCore (single impl) | existing | ✅ |
| id-keyed tree traversal (find / contains / collect / mutate / remove / path rewrite over `StructureItem` + `ResearchItem`) | 1 | `TreeWalk.*` over the `TreeNode` protocol (MaughamCore); both `StructureItem`/`ResearchItem` conform. Surfaces call `TreeWalk` (`find(id:)`, `contains(id:)`, `first(in:where:)`, `collectIds`, `collect(in:where:)`, `leaves(in:)`, `mutate`, `remove`, `rewritePaths`, `idsByPath`) — do not re-implement recursive children-walks. The phone research reader's leaf-collect uses `TreeWalk.leaves(in:)`; no hand-rolled tree walks remain on either surface. | `TreeNodeTests` (MaughamCoreTests) | ✅ |
| anchor stripping | 1 | `MarkdownDisplayFilter` | existing | ✅ |
| annotation task-anchor strip in diff cards | 1 | `MarkdownDisplayFilter.stripTaskAnchorsInline` (Mac must call) | both | ✅ |
| Fountain inline emphasis spans | 2A | consume `FountainLine.inlineSpans` on both surfaces | both | ✅ |
| Fountain bold/italic emphasis | 2A | `ScreenplayEmphasis.contract(for:)` | `ScreenplayEmphasisContractTests` (both) | ✅ |
| Fountain section underline | 2A | `ScreenplayEmphasis.underline` field | `ScreenplayEmphasisContractTests` (both) | ✅ |
| Fountain display-uppercase | 2A | `ScreenplayUppercase.shouldDisplayUppercase(_:)` | both | ✅ |
| title-page per-key style | 2B | `TitlePageFieldStyle.style(forKey:)` (display-IR) | both | ✅ |
| annotation kind → SF Symbol | 2A | `AnnotationKind.systemImageName` | both | ✅ |
| annotation kind → label | 2A | `AnnotationKind.displayName` | both | ✅ |
| write-rule pins (claudeAccept materialize, monotonic writtenAt, manifest date) | 2 | Mac-side contract tests | both | ✅ |
| annotation lifecycle op set (`claudeAccept`/`claudeReject`/`claudeArchive`/`claudeAcceptRevert`) | 1 | Shared `AnnotationDeriver.derive`/`Deriver.derive` (MaughamCore) classify every lifecycle op the same way on both surfaces. `claudeAcceptRevert` (schema v2, 2026-07-08/09) joined the set as the inverse of `claudeAccept`: with `changes` populated it restores the pre-accept paragraph text (Mac ⌘Z); with empty `changes` it's a manuscript no-op (rewind path — the checkpoint restore already reverted the text) but still reopens the annotation (`AnnotationDeriver.resolution` special-cases `.claudeAcceptRevert` to `.open`, clearing `resolvedAt`/`userResponse`). The phone never AUTHORS a revert — `AnnotationWriter` builds only accept/reject/archive; ⌘Z and rewind are Mac-only UI. The phone's contract is read-only: it must derive a Mac-authored revert correctly. | `AcceptRevertOpTests` (MaughamCoreTests); `PhoneAnnotationIntegrationTests.test_macAcceptRevertWithChanges_phoneReadsBack_restoresTextAndReopens` + `test_macAcceptRevertChangesFree_phoneReadsBack_reopensAsManuscriptNoOp` (MaughamPhoneTests, JSONL-bytes round-trip) | ✅ |
| intra-Mac dedup: device id, manifest filename lookups, id minting | 1 | `MacDeviceID.current`; `ProjectManifest.fileName`; `ProjectStore.newId` | Mac suite | ✅ |
| Bootstrap / Materializer / checkpoints / inbox merge / heading-scale / etc. | 3 | single-surface or agreed | — | 📋 |
| manuscript read source (op log vs on-disk `.md`) | 2 | Mac reads manuscript content/sequence/anchors ONLY from the op log (DerivedManuscript / live Document, ADR 0018); the phone Read tab renders the on-disk `.md` (display only; anchors/sequence still derive from the op log via `AnnotationLoading`). Every production file-read is annotated `// adr-0018-ok: <reason>`; the phone display read (`CoordinatedFileIO.coordinatedRead`, reached from `DocumentReaderView`) is annotated as a contracted display read. | `test_noManuscriptFileReadsOutsideReconciler` (both targets) | ✅ |
| asterisk inline emphasis grammar (`*italic*`, `**bold**`, `***both***`, nesting) | 1+2A | `InlineEmphasisScanner` + `EmphasisTraits` (MaughamCore) is single source of truth; consumed by `MarkdownTokenizer` (prose) and `FountainTokenizer` (Fountain). iOS markdown reader keeps `AttributedString(markdown:)` but is pinned by `InlineEmphasisAppleParityTests` (MaughamCoreTests). Spec: `docs/superpowers/specs/2026-06-03-inline-emphasis-contract-design.md`. **Boundary:** asterisks only — underscore is each grammar's own concern. **Out-of-scope follow-ups:** (1) `_x_`/`__x__` unstyled on Mac prose editor (phone reader italicizes via Apple); (2) phone markdown reader strips emphasis markers (Apple) while Fountain reader fades them — strip-vs-fade inconsistency. | `InlineEmphasisAppleParityTests` | ✅ |
| strikethrough (`~~x~~`, prose only) | 2A | `EmphasisTraits.strikethrough` (MaughamCore) is the shared trait bit. Mac editor: `MarkdownTokenizer` scans via `InlineEmphasisScanner.scan(_:options:[.strikethrough])`, `ProseMode` paints `.strikethroughStyle` as an attribute (not a font trait). Publish: `InlineParser` passes `options: [.strikethrough]` into the same scanner (LaTeX `\st` via `soul`, XHTML `<s>`). Phone: relies on swift-cmark's native GFM strikethrough through `AttributedString(markdown:)` — no custom MaughamCore code path, so this is a **contracted divergence** (same visible behavior, different implementation) rather than tier 1. Fountain excludes strikethrough entirely on every surface (`~` is the lyric marker) — `FountainInline`/`FountainTokenizer` pass no `.strikethrough` option. Spec: `docs/superpowers/specs/2026-07-06-commonmark-fountain-expansion-design.md`. | Mac: emission-contract golden example (`EmissionContractTests`) + `MarkdownTokenizer` tests. Phone: no dedicated round-trip test yet (Apple's parser is trusted as-is) — filed as a gap, not a blocker. | ✅ |
| Fountain scene numbers (`#4A#`) | 3 | `FountainLine.sceneNumber: String?` (MaughamCore), populated by `FountainTokenizer.extractSceneNumber(from:)` off a `.sceneHeading` line, plus a `.sceneNumber`-kind `FountainInlineSpan` over the marker range (document-relative, computed against the RAW pre-strip line). **Free divergence, recorded not contracted:** Mac editor (`ScreenplayMode`) renders live source text (marker included) so the span lands on real on-screen text and fades it dim. Phone reader (`FountainSemanticRenderer`) renders `line.content`, which already had the marker stripped by the tokenizer — the span's raw-line-relative range no longer falls inside the shorter stripped string, so `attrRange`'s bounds check drops it silently: **the phone Read tab does not display the scene number at all**, faded or otherwise. Publish (`FountainNodeMapper` → `ProjectAST.sceneHeading(_:sceneNumber:)`) right-aligns it: LaTeX via `\scenenumber` (`LaTeXBodyEmitter`), XHTML via a `scene-number` span (`XHTMLBodyEmitter`). | Each surface's own tests (`FountainSceneNumberTests`, `FountainNodeMapperTests`, `LaTeXBodyEmitterTests`, `XHTMLBodyEmitterTests`) pin their own behavior; no test asserts Mac ≡ phone because they don't. | 📋 |
| publish inline + Fountain parsing = adapters over the shared scanner/tokenizer | 1 | Bringing the publish pipeline inside this registry for the first time (it sat entirely outside it per the 2026-07-06 audit's B4 finding). `Maugham/Publish/InlineParser.swift` and `Maugham/Publish/FountainInline.swift` are thin adapters over `InlineEmphasisScanner` (via `EmphasisRunConverter`) — not independent emphasis grammars. `Maugham/Publish/ProjectASTBuilder.swift` parses Fountain via the real `FountainTokenizer`, mapped through `FountainNodeMapper` — the previously-independent hand-rolled `parseFountain` classifier (audit A1: it leaked boneyard/notes/synopses into compiled output) is deleted. | `EmissionContractTests`, `FountainNodeMapperTests`, `MaughamTests/Publish/*` | ✅ |
| Markdown block segmentation (headings, lists, fenced code, GFM tables, blockquotes, thematic breaks, solo images) | 1 | `MarkdownBlockParser.parse(_:)` (MaughamCore) is the single block-splitter, replacing five independent ones (audit's structural root cause behind A3/A4 and section E rows 1, 6, 7, 8). Consumed by: phone Read tab (`MaughamPhone/Read/MarkdownBlocks.swift` adapter → `DocumentReaderView`), the in-app Guide/Help renderer (`Maugham/Views/GuideMarkdownView.swift`), research note previews (`Maugham/Views/ResearchNotePreviewPane.swift`), the syntax-help sheet (`Maugham/Views/SyntaxHelpSheet.swift`), and publish (`Maugham/Publish/ProjectASTBuilder.swift`, which maps `MarkdownBlock` into `ProjectAST.ProseNode` ahead of `LaTeXBodyEmitter`/`XHTMLBodyEmitter`). Each consumer is a thin `MarkdownBlock`-to-view-layer-type adapter, not a re-implementation of block-boundary detection. **Contracted divergences, not tier-1 uniformity, downstream of the shared parser:** ordered-list markers never carry the source's numeric value (`parseListMarker` returns `ordered: Bool` only), so every consumer renumbers sequentially from position — none round-trip the typed ordinal; a table degrades to literal pipe text in publish only (not in any UI surface); a nested blockquote renders fully nested on the phone Read tab (`MarkdownBlocks.Block.quote` + mutually-recursive `quoteView`) and in publish (`ProjectASTBuilder.mapProse` recurses into `ProseNode.blockquote`, both emitters nest `\begin{quote}`/`<blockquote>`), but flattens to one level of plain quoted text on research preview, Guide, and the syntax-help sheet (each has its own `flattenQuote` helper) — a genuine 3-flatten/2-recurse presentation split, not a bug; a leading table/image block followed immediately by prose (no blank line) splits into two paragraph nodes in publish — an intentional, ledger-sanctioned deviation from the pre-cutover glue behavior (Task 5 review, option (a)). | `MarkdownBlockParityCorpusTests` (MaughamCoreTests, permanent regression corpus keyed to the audit's section E table + B5 findings) + per-surface adapter tests + `ProjectASTBuilderTests` (publish split-pin tests) + `EMISSION.md` golden | ✅ |
