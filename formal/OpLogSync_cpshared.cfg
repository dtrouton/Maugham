\* FINDING 2 — models PRODUCTION AS IT SHIPS.
\* PerDeviceCheckpoints = FALSE is not a hypothetical: CheckpointStore.swift:27
\* points every device at one .maugham/checkpoints.jsonl. Expect
\* CheckpointNoLoss VIOLATED.
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT LocalNoLoss
INVARIANT CheckpointNoLoss
CONSTANTS
    Devices = {d1, d2}
    MaxOps = 2
    MaxSeals = 1
    MaxCps = 1
    PerDeviceFiles = TRUE
    PerDeviceCheckpoints = FALSE
    SealHasSuspensionPoint = FALSE
