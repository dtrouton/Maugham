\* FINDING 2, other direction — ADR 0012's pattern applied to the checkpoint
\* file. Identical to OpLogSync_cpshared.cfg but for PerDeviceCheckpoints.
\* Expect NO violation: this is the proof that partitioning is the fix.
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT LocalNoLoss
INVARIANT CheckpointNoLoss
CONSTANTS
    Devices = {d1, d2}
    MaxOps = 2
    MaxSeals = 1
    MaxCps = 1
    PerDeviceFiles = TRUE
    PerDeviceCheckpoints = TRUE
    SealHasSuspensionPoint = FALSE
